# 计算机网络 第7章 网络安全 — 体系化讲解

> 从第一性原理出发，逐层构建知识体系，涵盖全部小节及所有例题。

---

## 目录

- [7.1 网络安全概述](#71-网络安全概述)
  - [7.1.1 网络安全威胁](#711-网络安全威胁)
  - [7.1.2 安全的计算机网络](#712-安全的计算机网络)
  - [7.1.3 数据加密模型](#713-数据加密模型)
  - [补充：信息系统安全](#补充信息系统安全)
  - [补充：信息系统安全防护基本原则](#补充信息系统安全防护基本原则)
  - [📝 本节例题(含PDF原题)](#-本节例题pdf原题解析)
- [7.2 两类密码体制](#72-两类密码体制)
  - [7.2.1 密码学基本概念](#721-密码学基本概念)
  - [7.2.2 对称密钥密码体制](#722-对称密钥密码体制)
  - [7.2.3 公钥密码体制](#723-公钥密码体制)
  - [7.2.4 两类体制对比](#724-两类体制对比)
  - [📝 本节例题](#-本节例题-2)
- [7.3 数字签名与报文鉴别](#73-数字签名与报文鉴别)
  - [7.3.1 报文鉴别](#731-报文鉴别)
  - [7.3.2 散列函数](#732-散列函数)
  - [7.3.3 数字签名](#733-数字签名)
  - [📝 本节例题](#-本节例题-3)
- [7.4 认证协议](#74-认证协议)
  - [7.4.1 实体认证](#741-实体认证)
  - [7.4.2 密钥分发中心KDC](#742-密钥分发中心kdc)
  - [7.4.3 Kerberos协议](#743-kerberos协议)
  - [📝 本节例题](#-本节例题-4)
- [7.5 公钥基础设施与密钥分配](#75-公钥基础设施与密钥分配)
  - [7.5.1 公钥的分配](#751-公钥的分配)
  - [7.5.2 公钥基础设施PKI](#752-公钥基础设施pki)
  - [📝 本节例题](#-本节例题-4)
- [7.6 网络安全协议](#76-网络安全协议)
  - [7.6.1 网络层安全——IPSec](#761-网络层安全ipsec)
  - [7.6.2 传输层安全——SSL/TLS](#762-传输层安全ssltls)
  - [7.6.3 应用层安全——PGP](#763-应用层安全pgp)
  - [📝 本节例题](#-本节例题-5)
- [7.7 防火墙与入侵检测](#77-防火墙与入侵检测)
  - [7.7.1 防火墙](#771-防火墙)
  - [7.7.2 入侵检测系统](#772-入侵检测系统)
  - [📝 本节例题](#-本节例题-6)
- [附录：第7章完整定义与公式汇总](#附录第7章完整定义与公式汇总)

---

## 7.1 网络安全概述

### 7.1.1 网络安全威胁

#### 第一性原理：为什么需要网络安全？

原始的网络协议(TCP/I)P设计于"大家都是好人"的假定之上——没有考虑通信被窃听、数据被篡改、身份被冒充的可能性。随着网络从封闭的科研环境走向开放的商业和日常生活，这些"坏人"行为成为现实威胁。网络安全的核心目标就是：**在不安全的信道上实现安全通信**。

> **定义——网络安全(Network Securit)y**：保护网络系统及其数据免受非授权访问、使用、泄露、修改、破坏的综合性技术和管理体系。核心目标是在不可信的网络环境中实现信息的保密性、完整性、可用性和不可否认性。

#### 网络攻击的四个层次

| 攻击层次 | 攻击目标 | 攻击手段示例 |
|---------|---------|------------|
| **物理层** | 物理链路和设备 | 线路窃听、设备破坏 |
| **网络层** | 路由和寻址 | IP欺骗、路由攻击、DDoS |
| **传输层** | 连接和会话 | SYN泛洪、会话劫持 |
| **应用层** | 应用服务 | 病毒、蠕虫、SQL注入、XSS |

#### 被动攻击 vs 主动攻击

> **定义——被动攻击(Passive Attac)k**：攻击者只**观测和监听**通信而不干扰通信过程。主要包括**报文内容窃听**(直接读取通信内容)和**流量分析**(根据通信模式推断信息)。被动攻击难以检测，应以预防为主。

> **定义——主动攻击(Active Attac)k**：攻击者**主动修改、伪造或中断**通信。主要包括**假冒**(Masquerade)、**重放**(Replay)、**篡改报文**(Modification of Messages)和**拒绝服务**(Denial of Service, DoS)。主动攻击难以预防，应以检测和恢复为主。

```
被动攻击示意：                       主动攻击示意：
                                    
发送方 ───→ 接收方                   发送方 ───X──→ 接收方
          │                                  │
          ▼ (窃听)                            ▼
       攻击者                            攻击者
                                    (截获、篡改、伪造、阻断)
```

| 对比维度 | 被动攻击 | 主动攻击 |
|---------|---------|---------|
| **攻击行为** | 仅观察，不修改 | 修改、伪造、阻断 |
| **对通信影响** | 无直接影响 | 有直接影响 |
| **检测难度** | 极难检测(无痕迹) | 相对容易检测 |
| **防御策略** | **以防为主**(加密) | **以检测和恢复为主** |
| **典型手段** | 窃听、流量分析 | 假冒、重放、篡改、DoS/DDoS |
| **目的** | 获取信息内容 | 破坏系统/伪造信息 |

**三类主动攻击详解**(教材分类：篡改、恶意程序、拒绝服务)：

```
① 篡改报文 (Modification of Messages):
   A: "转账100元" → 攻击者截获并修改 → B收到: "转账10000元"
   还包括：假冒(Masquerade)——C伪装成A与B通信；
   重放(Replay)——C截获A的报文后重新发送给B

② 恶意程序 (Rogue Program / Malware):
   指侵入系统、破坏数据或窃取信息的恶意软件，包括：
   - 计算机病毒 (Virus)：寄生在文件中，需要用户"运行"才能激活，可自我复制
   - 计算机蠕虫 (Worm)：独立程序，无需宿主，通过网络自我传播
   - 特洛伊木马 (Trojan Horse)：伪装成正常软件，用户主动下载运行后激活
   - 逻辑炸弹 (Logic Bomb)：潜伏在系统中，满足特定条件(日期/事件)时触发破坏
   - 后门入侵 (Backdoor)：绕过正常安全机制的秘密入口
   - 流氓软件 (Rogue Software)：欺骗用户安装或付费的恶意软件

③ 拒绝服务 (Denial of Service, DoS):
   攻击者向服务器发送海量请求 → 服务器资源耗尽 → 合法用户无法访问
```

> **定义——分布式拒绝服务(Distributed Denial of Service, DDo)S**：若从互联网上成百上千的网站集中攻击一个网站，则称为分布式拒绝服务DDoS，又称**网络带宽攻击或连通性攻击**。DDoS通过控制大量"僵尸主机"(Bot)同时向目标发起攻击，威力远大于单点DoS。

### 7.1.2 安全的计算机网络

#### 为什么需要安全目标框架？

面对7.1.1中列举的各种威胁，我们需要一个**系统化的安全目标框架**来指导防御。网络安全不是某个单一技术的堆砌，而是一个多维目标体系——如果连"要保护什么"都不清楚，安全措施就是盲目的。

#### 计算机网络通信安全的四个目标

> **定义——安全的计算机网络(Secure Computer Networ)k**：一个安全的计算机网络应当达到以下四个目标：(1)**保密性 (Confidentiality)**：只有发送方和预定接收方才能理解报文内容；(2)**端点鉴别 (End-point Authentication)**：发送方和接收方都应能证实通信过程所涉及的另一方；(3)**信息的完整性 (Message Integrity)**：内容未被篡改；(4)**运行的安全性 (Operational Security / Access Control)**：通过访问控制确保系统的可用性。

```
             ┌──────────────────────────┐
             │   安全的计算机网络四目标    │
             ├──────────────────────────┤
             │ ① 保密性 (Confidentiality)│
             │    加密保护内容不被窃听    │
             ├──────────────────────────┤
             │ ② 端点鉴别 (Authentication)│
             │    确认通信对方的真实身份   │
             ├──────────────────────────┤
             │ ③ 完整性 (Integrity)     │
             │    确保数据未被篡改        │
             ├──────────────────────────┤
             │ ④ 运行的安全性 (Access    │
             │    Control / Availability)│
             │    控制访问权限，保证可用   │
             └──────────────────────────┘
```

| 目标 | 英文 | 核心问题 | 主要实现技术 |
|------|------|---------|------------|
| **保密性** | Confidentiality | 谁可以看到我的数据？ | 加密 (Encryption) |
| **端点鉴别** | End-point Authentication | 对方是谁？是否可信？ | 数字签名、口令、证书 |
| **完整性** | Message Integrity | 数据是否被修改过？ | 报文鉴别码、数字签名 |
| **运行的安全性** | Operational Security | 谁能访问什么？系统是否可用？ | 访问控制、防火墙、备份恢复 |

**网络安全目标的另一种常见表述**(CIA+AN模型)：

| 目标 | 英文 | 说明 |
|------|------|------|
| **机密性** | Confidentiality | 信息不泄露给非授权实体 |
| **完整性** | Integrity | 信息未经授权不能改变 |
| **可用性** | Availability | 授权实体可随时访问和使用 |
| **认证** | Authentication | 确认实体身份 |
| **不可否认性** | Non-repudiation | 行为不可事后抵赖 |

> **重要观点——网络安全是不可判定的(来自PDF教材)**：网络安全没有绝对的标准。只能根据具体应用场景制定相对的安全策略，在安全性和可用性之间寻求平衡。不存在"绝对安全的系统"。

#### 对付攻击的措施

| 攻击类型 | 对付措施 |
|---------|---------|
| **被动攻击(窃听)** | 加密——使窃听者无法理解截获的数据 |
| **主动攻击(篡改/伪造/Do)S** | 加密 + 鉴别 + 访问控制 + 备份与恢复系统 |
| **恶意程序** | 防病毒软件、入侵检测、访问控制 |

### 7.1.3 数据加密模型

#### 为什么需要加密模型？

如果要在不安全信道上传递信息，最直观的想法是把信息"伪装"成外人看不懂的样子——这就是加密。数据加密模型给出了密码通信的形式化框架，是后续所有密码体制讨论的基础。

> **定义——数据加密模型(Data Encryption Mode)l**：描述加密和解密过程的形式化框架。发送方用加密密钥K对明文X执行加密运算E，产生密文Y=E_K(X)；接收方用解密密钥对Y执行解密运算D，恢复明文D_K(Y)=D_K(E_K(X))=X。密钥K由密钥中心提供，通过安全信道传递。

```
══════════════ 数据加密模型 ══════════════

              截获/篡改(截取者/攻击者)
                    │
                    ▼
 发送方A ──→ 加密器E ──密文Y──→ 不安全信道 ──密文Y──→ 解密器D ──→ 接收方B
 明文X        ↗ 密钥K                           密钥K ↗       恢复明文X
             │                                           │
             └────── 安全信道(密钥分发) ────────────────┘
                  (可由密钥中心提供密钥)K
```

#### 加密模型的核心公式

```
(7-1)  加密:  Y = E_K(X)
(7-2)  解密:  D_K(Y) = D_K(E_K(X)) = X

符号说明：
  X — 明文 (Plaintext)，加密前的原始信息
  Y — 密文 (Ciphertext)，经加密后的不可读信息
  K — 密钥 (Key)，控制加密/解密过程的秘密参数(一串比特串)
  E — 加密算法 (Encryption Algorithm)
  D — 解密算法 (Decryption Algorithm)
```

#### 无条件安全与计算上安全

> **定义——无条件安全 (Unconditionally Secure)**：无论截获多少密文、拥有多少计算资源，都无法从密文中获得任何关于明文的信息。**一次一密 (One-Time Pad)** 是唯一已被证明无条件安全的密码体制。

> **定义——计算上安全 (Computationally Secure)**：在可接受的计算资源下，破解密码在计算上是不可行的——不是绝对不可能破解，而是破解的代价远超信息本身的价值，或破解所需时间远超信息的有效期。**现代实用的密码体制基本上都属于计算上安全**。

#### 代换与置换——密码设计的两个基本操作

密码学中，加密算法通过对明文进行**代换**和**置换**操作来混淆信息。这是理解所有现代密码(DES/AE)S的基础。

> **定义——代换 (Substitution)**：将明文中的每个元素(字母、比特等)按照某种规则**映射**为另一个元素。例如：将每个字母替换为字母表中向后3位的字母(凯撒密码)。

> **定义——置换 (Permutation)**：保持明文元素不变，但**重新排列**它们的顺序。例如：将"attack"重排为"kcaatt"。

```
代换示例(每个字母映射为另一个)：      置换示例(字母不变，改变顺序)：
  attack → ZTTRYX                        attack → KACATT
  (a→Z, t→T, c→R, k→Y, 注意每个字母      (字母不变，位置重排)
   映射到确定的字母——同一字母同一映射)
  
单表代换的弱点：保留了自然语言的统计
特性(字母频率分布不变)，可被统计分析
法破译。
```

#### 经典密码体制

##### 单表代换密码(Monoalphabetic Ciphe)r

```
示例：设代换规则为随机映射(每个字母唯一对应另一个字母)
  a→F, b→X, c→G, d→M, e→J, f→Q, g→R, h→L,
  i→S, j→V, k→P, l→B, m→C, n→M, o→Z, p→W,
  q→D, r→A, s→Y, t→X, u→H, v→O, w→N, x→E,
  y→T, z→K

明文:  attack on three am
密文:  FXXFGP ZQ XALMM FC

弱点：
① 保留了自然语言的统计特性(字母频率分布不变)
② 密钥空间 26! ≈ 4×10^26，虽极大但可通过统计分析法破译
   (单靠暴力穷举不可行——即使每秒10^12次尝试也需约1200万年)
③ 可用统计分析法破译

破译方法：
- 统计分析法：利用字母频率(如英文中e出现频率最高)，
  将密文中最频繁出现的字母猜测为e的密文，逐步还原
- 若已知是Caesar移位密码(密钥空间仅26)，可暴力穷举
```

##### 多表代换密码——Vigenère密码

> **定义——Vigenère密码 (Vigenère Cipher)**：一种多表代换密码，使用一个**关键词**作为密钥，密钥中的每个字母决定使用哪个代换表。第i个明文字母用密钥第(i mod 密钥长度)个字母确定的代换表加密。由于同一字母在不同位置可能被加密为不同字母，统计分析法失效。

```
Vigenère加密示例：

明文:   a t t a c k o n t h r e e a m
密钥:   p l a p l a p l a p l a p l a
       (重复使用关键词 "pla")

加密规则: 密文字母 = (明文字母位置 + 密钥字母位置) mod 26
          (a=0, b=1, ..., z=25)

明文:  a(0)  t(19) t(19) a(0)  c(2)  k(10) o(14) n(13) t(19) h(7)  r(17) e(4)  e(4)  a(0)  m(12)
密钥:  p(15) l(11) a(0)  p(15) l(11) a(0)  p(15) l(11) a(0)  p(15) l(11) a(0)  p(15) l(11) a(0)
密文:  P(15) E(4)  T(19) P(15) N(13) K(10) D(3)  Y(24) T(19) W(22) C(2)  E(4)  T(19) L(11) M(12)

密文: PETPNKDYTWCETLM

关键优势：明文中多次出现的字母(如a出现3次——第1、4、14位)，
在密文中被加密为P, P, L——其中第1和第4位因密钥周期为3
使用同一密钥字母p而结果相同，但第14位则因密钥字母不同
(l而非)p而产生不同密文。整体上同一明文字母在不同位置
可被加密为不同字母，使统计分析法失效。
```

---

### 📝 本节例题(PDF原题+解析)

**PDF例题7-0-1**(填空题)：网络安全的威胁可以分为两大类：即(\_\_\_\_\_\_)和(\_\_\_\_\_\_)。

**答案**：被动攻击、主动攻击。

---

**PDF例题7-0-2**(填空题)：在(\_\_\_\_\_\_)攻击中，攻击者只是观察通过某一个协议数据单元PDU，而不干扰信息流。

**解析**：被动攻击分为窃听(直接读取报文内容)和流量分析(观察通信模式推断信息)。该题描述的是流量分析——只观察PDU，不干扰信息流。

**答案**：流量分析。

---

**PDF例题7-0-3**(填空题)：(\_\_\_\_\_\_)攻击是指攻击者对某个连接中通过的PDU进行各种处理。

**解析**：主动攻击与被动攻击相对——攻击者主动修改、伪造或中断通信。题目中"对PDU进行各种处理"表明攻击者在主动干预通信过程。

**答案**：主动。

---

**PDF例题7-1**(多选题)：下列哪些行为属于主动攻击？

A. 窃听 B. 流量分析 C. 篡改 D. DoS E. 恶意程序

**解析**：主动攻击=攻击者主动修改/干扰通信。PDF教材将主动攻击分为三类：篡改、恶意程序、拒绝服务(DoS)。

**答案**：C, D, E。窃听和流量分析属于被动攻击(仅观察不干扰)。

---

**PDF例题7-2**(单选题)：拒绝服务攻击描述正确的是？

A. 属于被动攻击 B. 获取端口列表 C. 被攻击计算机无法正常工作 D. 属于对密码系统的破坏

**解析**：DoS通过发送海量请求耗尽目标资源，使目标无法服务合法用户。如教材所述："分布式拒绝服务DDoS"又称"网络带宽攻击或连通性攻击"。

**答案**：C。

---

**PDF例题7-3**(填空题)：篡改报文破坏了数据的\_\_\_\_\_\_。(完整性/一致性/保密性/可利用性)

**答案**：完整性。

---

**PDF例题7-4**(单选题)：网络安全的可用性是指：

A. 信息不泄露 B. 信息不被篡改 C. 合法用户可正常使用网络资源 D. 可以追踪通信方身份

**答案**：C。

---

**PDF例题7-5**(填空题)：伪装消息以隐藏其真实内容的过程称为\_\_\_\_\_\_。(加密/认证/解密/数字签名)

**答案**：加密。

---

### 补充：信息系统安全

#### 为什么需要了解信息系统安全？

前面讨论的网络安全侧重于通信信道的安全(加密、鉴别、协议等)，但在实际应用中，安全是一个**系统级问题**——设备本身是否可靠？数据存储在服务器上是否安全？发布的内容是否合规？用户行为是否可控？这些超出了传统网络安全的技术范畴，需要在**信息系统**的层面上统一考虑。

> **定义——信息系统安全(Information System Securit)y**：在信息系统生命周期中，保障其设备、数据、内容和行为安全的综合性体系。信息系统安全不仅涉及技术手段，还涉及管理和法律层面。

#### 信息系统安全的四个层面

| 层面 | 核心目标 | 说明 |
|------|---------|------|
| **设备安全** | 稳定性、可靠性、可用性 | 硬件设备不被破坏、能持续正常运行 |
| **数据安全** | 秘密性、完整性、可用性 | 数据不被泄露、篡改或丢失(包括备份恢复) |
| **内容安全** | 合法合规 | 信息内容符合国家法律法规、政治和道德标准 |
| **行为安全** | 秘密性、完整性、可控性 | 用户和管理员的操作行为合法、可审计、可追溯 |

```
信息系统安全四层面关系：

        ┌─────────────────────────────┐
        │        内容安全(最上层)      │ ← 信息内容合法合规
        ├─────────────────────────────┤
        │        行为安全              │ ← 用户行为可控可审计
        ├─────────────────────────────┤
        │        数据安全              │ ← 数据秘密/完整/可用
        ├─────────────────────────────┤
        │        设备安全(基础层)      │ ← 硬件设备稳定可靠
        └─────────────────────────────┘
        四层逐级支撑，上层依赖下层
```

### 补充：信息系统安全防护基本原则

面对信息系统的多层次安全需求，需要一套**系统化的防护原则**来指导安全策略的制定。这些原则是安全架构设计的指导思想。

**五项基本原则**：

| 原则 | 核心思想 | 说明 |
|------|---------|------|
| **① 安全隔离原则** | 主体、客体、访问操作和安全策略相互隔离 | 将系统分为主体(用户/进程)、客体(资源/数据)和访问操作，通过安全策略控制主体对客体的访问，实现"不该访问的碰不到" |
| **② 最小化原则** | 最小权限 + 最小泄露 | 每个主体只拥有完成其任务所**必需**的最小权限；系统只对外暴露**必需**的最少信息——"够用即可，多一分就是风险" |
| **③ 分权制衡原则** | 分离权限，互相制约 | 关键操作需要多人共同完成(如双人授权)，防止单人拥有过高权限——"不能让一个人说了算" |
| **④ 整体性原则(木桶原理)** | 安全强度 = 最弱一环的强度 | 系统的整体安全性取决于**安全防护最薄弱的那个环节**，必须对所有可能的安全漏洞进行全面防护，不能有明显短板——"一块短板毁掉整个水桶" |
| **⑤ 分层性原则** | 纵深防御(Defense in Dept)h | 不依赖单一防线，部署多层防护机制(网络边界+内网+主机+应用+数据)，每一层都提供独立防护——"一道墙挡不住，多道墙延缓攻击" |

> **辨析——分层性原则 vs 木桶原理**：木桶原理说的是"不能有短板"(每层都要足够强)；分层性原则说的是"不能只有一层"(要有多个层)。两者互补：既要多层防御(纵深)，又要每层都做好(不短板)。

---

## 7.2 两类密码体制

### 7.2.1 密码学基本概念

#### 为什么需要密码学？

7.1.3节介绍了数据加密模型——但加密只是手段。密码学是研究加密/解密背后数学原理的学科。在讨论具体算法（DES/AES/RSA）之前，需要先建立密码学的通用语言：什么是明文/密文/密钥？加密算法的安全性究竟依赖什么？这些基本概念是后续所有讨论的基石。

> **定义——密码学(Cryptography)**：研究如何在敌手存在的情况下进行安全通信的学科。**密码编码学**(Cryptography)设计加密算法，**密码分析学**(Cryptanalysis)破解加密算法。

#### 基本术语

| 术语 | 符号 | 定义 |
|------|------|------|
| **明文** | `M` / `P` (Plaintext) | 加密前的原始信息 |
| **密文** | `C` (Ciphertext) | 加密后的不可读信息 |
| **加密算法** | `E` (Encryption) | 将明文转换为密文的数学函数 |
| **解密算法** | `D` (Decryption) | 将密文恢复为明文的数学函数 |
| **密钥** | `K` (Key) | 控制加密/解密过程的参数 |
| **加密过程** | `C = E_K(M)` | 用密钥K加密明文M得到密文C |
| **解密过程** | `M = D_K(C)` | 用密钥K解密密文C得到明文M |

```
密码通信模型：

                       攻击者(窃听/分析)
                            │
                            ▼
发送方A ───→ 加密器 ──密文C──→ 信道 ──密文C──→ 解密器 ───→ 接收方B
                              (不安全)
  密钥Ke ↗                          密钥Kd ↘
```

> **核心原则——Kerckhoffs原则(柯克霍夫原则)**：密码系统的安全性应**完全依赖于密钥的保密**，而不依赖于算法的保密。也就是说，加密/解密算法可以完全公开，只要密钥不泄露，系统就是安全的。这是因为：①公开算法可以接受全球密码学者的检验，发现漏洞；②隐匿算法("通过隐匿实现安全")一旦泄露则全军覆没。

### 7.2.2 对称密钥密码体制

#### 为什么对称密钥密码是基础？

密码学的核心操作是"加密"和"解密"。最自然的想法是：加密和解密用同一把密钥——就像一把物理锁的钥匙，锁门和开门用同一把。这带来了密码学中历史最悠久、速度最快的体制。但也带来了一个根本难题：双方如何在不安全的信道上安全地共享这把密钥？

> **定义——对称密钥密码体制(Symmetric-Key Cryptography / 私钥密码体制)**：加密和解密使用**相同密钥**(或解密密钥可由加密密钥容易推导)的密码体制。发送方和接收方必须事先通过安全渠道共享密钥。

#### 对称加密模型

```
        同一个密钥K(或由K1易推出K2)

发送方A                         接收方B
  │                              │
  ▼                              ▼
明文M ──→ 加密E_K(M)=C ──→ 解密D_K(C)=M ──→ 恢复明文
            不安全信道
```

#### 对称密码的分类

| 类型 | 原理 | 代表算法 | 特点 |
|------|------|---------|------|
| **流密码(Stream Cipher)** | 逐位(bit)加密明文流，密钥流与明文流逐位异或 | RC4, A5/1(GSM) | 速度快、无错误扩散、适用于连续数据 |
| **分组密码(Block Cipher)** | 将明文分成固定长度的块，逐块加密 | DES, 3DES, AES, SM4 | 安全性高、适用于离散数据 |

#### 分组密码的工作模式

分组密码一次处理一个固定长度的块(如AES块长128位)，对于长明文需要选择工作模式：

| 模式 | 全称 | 原理 | 误差传播 | 并行 |
|------|------|------|---------|------|
| **ECB** | 电子密码本 | 每块独立加密，同明文→同密文 | 无扩散 | 可并行加解密 |
| **CBC** | 密码分组链接 | 每块先与前一块密文异或再加密，需IV | 有扩散 | 解密可并行 |
| **CFB** | 密码反馈 | 将分组密码转为流密码 | 有扩散 | 加密不可并行 |
| **OFB** | 输出反馈 | 密钥流独立生成，与明文无关 | 无扩散 | 不可并行 |
| **CTR** | 计数器 | 用计数器值加密产生密钥流，与明文异或 | 无扩散 | 可并行加解密(最佳) |

> **辨析——ECB vs CBC**：ECB每个明文块独立加密，相同的明文块产生相同的密文块(可看出明文模式)，是最弱的模式，应避免使用。CBC通过链接机制使每块密文依赖前一块密文，相同明文产生不同密文，安全得多。实际应用首选CBC和CTR。

#### 数据加密标准(DE)S

> **定义——数据加密标准(Data Encryption Standard, DE)S**：IBM研发、1977年被NBS(美国国家标准局，1988年更名为NIS)T标准化的对称分组密码。明文64位分组，密钥56位(8位奇偶校验)，通过16轮Feistel置换产生64位密文。

```
DES基本参数：
明文分组长度: 64 bits
密钥长度:      56 bits (有效) + 8 bits (奇偶校验) = 64 bits
轮数:         16 轮
算法结构:     Feistel网络
输出:         64 bits 密文

密钥空间: 2^56 ≈ 7.2 × 10^16
```

**DES的安全性**：56位密钥在当年被认为足够安全。然而随着计算能力增长和专用硬件出现，DES被证明不再安全——1997年6月distributed.net/DESCHALL项目以纯软件方式耗时约96天完成首次公开DES破解(RSA DES Challenge )I；1998年EFF建成专用破解硬件Deep Crack(造价约25万美元)，1998年7月与distributed.net合作耗时39天完成第二次公开破解(RSA DES Challenge II-1)，1999年1月第三次破解(RSA DES Challenge II)I仅用22小时15分钟——标志着DES已不再安全。2001年NIST正式选定AES为DES的替代标准。

#### 三重DES(3DES / Triple DE)S

> **定义——三重DES(3DE)S**：通过对明文执行三次DES操作来增强安全性。两种模式：①**3DES-EEE**：用三个不同密钥加密三次 `C = E_K3(E_K2(E_K1(M)))`；②**3DES-EDE**：加密-解密-加密 `C = E_K3(D_K2(E_K1(M)))`。

```
3DES-EDE (最常用):
- 两密钥方案: K1=K3≠K2, 密钥总长128位(有效112位)
- 三密钥方案: K1≠K2≠K3, 密钥总长192位(有效168位)

C = E_K3( D_K2( E_K1( M ) ) )
```

#### 高级加密标准(AE)S

> **定义——高级加密标准(Advanced Encryption Standard, AE)S**：NIST于2001年选定的取代DES的新一代对称加密标准，选用了比利时密码学家Daemen和Rijmen设计的**Rijndael**算法。基于**代换-置换网络(SPN)**结构，支持128位分组和128/192/256位三种密钥长度。

| 参数 | AES-128 | AES-192 | AES-256 |
|------|---------|---------|---------|
| **分组长度** | 128 bits | 128 bits | 128 bits |
| **密钥长度** | 128 bits | 192 bits | 256 bits |
| **加解密轮数** | 10 轮 | 12 轮 | 14 轮 |
| **密钥空间** | 2^128 | 2^192 | 2^256 |
| **状态矩阵** | 4×4 字节 | 4×4 字节 | 4×4 字节 |

#### AES的一轮操作(SPN结构)

```
AES每轮包含四个步骤(最后一轮无MixColumn)s：

① SubBytes    (字节代换)   ── 查S盒(非线性的8位替换表)
② ShiftRows   (行移位)     ── 第i行循环左移i字节
③ MixColumns  (列混合)     ── 每列做GF(2^8)上的矩阵乘法
④ AddRoundKey (轮密钥加)    ── 状态矩阵与轮密钥逐位异或(XOR)

Key Schedule (密钥扩展):
原始密钥 → 扩展为(Nr+1)个轮密钥  (Nr = 轮数)
```

#### 对称密钥的优缺点

| 优点 | 缺点 |
|------|------|
| 加解密速度快(适合大数据量) | 密钥分发困难——需要安全信道共享密钥 |
| 数学运算简单高效 | 密钥管理复杂——N个用户需要N(N-1)/2对密钥 |
| 硬件实现效率高(AES有硬件指令) | 不支持灵活的签名和认证(无法区分发送方和接收方) |
| 安全性易于证明(密钥越长越安全) | 无法提供不可否认性 |
| 适合加密通信和存储 | 缺少非对称体制的密钥管理便捷性 |

```
对称密钥管理的"n²问题"：
N个用户彼此安全通信需要的密钥对数量：

K = N(N-1)/2

N=100  → K=4,950 个密钥
N=1000 → K=499,500 个密钥
```

### 7.2.3 公钥密码体制

#### 为什么需要公钥密码？

对称密码的根本缺陷在于：**通信双方必须通过安全的独立信道共享密钥**。如果你能安全地传送密钥，为什么不直接安全地传送信息本身？公钥密码的发明(Diffie & Hellman, 1976)颠覆了这个前提——密码信息可以全部在公开信道上传输。

> **定义——公钥密码体制(Public-Key Cryptography / 非对称密码体制)**：使用一对数学相关的密钥：**公钥(Public Key) PK**可以公开分发，**私钥(Private Key) SK**必须严格保密。公钥加密→私钥解密；私钥签名→公钥验证。由公钥推导私钥在计算上不可行。

#### 公钥加密模型

```
                        公钥目录
                    ┌──────────────┐
                    │ B的公钥: PK_B │
                    │ C的公钥: PK_C │
                    └──────┬───────┘
                           │(公开获取)
                           │
发送方A ──→ 加密 ──密文C──→ 不安全信道 ──密文C──→ 解密 ──→ 接收方B
          E_PK_B(M)=C                           D_SK_B(C)=M
            ↑                                      ↑
            │ (B的公钥)                              │ (B的私钥)
            └────────────────────────────────────────┘
                      公钥可以从公开目录获取    只有B知道自己的私钥
```

#### 公钥密码的核心要求

1. 产生密钥对(PK, SK)在计算上容易
2. 已知PK和加密算法E，产生密文 `C = E_PK(M)` 计算上容易
3. 已知SK和密文C，解密 `M = D_SK(C)` 计算上容易
4. **已知PK而不知SK，由C还原M在计算上不可行** ← 安全根基
5. **已知PK而不知SK，由PK推导SK在计算上不可行** ← 基于数学难题

#### RSA算法

> **定义——RSA算法**：由Rivest、Shamir和Adleman于1978年提出的第一个实用公钥密码算法，基于**大整数分解**的数学难题(两个大素数的乘积容易算，但乘积分解回素因数极其困难)。可用于加密和数字签名。

```
══════════════ RSA密钥生成 ══════════════

① 选择两个大素数 p 和 q (通常各512位以上)
② 计算 n = p × q
③ 计算 φ(n) = (p-1)(q-1)   ← 欧拉函数
④ 选择公钥指数 e: 1 < e < φ(n)，且 gcd(e, φ(n)) = 1
   (常用e = 65537 = 2^16 + 1)
⑤ 计算私钥指数 d: d ≡ e^(-1) (mod φ(n))
   即 e × d ≡ 1 (mod φ(n))
   (通过扩展欧几里得算法求模逆)

公布: 公钥 PK = {e, n}
保密: 私钥 SK = {d, n}
销毁: p, q, φ(n)
```

```
══════════════ RSA加密与解密 ══════════════

加密(使用接收方B的公钥):
  C = M^e mod n

解密(使用接收方B的私钥):
  M = C^d mod n

数学保证:
  C^d mod n = (M^e)^d mod n = M^(ed) mod n = M mod n

  (因为 ed ≡ 1 mod φ(n)，由欧拉定理保证)
```

**RSA计算示例(小数字演示)**：

```
① 选择 p = 3, q = 11
② n = 3 × 11 = 33
③ φ = (3-1)(11-1) = 2 × 10 = 20
④ 选 e = 3  (满足gcd(3,20)=1)
⑤ 求d: 3d ≡ 1 mod 20
   d = 7  (因为3×7=21≡1 mod 20)

公钥: PK = {e=3, n=33}
私钥: SK = {d=7, n=33}

加密：设明文 M = 15 (要求 M < n)
  C = 15^3 mod 33 = 3375 mod 33 = 9

解密：
  M = 9^7 mod 33 = 4782969 mod 33 = 15 ✓

验证：15恢复！
```

#### RSA的安全性

| 依赖难题 | 攻击方法 | 防御手段 |
|---------|---------|---------|
| 大整数分解难题 | 数域筛法(GNFS) | p和q各≥1024位(RSA-2048) |
| 计算φ(n)需要知道p和q | 试除法 | 使用足够大的素数 |
| 共模攻击 | 对同一n用不同e加密相同M | 不同用户使用不同的n |

**密钥长度与安全强度**：

| RSA密钥长度 | 等效安全性 | 状态 |
|-----------|-----------|------|
| RSA-512 | ~50位对称密钥 | 已被破解 |
| RSA-1024 | ~80位对称密钥 | 不再安全 |
| RSA-2048 | ~112位对称密钥 | **当前推荐最低标准** |
| RSA-4096 | ~128位对称密钥 | 长期安全 |

### 7.2.4 两类体制对比

| 对比维度 | 对称密钥密码 | 公钥密码 |
|---------|------------|---------|
| **密钥关系** | 加密密钥 = 解密密钥 | 公钥 ≠ 私钥 |
| **密钥数量** | N方需要 N(N-1)/2 对密钥 | N方需要 2N 个密钥(每方一对) |
| **密钥分发** | 需要安全信道事先共享 | 公钥可公开，私钥只需自己知道 |
| **加密速度** | 快(位/块操作，硬件支持) | 慢(大整数模幂运算，~100-1000倍慢) |
| **典型密钥长度** | AES-128/192/256 | RSA-2048/4096 |
| **算法** | DES, 3DES, AES, SM4 | RSA, ElGamal, ECC, SM2 |
| **安全基础** | 混乱(Confusion)+扩散(Diffusion) | 数学难题(大整数分解/离散对数/椭圆曲线) |
| **主要用途** | 数据加密(大数据量) | 密钥交换、数字签名、身份认证 |
| **不可否认性** | 不能提供(双方都有同一密钥) | 可以提供(通过数字签名) |

> **核心辨析——对称 vs 公钥：为什么我们不是"二选一"？** 在实际系统中，两种体制互补使用。对称加密速度快但密钥分发困难；公钥密码密钥分发方便但速度慢。最佳实践是**用公钥密码安全交换/协商对称会话密钥，然后用对称密钥加密实际通信数据**(混合密码系统，如SSL/TLS/HTTP)S。

---

### 📝 本节例题-2

**例题 7-3**：RSA中，已知 p=5, q=11，公钥 e=3，明文 M=9，求密文C并进行解密验证。

**解析**：先计算n和φ(n)，再用扩展欧几里得求d，最后加密解密。

**答案**：
```
① n = p × q = 5 × 11 = 55
② φ(n) = (5-1)(11-1) = 4 × 10 = 40
③ 验证 e=3 有效: gcd(3, 40) = 1 ✓
④ 计算 d:
   3d ≡ 1 (mod 40)
   3 × 27 = 81 ≡ 1 (mod 40)  →  d = 27

   公钥 PK = {e=3, n=55}
   私钥 SK = {d=27, n=55}

⑤ 加密: C = M^e mod n = 9^3 mod 55 = 729 mod 55 = 14

⑥ 解密: M = C^d mod n = 14^27 mod 55

   分步计算:
   14^1 mod 55 = 14
   14^2 mod 55 = 196 mod 55 = 31
   14^4 mod 55 = 31^2 mod 55 = 961 mod 55 = 26
   14^8 mod 55 = 26^2 mod 55 = 676 mod 55 = 16
   14^16 mod 55 = 16^2 mod 55 = 256 mod 55 = 36

   27 = 16 + 8 + 2 + 1
   M = 36 × 16 × 31 × 14 mod 55
     = 36 × 16 mod 55 = 576 mod 55 = 26
     = 26 × 31 mod 55 = 806 mod 55 = 36
     = 36 × 14 mod 55 = 504 mod 55 = 9 ✓

密文 C = 14，解密成功恢复 M = 9
```

---

**例题 7-4**：在RSA中，若 p=17, q=31，求加密指数e=7时的解密指数d。加密M=2，求C。

**解析**：n=527，φ=16×30=480。求d使7d≡1(mod 480)。

**答案**：
```
n = 17 × 31 = 527
φ(n) = 16 × 30 = 480
e = 7

扩展欧几里得算法求d:
480 = 7 × 68 + 4
7 = 4 × 1 + 3
4 = 3 × 1 + 1
3 = 1 × 3 + 0

回代:
1 = 4 - 3 × 1
  = 4 - (7 - 4 × 1) × 1
  = 4 × 2 - 7 × 1
  = (480 - 7 × 68) × 2 - 7 × 1
  = 480 × 2 - 7 × 137

所以 -137 ≡ 343 (mod 480)
验证: 7 × 343 = 2401 = 480 × 5 + 1 ≡ 1 (mod 480) ✓

d = 343

加密: C = 2^7 mod 527 = 128
```

---

**例题 7-5**：对N个用户的系统，对称密码系统需要多少个密钥？公钥密码系统呢？

**答案**：
- **对称密码**：每个通信对需要一对密钥，N方需要 **N(N-1)/2** 对密钥。如100个用户，需要4,950个不同密钥。
- **公钥密码**：每个用户只需一对密钥(公钥+私钥)，N方需要 **N对密钥**(N个私钥 + N个公钥)。如100个用户，各2个密钥共计200个密钥(只有100个私钥需要严格保密)。

---

**PDF例题7-6**(单选题)：DES的密文分组长度和有效密钥长度分别是？

A. 56bit, 128bit B. 64bit, 56bit C. 64bit, 64bit D. 64bit, 128bit

**答案**：B。DES以64位为分组，其中有效密钥56位(另8位为奇偶校验位)。

---

**PDF例题7-7**(多选题)：关于AES算法的说法正确的是？

A. 属于非对称加密算法 B. 属于分组密码 C. 密钥固定为64位 D. 算法是公开的

**答案**：B, D。AES是对称分组密码(非"非对称")，支持128/192/256位密钥(非"固定64位")，算法完全公开。

---

**PDF例题7-8**(填空题)：所谓(\_\_\_\_\_\_)密码体制，即加密密钥与解密密钥相同。所谓(\_\_\_\_\_\_)密码体制，即使用不同的加密密钥与解密密钥。

**答案**：对称(或私钥)；公钥(或非对称/双钥)。

---

**PDF例题7-9**(单选题)：关于双钥密码体制的说法中错误的是？

A. 可以公开加密密钥 B. 密钥管理问题比较简单 C. 可以用于数字签名 D. 加解密处理速度快

**答案**：D。公钥密码(双钥体制)的加解密速度比对称密码慢得多(大整数模幂运算)，"速度快"是错误说法。A/B/C都是公钥密码的正确特性。

---

## 7.3 数字签名与报文鉴别

### 7.3.1 报文鉴别

#### 为什么需要报文鉴别？

加密解决保密性(外人看不懂)，但解决不了完整性——接收方收到密文后解密，如何确定这不是攻击者篡改后的内容？报文鉴别提供了这个保证。

> **定义——报文鉴别(Message Authenticatio)n**：验证报文**确实来自声称的发送方**(真实性)且**在传输中未被修改**(完整性)。报文鉴别与保密性相互独立——可以有鉴别无加密，也可有加密无鉴别。

#### 报文鉴别码(MA)C

> **定义——报文鉴别码(Message Authentication Code, MA)C**：发送方将报文M和共享密钥K输入MAC函数(常用CBC-MAC或HMA)C，生成固定长度的鉴别码。接收方对收到报文计算MAC'，与收到的MAC比较——一致则未被篡改且来自声称的发送方。

```
MAC生成与验证：

发送方A                         接收方B
  │                              │
  ├─ M, K ──→ MAC函数 ──→ MAC    │
  │                              │
  └──→ 发送 (M, MAC) ────────────→ ├─ 收到 (M', MAC)
                                 │
                                 ├─ M', K ──→ MAC函数 ──→ MAC'
                                 │
                                 └─ 比较: MAC' == MAC ?
                                      是 → 验证通过
                                      否 → 被篡改/假冒！
```

> **MAC vs 加密**：MAC不提供保密性——报文M可以是明文。MAC只提供**完整性+认证**。如果你需要保密性，可以在计算MAC后对(M+MAC)加密。关键在于：**攻击者可以修改M，但没有K就无法计算新的MAC**。

### 7.3.2 散列函数

#### 为什么需要散列函数？

数字签名中，如果直接对报文用RSA签名，速度极慢（RSA模幂运算比对称加密慢100-1000倍），且RSA只能处理小于模数n的数据。能否先对报文"压缩"成一个短小的指纹，再对这个指纹签名？散列函数就是这个"压缩器"——将任意长度的报文压缩成固定长度的摘要，同时保证两个不同报文产生相同摘要在计算上不可行。它是数字签名、报文完整性校验的数学基础。

> **定义——密码散列函数(Cryptographic Hash Function)**：将任意长度的输入映射为固定长度输出(散列值/消息摘要)的单向函数。记为 `h = H(M)`。核心特性：①**单向性**(由h推M在计算上不可行)；②**抗弱碰撞性**(给定M，找M'≠M使H(M')=H(M)不可行)；③**抗强碰撞性**(找任何M≠M'使H(M)=H(M')不可行)。

```
散列函数特性：
┌─────────────────────────────────────────────────┐
│ 输入: 任意长度 (一个字节到几个GB都行)              │
│ 输出: 固定长度 (MD5→128位, SHA-1→160位, SHA-256→256位)│
│ 单向: 由h求M在计算上不可行                         │
│ 抗碰撞: 找不到两个不同输入有相同散列值              │
│ 雪崩效应: 输入改变1位，输出约一半位发生变化          │
│ 速度: 快 (无需密钥，计算效率高)                    │
└─────────────────────────────────────────────────┘
```

**常见散列算法**：

| 算法 | 输出长度 | 安全性 | 状态 |
|------|---------|--------|------|
| **MD5** | 128 bits | 已被破解(2004年找到碰撞) | **不推荐使用** |
| **SHA-1** | 160 bits | 已被破解(2017年Google找到碰撞) | **不建议使用** |
| **SHA-2 家族** | 224/256/384/512 bits | 安全 | **当前推荐** |
| **SHA-3** | 224/256/384/512 bits | 安全 | 新一代标准 |
| **SM3** | 256 bits | 安全 | 中国国家标准 |
| **BLAKE3** | 256 bits/无限 | 安全 | 新一代高性能 |

**散列函数的应用**：

| 应用 | 说明 |
|------|------|
| **报文完整性校验** | 发送方计算H(M)，接收方比较H(M')和收到的H(M) |
| **数字签名** | 先散列H(M)再对散列值签名(比直接对长报文签名高效) |
| **口令存储** | 存储H(password+盐)而非明文密码 |
| **HMAC** | 带密钥的散列，`HMAC(K, M) = H((K⊕opad) ∥ H((K⊕ipad) ∥ M))` |
| **区块链** | 工作量证明、Merkle树、交易ID |

**HMAC (基于散列的报文鉴别码)**：

```
HMAC(K, M) = H( (K ⊕ opad) ∥ H( (K ⊕ ipad) ∥ M ) )

符号说明：
  H      — 密码散列函数（如SHA-256）
  K      — 共享密钥
  M      — 报文
  opad   — 外部填充 (Outer Padding): 常量 0x5c5c...5c（与散列块长等长）
  ipad   — 内部填充 (Inner Padding): 常量 0x3636...36（与散列块长等长）
  ⊕      — 按位异或 (XOR)
  ∥      — 连接 (Concatenation)

HMAC双层结构的设计意图：
  外层散列 H(K⊕opad ∥ inner)  — 即使内层散列被攻破，
  仍提供额外安全保护，比单纯 H(K ∥ M) 更安全。
```

> **辨析——散列函数 vs MAC**：散列函数**不需要密钥**，任何人都可以计算H(M)并验证。这意味着散列值本身不能防止攻击者修改M后重新计算散列值。MAC引入了共享密钥K，攻击者不知道K，所以修改M后无法计算出正确的MAC。简单说：**散列告诉你数据变了没变，MAC告诉你数据是谁发的并且变了没变**。

### 7.3.3 数字签名

#### 第一性原理：现实签名的数字化

手写签名证明"我同意这份文件的内容"——但复制一份手写签名到另一份文件上是容易的，这是电子签名的根本问题。数字签名必须在**每个报文上都不同**且**与报文内容绑定**——对报文内容改动意味着签名失效。

> **定义——数字签名(Digital Signatur)e**：一种基于公钥密码的技术，签名者使用**私钥**对报文(或其散列值)签名，验证者使用签名者的**公钥**验证签名。提供**完整性**(签过名的报文不可修改)、**认证**(可确认签名者身份)和**不可否认性**(签名者事后无法否认签名)。

#### 数字签名过程

```
══════════════ 数字签名与验证流程 ══════════════

                      签名过程(发送方)A：
                      ┌─────────────┐
        私钥SK_A ────→│   签名算法   │
                      │   (RSA等)   │──→ 数字签名S
散列值 H(M) ─────────→│             │
                      └─────────────┘

                      验证过程(接收方)B：
                      ┌─────────────┐
        公钥PK_A ────→│   验证算法   │
                      │   (RSA等)   │──→ H(M') == 解密(S) ?
        签名S ────────→│             │
                      └─────────────┘

发送方A:                                       接收方B:
① 计算 h = H(M)                               ① 计算 h' = H(M')
② 用私钥签名: S = E_SK_A(h)                   ② 用A公钥解密: h = D_PK_A(S)
③ 发送 (M, S)                                 ③ 比较 h' == h ?
                                                 是→验证通过
                                                 否→报文被篡改或非A签名
```

#### RSA数字签名

RSA同时支持加密和签名，但两者的密钥使用方向相反：

| 操作 | 密钥方向 | 说明 |
|------|---------|------|
| **RSA加密** | 用**接收方公钥**加密 → 接收方用**私钥**解密 | 保密性：只有接收方能解密 |
| **RSA签名** | 用**发送方私钥**签名 → 验证者用**发送方公钥**验证 | 认证+不可否认性：只有发送方能生成该签名 |

```
RSA数字签名公式：

签名:  S = M^d mod n      (发送方用私钥SK={d,n})
验证:  M = S^e mod n ?= M' (验证方用发送方公钥PK={e,n})

(实际应用中是对散列值h=H(M)签名: S = h^d mod n)
```

#### 数字签名的安全属性

| 属性 | 如何保证 | 攻击手段 |
|------|---------|---------|
| **不可伪造** | 只有知道私钥SK才能生成有效签名 | 私钥泄露则签名可被伪造 |
| **不可否认** | 签名需私钥，只有签名者拥有私钥 | 声称"私钥被盗"需要时间戳等辅助 |
| **不可修改** | 改动M→H(M')≠H(M)→签名不匹配 | 找到散列碰撞可伪造签名(选择安全散列函数) |
| **不可重用** | 签名绑定具体报文内容 | 攻击者截获(M,S)无法用于(M',S) |

> **辨析——数字签名 vs 手写签名**：手写签名对每份文件都相同(可复制到其他文件)，验证靠笔迹比对(主观)。数字签名对每个报文都不同(绑定报文内容)，验证靠数学算法(客观确定)。手写签名的"不可伪造"靠物理难度；数字签名的"不可伪造"靠计算不可行性。

---

### 📝 本节例题-3

**例题 7-6**：简述MAC和数字签名各自的优势与适用场景。

**答案**：

| 维度 | MAC | 数字签名 |
|------|-----|---------|
| **密钥类型** | 对称密钥(共享) | 非对称密钥(公钥/私钥) |
| **速度** | 快 | 慢(非对称运算) |
| **提供不可否认性？** | 否(双方都有密钥) | 是(只有签名者有私钥) |
| **适用场景** | 银行间报文验证、IPSec ESP | 电子合同、软件分发、证书签发 |
| **第三方验证** | 不能(第三方不知道密钥) | 能(用公钥验证) |

---

**例题 7-7**：使用RSA进行数字签名时，已知n=33, 私钥d=7, 公钥e=3。对报文散列值h=4进行签名和验证。

**解析**：
```
签名: S = h^d mod n = 4^7 mod 33
     4^2 = 16, 4^4 = 256 ≡ 25 (mod 33), 4^7 = 4^4 × 4^2 × 4 = 25 × 16 × 4
     = 400 × 4 = 1600 ≡ 16 (mod 33)
     S = 16

验证: h' = S^e mod n = 16^3 mod 33
     16^2 = 256 ≡ 25 (mod 33)
     16^3 = 25 × 16 = 400 ≡ 4 (mod 33) ✓

h' = h = 4，验证通过！
```

---

**例题 7-8**：为什么数字签名通常先对报文做散列，再对散列值签名，而不是直接对报文签名？

**答案**：
1. **效率**：公钥操作(如RSA签名)对大块数据极慢。先散列(非常快)得到短摘要，再对摘要签名，大幅提升效率。
2. **长度限制**：RSA加密的明文必须小于模数n。一个大的PDF文档远超过n的长度。散列值长度固定且短(如SHA-256只256位)，可以直接签名。
3. **安全性**：如果直接对报文分块签名再拼接，攻击者可以重新排列、删除或替换某些块。对整个散列值签名相当于签名了完整报文。

---

**PDF例题7-10**(单选题)：防止数据源被假冒，最有效的加密机制是？

A. 消息认证 B. 消息摘要 C. 数字签名 D. 替换加密

**答案**：C。数字签名用发送方私钥签名，验证方用公钥验证——只有拥有私钥的发送方能产生有效签名，因此能有效防止数据源被假冒。

---

**PDF例题7-11**(单选题)：关于数字签名与手写签名，下列说法中错误的是？

A. 手写签名和数字签名都可以被模仿
B. 手写签名可以被模仿，而数字签名在不知道密钥的情况下无法被模仿
C. 手写签名对不同内容是不变的
D. 数字签名对不同的消息是不同的

**答案**：A。数字签名在不知道私钥的情况下是**无法被模仿**的(基于计算不可行性)，而手写签名可以被模仿(练字、复制等)。B/C/D均正确。

---

## 7.4 认证协议

### 7.4.1 实体认证

> **定义——实体认证(Entity Authenticatio)n**：验证通信对方身份的过程。区别于报文鉴别(验证单个报文)，实体认证验证的是"正在与你通信的这个实体是谁"——在通信**开始时**建立身份信任。

#### 认证的三类因子

| 类别 | 原理 | 示例 | 特点 |
|------|------|------|------|
| **所知(Something you know)** | 只有你知道的信息 | 口令、PIN、密钥 | 易分发、易忘记、可被窃取 |
| **所有(Something you have)** | 只有你持有的物理设备 | 智能卡、U盾、手机(SMS验证码) | 难伪造、可能丢失 |
| **所是(Something you are)** | 你固有的生物特征 | 指纹、虹膜、面部、声纹 | 不会丢失、难以共享 |

> **双因子认证(Two-Factor Authentication, 2FA)**：使用两种不同类别的因子(如口令+手机验证码)，即使口令被窃取，没有手机验证码也无法认证。

#### 认证协议面临的攻击

| 攻击类型 | 描述 | 防御手段 |
|---------|------|---------|
| **重放攻击(Replay)** | 截获之前的认证消息，稍后重放以冒充合法用户 | **现时值(Nonce)**——每次认证使用不同随机数 |
| **中间人攻击(MITM)** | 攻击者插入通信双方之间，分别冒充对方 | 公钥证书验证、带外信道确认 |
| **口令猜测** | 暴力尝试或字典攻击 | 尝试次数限制、强口令策略、账户锁定 |

> **定义——现时值(Nonce, Number used ONC)E**：在认证协议中使用的一次性随机数。每次认证使用不同的Nonce，即使攻击者截获了上次通信，重放时也会因Nonce不匹配而失败。

#### 基于Nonce的挑战-响应认证

```
挑战-响应(Challenge-Response)认证流程：

    A (声称者)                              B (验证者)
       │                                       │
       │ ① A发出认证请求                        │
       │──────────────────────────────────────→│
       │                                       │
       │ ② B发送挑战: Nonce R                  │
       │←──────────────────────────────────────│
       │                                       │
       │ ③ A用共享密钥K加密R: E_K(R)           │
       │   或者A用自己的私钥签名R               │
       │──────────────────────────────────────→│
       │                                       │
       │ ④ B解密/验证，比较R是否正确            │
       │   正确→A拥有正确的密钥→身份确认        │
       │                                       │
```

### 7.4.2 密钥分发中心(KD)C

#### 为什么需要KDC？

对称密钥密码体制要解决的核心问题：N个用户之间的密钥分发。如果每对用户面地面交换密钥，N个用户需要N(N-1)/2次安全会面。KDC(Key Distribution Cente)r将O(N²)的密钥管理问题降为O(N)——每方只需与KDC共享一个主密钥。

> **定义——密钥分发中心(Key Distribution Center, KD)C**：一个可信的第三方服务器，每个用户与KDC之间预先共享一个主密钥(长期密钥)。当两个用户需要通信时，KDC生成一个**一次性会话密钥(Session Key)**并将它安全地分发给双方。

```
KDC的基本工作流程：

           ┌───────────────┐
           │     KDC       │
           │  共享K_A, K_B  │
           └───┬───────┬───┘
       K_A↗         K_B↘
    ┌──────┐              ┌──────┐
    │  A   │              │  B   │
    └──────┘              └──────┘

① A→KDC:  请求与B通信

② KDC→A:  E_K_A[K_S, E_K_B[K_S, A的身份]]
           │      └─ A打不开，只能转给B ─┘
           └─ A解密得到K_S ─┘

③ A→B:    E_K_B[K_S, A的身份]
           (KDC发给A的"票据"，A解密不了但可以转发)

④ A→B:    E_K_S[通信数据]
           B从票据中得到K_S，两者用K_S安全通信
```

**KDC的优势**：
- 每用户只管理一个主密钥(与KDC共享的)
- 会话密钥只在本次会话有效，会话结束销毁
- 主密钥很少使用，减少了被破解的窗口

**KDC的局限**：
- **单点故障**——KDC崩溃或不可达，整个系统无法通信
- **单点安全风险**——KDC被攻破，所有通信暴露
- **需要KDC始终在线**——每次新会话都要联系KDC

### 7.4.3 Kerberos协议

> **定义——Kerberos**：MIT开发的基于KDC思想的网络认证协议，使用**票据(Ticket)**机制实现安全的实体认证和会话密钥分发。默认对称密钥体制(当前版本也可用公钥)，是Windows Active Directory和许多Unix/Linux系统的默认认证协议。

#### Kerberos的三方结构

```
┌──────────────────────────────────────────────────────────────┐
│                       Kerberos 三方结构                       │
├──────────────────────────────────────────────────────────────┤
│  ① 客户端(C) — 请求访问服务的用户/应用                        │
│  ② 服务端(S) — 提供资源的服务器(文件服务器、邮件服务器等)    │
│  ③ KDC (AS+TGS) — 认证服务器AS + 票据授予服务器TGS            │
└──────────────────────────────────────────────────────────────┘
```

```
Kerberos认证流程(简化)：

┌──────┐                        ┌──────────────┐  ┌──────┐
│ 客户C │                        │     KDC      │  │服务端S│
└──┬───┘                        │  ┌──AS─────┐ │  └──┬───┘
   │                            │  │认证服务器│ │     │
   │① C→AS: ID_C, ID_TGS, N1   │  └─────────┘ │     │
   │───────────────────────────→│              │     │
   │② AS→C: E_K_C[K_C-TGS, ...]│              │     │
   │     + TGT (含K_C-TGS)      │              │     │
   │←───────────────────────────│              │     │
   │                            │  ┌──TGS────┐ │     │
   │③ C→TGS: TGT + 请求S       │  │票据授予  │ │     │
   │───────────────────────────→│  │服务器    │ │     │
   │④ TGS→C: 含K_C-S的两个票据  │  └─────────┘ │     │
   │←───────────────────────────│              │     │
   │                            └──────────────┘     │
   │⑤ C→S: 服务票据(含K_C-S) + Authenticator       │
   │────────────────────────────────────────────────→│
   │⑥ S→C: 可选认证响应                              │
   │←────────────────────────────────────────────────│

TGT: 票据授予票据(Ticket Granting Ticket)
      ——"你有权申请任何服务的票据"
服务票据: ——"你有权访问特定服务S"
```

**Kerberos使用三类时间戳/现时值来防御重放攻击**：现时值(Nonce)、时间戳(Timestamp)和票据有效期(Lifetime)。

---

### 📝 本节例题-4

**例题 7-9**：什么是重放攻击？Nonce如何防御重放攻击？

**答案**：
**重放攻击**：攻击者截获合法用户的认证报文(如加密的密码或认证令牌)，稍后向服务器重发该报文以冒充合法用户登录。

**Nonce防御机制**：
1. 服务器B在每次认证开始时，向A发送一个新鲜的随机数(Nonce)作为挑战
2. A必须用自己持有的密钥(或私钥)对Nonce进行加密(或签名)后返回
3. B验证返回的加密值是否等于自己发出的Nonce
4. 即使攻击者截获了这次通信的全部报文，重放时B会生成新的Nonce，旧响应无法匹配新Nonce→重放失败

**重放攻击类比**：就像盗贼录下你说"开门"的语音，在你离开后播放录音试图骗开门锁。Nonce就像"动态口令"——每次开门时门锁会问你一个不同的问题(比如"今天的日期是多少的平方？")，录音里的答案就无效了。

---

**例题 7-10**：Kerberos为什么分AS和TGS两步？

**解析**：分步设计是为了**减少用户主密钥的使用频率**。用户的主密钥只在初始阶段(从AS获取TG)T时使用一次；后续申请各种服务票据都通过TGS完成，用的是短期密钥而非主密钥。

**答案**：
1. **安全性**：主密钥是长期密钥，泄露后果严重。分步后只在第一次AS阶段使用主密钥，大幅减少主密钥暴露在网上的时间窗口。
2. **单点登录(SSO)**：用户只需输入一次口令(通过AS验证)获得TGT后，持有TGT可以申请任意服务的票据，无需每次重新输入口令。
3. **服务隔离**：不同的服务使用不同的会话密钥(K_C-S)，一个服务的密钥泄露不影响其他服务。

---

## 7.5 公钥基础设施与密钥分配

### 7.5.1 公钥的分配

#### 核心问题

公钥密码解决了对称密钥分发问题，但又引入了一个新问题：**如何确保你获得的"公钥"确实属于你声称要通信的那个人？**

```
中间人攻击(MITM)利用公钥分配漏洞：

Alice ─────────────────→ 想要和Bob通信 ←───────────────── Bob
  │                                                          │
  │      Alice以为自己拿到了                          Bob以为自己拿到了
  │      "Bob的公钥"                                "Alice的公钥"
  │         ↓ 实际是 ← Trudy的公钥 →                     ↓ 实际是
  │                                                          │
  └────────────→  Trudy (中间人)  ←──────────────────────────┘
                  解密→阅读→篡改→重新加密
                  
Alice和Bob的通信对Trudy完全透明，且双方都以为自己
用的是对方的"真实"公钥！
```

### 7.5.2 公钥基础设施(PK)I

> **定义——公钥基础设施(Public Key Infrastructure, PK)I**：一种基于公钥密码的安全架构，通过**证书认证中心(CA)**颁发的**数字证书**来绑定公钥与其实体身份，使通信方能安全地验证对方公钥的真实性。

> **定义——数字证书(Digital Certificat)e**：由CA(证书认证中心)签名的电子文档，包含证书主体的身份信息、公钥、有效期以及CA的数字签名。其核心作用是将**公钥与持有者身份绑定**。标准格式为ITU-T X.509。

```
X.509数字证书结构：

┌──────────────────────────────────────┐
│              版本号 (Version)         │
├──────────────────────────────────────┤
│           序列号 (Serial Number)      │
├──────────────────────────────────────┤
│       签名算法标识 (Signature Algo)    │
├──────────────────────────────────────┤
│          签发者DN (Issuer)             │  ← CA的名称
├──────────────────────────────────────┤
│         有效期 (Validity Period)       │  ← 起止日期
│      (Not Before ~ Not After)        │
├──────────────────────────────────────┤
│          主体DN (Subject)             │  ← 证书持有者名称
├──────────────────────────────────────┤
│    主体公钥信息 (Subject Public Key)   │  ← 持有者的公钥+算法
├──────────────────────────────────────┤
│        扩展字段 (Extensions)          │  ← SAN, 密钥用途等
├──────────────────────────────────────┤
│   ───── CA的数字签名 ─────             │  ← CA用其私钥对整个证书签名
└──────────────────────────────────────┘
```

#### PKI的信任模型

```
层级信任模型(树形CA结构)：

                    ┌──────────┐
                    │  根CA    │ ← 自签名证书 (信任锚点)，离线保存
                    │ (Root CA)│
                    └────┬─────┘
              ┌──────────┼──────────┐
          ┌───┴──┐    ┌──┴───┐   ┌─┴─────┐
          │ CA-1 │    │ CA-2 │   │  CA-3  │ ← 中间CA (Intermediate CA)
          └──┬───┘    └──┬───┘   └───┬────┘
        ┌────┼────┐      │           │
     ┌──┴─┐┌──┴─┐┌──┴─┐   │           │
     │实体││实体││实体│    ...         ...        ← 终端实体证书
     │ A  ││ B  ││ C  │
     └────┘└────┘└────┘
     
信任链验证: 实体A证书 ← CA-1签名 ← 根CA签名 → 验证路径
```

> **定义——证书链(Certificate Chain / Trust Chai)n**：从终端实体证书→中间CA证书→根CA证书的验证路径。根CA自签名证书是**信任锚(Trust Anchor)**，是系统信任的起点。验证证书需要沿链向上追溯每一级的签名有效性，直到到达信任的根。

#### PKI的核心组件

| 组件 | 角色 |
|------|------|
| **CA (Certificate Authority)** | 颁发、更新和吊销证书，保证证书内容的真实性 |
| **RA (Registration Authority)** | 验证证书申请者身份的辅助机构(CA的"前台") |
| **证书库(Certificate Repository)** | 存储和发布证书的目录服务(如LDAP目录) |
| **CRL (Certificate Revocation List)** | CA维护的已被吊销证书的列表 |
| **OCSP (Online Certificate Status Protocol)** | 实时查询证书状态的在线服务(比CRL更实时) |

#### 证书吊销

```
证书的生命周期：

[颁发] → [正常使用(有效期内)] → [到期] 或 [吊销(Revocation)]

吊销原因:
- 私钥泄露(最常见)
- CA私钥泄露
- 证书信息变更
- 实体关系终止(如员工离职)

吊销机制:
① CRL (证书吊销列表)：
   CA周期性发布被吊销证书的序列号列表
   客户端下载并检查
   缺点：可能有更新延迟(吊销后到下次CRL发布之间)

② OCSP (在线证书状态协议)：
   客户端向OCSP服务器实时查询某证书状态
   返回：Good / Revoked / Unknown
   优点：实时
```

---

### 📝 本节例题

**PDF例题7-12**(单选题)：关于数字证书的描述错误的是？

A. 是公钥与实体绑定的证明 B. 证书中写有公钥及拥有者标识信息 C. 证书中有CA用自己私钥的数字签名 D. 数字证书是用户私有的，不能公开

**答案**：D。数字证书是**公开的，不需要加密**——任何人都可以从CA获取证书以验证其中的公钥。任何用户都可从可信任的地方(如CA网站)获得CA的公钥来验证证书。A/B/C均正确。

---

## 7.6 网络安全协议

### 7.6.1 网络层安全——IPSec

> **定义——IPSec (IP Security)**：在**网络层**实现安全通信的协议套件，为IP数据报提供加密和认证功能。由**AH(Authentication Header)**提供认证，**ESP(Encapsulating Security Payload)**提供加密+认证。

#### IPSec的安全模式

| 模式 | 描述 | 加密范围 | 适用场景 |
|------|------|---------|---------|
| **传输模式(Transport Mode)** | 只保护IP报文的数据部分(传输层及以上)，IP头不变 | IP头(明文) + IPSec头 + 加密的TCP/UDP负载 | 端到端通信(主机到主机) |
| **隧道模式(Tunnel Mode)** | 将整个原始IP报文封装在IPSec中进行加密和认证 | 新IP头(明文) + IPSec头 + 加密的[原始IP头+数据] | 站点到站点VPN(网关到网关) |

```
传输模式 vs 隧道模式：

传输模式：
┌─────────┬─────────────┬──────────────────────────┐
│ 原始IP头 │  IPSec头    │     加密的TCP/UDP负载      │  ← IP头是明文
└─────────┴─────────────┴──────────────────────────┘

隧道模式：
┌─────────┬─────────────┬──────────────────────────────────┐
│ 新IP头   │  IPSec头    │ 加密的[原始IP头 + TCP/UDP负载]    │  ← 整个原始包被加密
└─────────┴─────────────┴──────────────────────────────────┘
```

#### IPSec的两种协议

| 协议 | 名称 | 提供服务 | 加密 | 认证 | 防重放 |
|------|------|---------|------|------|--------|
| **AH** | Authentication Header | 认证 + 防重放 | ❌ 不加密 | ✅ | ✅ |
| **ESP** | Encapsulating Security Payload | 加密 + 认证 + 防重放 | ✅ | ✅(可选) | ✅ |

#### 安全关联(S)A

> **定义——安全关联(Security Association, S)A**：IPSec中发送方与接收方之间的单向安全"状态"(包括加密算法、密钥、SPI等参数)。SA是单向的，双向通信需要两个SA(入站SA和出站S)A。

```
SA的三元组标识：
(SPI, 目的IP地址, 安全协议)

- SPI (Security Parameter Index)：32位标识符，用于区分同一目标的不同SA
- 目的IP地址：对方的IP
- 安全协议：AH 或 ESP

SA存储在每个主机的SAD (Security Association Database) 中
```

#### IPSec的密钥管理——IKE

> **定义——IKE (Internet Key Exchange)**：IPSec的密钥管理协议，用于自动协商和建立SA(包括密钥生成和交换)。使用Diffie-Hellman密钥交换来安全地建立共享密钥。

### 7.6.2 传输层安全——SSL/TLS

> **定义——TLS (Transport Layer Security)**：在**传输层**实现安全通信的协议，前身为SSL(Secure Sockets Layer, Netscape开发)。TLS介于应用层和传输层之间，向上层应用提供端到端的保密性、完整性和认证。最常见的应用是HTTPS(HTTP over TLS，端口443)。

#### TLS在协议栈中的位置

```
┌─────────────────────────────────┐
│    应用层 (HTTP, SMTP, FTP)      │  ← 明文应用
├─────────────────────────────────┤
│    TLS (传输层安全)              │  ← 加密/认证层
├─────────────────────────────────┤
│    TCP (传输控制协议)             │
├─────────────────────────────────┤
│    IP (网络层)                   │
└─────────────────────────────────┘

应用数据 → 分段 → 压缩(可选) → 计算MAC → 加密 → TLS记录 → TCP → IP
```

#### TLS握手协议

> **定义——TLS握手(TLS Handshak)e**：TLS连接的建立阶段，协商加密套件、交换密钥和完成身份认证。完整握手需要 2-RTT(TLS 1.2)或 1-RTT(TLS 1.3)，恢复握手可以0-RTT。

```
TLS 1.3 完整握手(简化，1-RT)T：

客户端 ClientHello                   服务器 ServerHello
─────────────────────────────→       ←─────────────────────────────
支持的密码套件列表                    选定的密码套件
客户端随机数 Nonce_C                  服务器随机数 Nonce_S
密钥共享(DH公钥)                      密钥共享(DH公钥)
                                      证书链 (服务器公钥)
                                      签名 (证明拥有对应私钥)
───────────────────────────────────────────────────────────────→

双方各自计算主密钥：
Master_Secret = HKDF(DH共享密钥, Nonce_C, Nonce_S, ...)

此后 → 应用数据全部用对称密钥加密传输(如 AES-GC)M
```

#### TLS的关键安全机制

| 机制 | 说明 |
|------|------|
| **服务器认证** | 服务器发送经CA签名的证书，客户用CA公钥验证 |
| **密钥协商** | 通过Diffie-Hellman(或ECDHE)安全协商对称会话密钥 |
| **前向安全(PFS)** | 使用ECDHE，即使服务器私钥泄露，历史会话密钥仍安全 |
| **数据加密** | 协商的对称密钥(如AES-256-GC)M加密所有应用数据 |
| **数据完整性** | 每条记录带MAC(已验证的加密模式如GCM自带认证) |

> **定义——前向安全(Perfect Forward Secrecy, PF)S**：即使服务器长期私钥在将来泄露，过去已截获的加密会话内容也无法被解密的安全属性。通过使用**临时密钥交换**(如ECDH)E而非服务器RSA公钥加密来传输会话密钥。每次会话生成新的临时DH密钥对，会话结束后立即销毁。

### 7.6.3 应用层安全——PGP

> **定义——PGP (Pretty Good Privacy)**：一种**应用层**的电子邮件加密和签名软件，由Phil Zimmermann于1991年开发。使用混合密码系统：对称密钥加密邮件体，公钥加密对称密钥，并提供数字签名功能。

#### PGP的混合加密机制

```
PGP加密邮件：

① 生成随机会话密钥 K_S
② 用对称密钥 K_S 加密邮件正文 → E_K_S(M)
③ 用接收方的RSA公钥 PK_B 加密 K_S → E_PK_B(K_S)
④ 将 E_K_S(M) + E_PK_B(K_S) 一起发送
⑤ 只有持有对应私钥 SK_B 的接收方能解开 K_S，进而解密邮件

这样既有对称加密的速度，又有公钥加密的密钥分发便利。
```

```
PGP签名邮件：

① 计算邮件的散列值 H(M)
② 用发送方的RSA私钥 SK_A 对散列值签名 → S = E_SK_A(H(M))
③ 发送 M + S
④ 接收方用发送方的公钥 PK_A 解密S得到H(M)，与重新计算的H(M')比较
```

**PGP的信任模型**：PGP不使用层级CA(不像X.509 PK)I，而是使用**信任网(Web of Trust)**——用户自行决定信任谁的密钥签名("我信任Alice，所以我也信任Alice签名过的Bob的密钥")，是去中心化的信任模型。

---

### 📝 本节例题-5

**例题 7-11**：IPSec ESP的传输模式和隧道模式有什么区别？各适用于什么场景？

**答案**：

| 维度 | 传输模式 | 隧道模式 |
|------|---------|---------|
| **加密范围** | 仅IP数据部分(传输层及以上) | 整个原始IP报文 |
| **IP头** | 原始IP头明文可见 | 新IP头包裹，原始IP被加密 |
| **开销** | 小(不增加额外IP头) | 大(20字节新IP头) |
| **源/目的地址** | 原始通信双方可见 | 可以隐藏真正的源/目的(只有网关地址可见) |
| **适用场景** | **端到端**：主机间直接安全通信 | **站点到站点VPN**：网关间隧道，内部主机透明 |
| **NAT穿越** | 困难(ESP加密了传输层端口) | 容易(新IP头可以NA)T |

---

**例题 7-12**：TLS握手协议中，为什么要使用Diffie-Hellman临时密钥交换(ECDH)E而不是直接用服务器RSA公钥加密传输会话密钥？

**答案**：这是为了提供**前向安全(PFS)**。

- **RSA密钥传输模式**(TLS 1.2及之前)：客户生成会话密钥→用服务器RSA公钥加密→传给服务器。如果服务器RSA私钥将来泄露，攻击者解密所有过去截获的会话密钥→所有历史通信暴露。

- **ECDHE模式**(TLS 1.3默认)：双方各自生成临时DH密钥对，通过DH密钥交换协商会话密钥。会话结束后**双方立即销毁临时DH私钥**。即使将来服务器RSA长期私钥泄露，临时DH私钥已不存在→历史会话密钥无法恢复→历史通信安全。

**结论**：即便服务器证书私钥泄露，历史TLS会话内容仍然安全。这就是为什么TLS 1.3强制使用具有前向安全的密钥交换方式。

---

**PDF例题7-13**(多选题)：关于IPSec的描述中，正确的是？

A. 在网络层提供安全服务 B. 主要协议包括AH和ESP C. SPI使用64位连接标识符 D. AH头位于原IP数据报数据和IP头之间 E. SA定义的逻辑连接是单向的

**答案**：A, B, D, E。C错误——SPI使用**32位**标识符（非64位）。D正确——AH在传输模式中确实位于原IP头与数据之间。

---

**PDF例题7-14**(单选题)：某网站 www.ok.edu.cn 要求用户使用SSL协议，用户访问URL应为？

A. http://www.ok.edu.cn B. https://www.ok.edu.cn C. rtsp://www.ok.edu.cn D. mns://www.ok.edu.cn

**答案**：B。HTTPS(HTTP over SSL/TLS)使用 `https://` 协议前缀，默认端口443。

---

## 7.7 防火墙与入侵检测

### 7.7.1 防火墙

#### 为什么需要防火墙？

安全协议(IPSec、TLS等)保护的是通信内容，但无法阻止你的内部网络中的主机访问恶意网站，也无法阻止外部攻击者扫描和攻击你的内部主机。防火墙在**网络边界**上控制进出的流量——"谁可以访问什么"。

> **定义——防火墙(Firewal)l**：一种部署在内部网络与外部网络(通常是互联网)边界上的访问控制设备，根据预定义的安全策略检查、过滤和限制进出网络的数据包，保护内部网络免受外部攻击。

```
                   ┌──────────────┐
   外网(互联网) ←──│   防火墙    │──→ 内网(局域网)
  (不可信)        │ (Trusted Zone)│    (受保护区)
                   └──────────────┘
                         │
          允许/拒绝 基于策略的决策
```

#### 防火墙的四种类型

| 类型 | 工作层次 | 原理 | 优点 | 缺点 |
|------|---------|------|------|------|
| **包过滤防火墙 (Packet Filtering)** | 网络层+传输层 | 检查每个包的IP头(源/目的地址)和TCP/UDP头(端口)，按规则放行或丢弃 | 简单、快速、透明 | 无状态记忆、难配置复杂策略、不检查应用内容 |
| **状态检测防火墙 (Stateful Inspection)** | 网络层+传输层 | 维护连接状态表，跟踪TCP连接状态，只允许符合正常连接生命周期的包 | 比包过滤更安全、动态管理规则 | 仍不检查应用内容、状态表可能被DDos耗尽 |
| **应用网关 (Application Gateway / 代理防火墙)** | 应用层 | 充当内外网之间的代理，完全隔离内外网的直接连接，对每类应用有专门的代理程序 | 安全级别最高、深度内容检查、细粒度控制 | 性能开销大、每种应用需要专门代理、可扩展性差 |
| **NAT网关** | 网络层 | 将内部私有IP映射为少数公网IP，隐藏内网拓扑 | 节省公网IP、隐藏内网结构 | 破坏端到端通信原则 |

```
状态检测防火墙的连接状态表：

┌────────────────────────────────────────────────────┐
│ 内网地址:端口     外网地址:端口      状态            │
├────────────────────────────────────────────────────┤
│ 192.168.1.5:54321 → 93.184.216.34:80  ESTABLISHED  │
│ 192.168.1.8:54322 → 142.250.80.46:443 ESTABLISHED  │
│ 192.168.1.5:54323 → 23.42.17.52:80    SYN_SENT     │
└────────────────────────────────────────────────────┘

规则示例：
- 内网→外网：允许 (默认允许出站)
- 外网→内网：仅允许已建立连接的回程包
- 丢弃所有从外网发起的TCP SYN连接请求
```

> **辨析——包过滤 vs 状态检测 vs 应用网关**：包过滤像"门禁只查工牌"(只看包头)，状态检测像"门禁记住你刚才出去过所以回来放行"(记住连接状态)，应用网关像"专人开车帮你办事不让你直接接触对方"(完全代理，内外隔离)。安全级别依次递增，性能开销也依次递增。

#### 防火墙的访问控制策略

| 策略类型 | 原则 | 示例 |
|---------|------|------|
| **默认拒绝 (Whitelist)** | 默认禁止所有，仅明确允许的通过 | 最安全，管理负担大 |
| **默认允许 (Blacklist)** | 默认允许所有，仅明确禁止的阻止 | 最宽松，安全风险大 |
| **默认拒绝(出站也控制)** | 进出双向默认禁止，仅必要服务开放 | 企业环境标准实践 |

### 7.7.2 入侵检测系统

#### 为什么需要入侵检测？

防火墙控制"谁能进"，但无法检测通过合法通道的恶意行为——例如一个合法用户通过正常HTTP连接下载了含病毒的邮件附件。入侵检测系统在防火墙的基础上进行**深度行为检测**——发现那些混在正常流量中的攻击。

> **定义——入侵检测系统(Intrusion Detection System, ID)S**：一种监控网络或系统活动的安全设备，能够在入侵已经开始但还没有造成危害或在造成更大危害前，**及时检测到入侵**，把危害降低到最小。IDS对进入网络的分组执行**深度分组检查(Deep Packet Inspection)**。当观察到可疑分组时，向网络管理员发出告警或执行阻断操作(由于IDS的"误报"率通常较高，多数情况下不执行自动阻断)。

#### IDS的三种分类(按数据来源)

教材明确将IDS按数据来源分为**三类**：

| 类型 | 监控对象 | 方法 | 可检测的攻击 | 不可检测的攻击 |
|------|---------|------|------------|--------------|
| **基于主机的IDS (HIDS)** | 单台主机的系统日志、文件变更、进程行为 | 文件完整性检查、日志分析、系统调用监控 | Rootkit安装、可疑文件修改、异常进程 | 网络层攻击(端口扫描等) |
| **基于网络的IDS (NIDS)** | 流经特定网段的所有网络流量 | 抓包分析、特征匹配、异常检测 | 端口扫描、DoS攻击、蠕虫和病毒、系统漏洞攻击 | 加密流量、主机内部行为 |
| **分布式IDS (DIDS)** | 结合HIDS和NIDS，全网范围协调 | 中央管理站+多个检测节点，关联分析全网事件 | 分布式攻击(如DDo)S、跨主机入侵模式 | 单点部署复杂度高 |

#### 入侵检测方法

| 方法 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **特征检测(Signature-based)** | 维护攻击模式库(规则/签名)，匹配已知攻击 | 准确率高、误报少 | 无法检测零日(0-day)攻击、需持续更新规则库 |
| **异常检测(Anomaly-based)** | 建立正常行为基线，偏离基线的行为视为异常 | 可检测未知攻击 | 误报率较高、基线本身可能包含攻击行为 |

**基于网络的IDS三种基本识别技术**(教材原文)：

| 识别技术 | 说明 |
|---------|------|
| **模式匹配 (Pattern Matching)** | 将收集到的信息与已知的网络入侵特征数据库进行比较，发现违背安全策略的行为 |
| **频率或阈值 (Frequency/Threshold)** | 当特定类型事件的发生频率超过预设阈值时触发告警 |
| **事件的相关性 (Event Correlation)** | 关联多个看似无害的事件，综合判断是否存在攻击模式 |

> **辨析——IDS vs 防火墙**：防火墙基于**规则**(IP/端口/协议)控制访问——"谁可以进来"；IDS基于**行为模式**检测威胁——"进出的人在做什么可疑的事"。两者互补：防火墙挡已知的禁止通道，IDS检测通过合法通道的恶意行为。

#### 补充：NMAP端口扫描

Nmap (Network Mapper) 是一种开源的网络探测和安全审计工具，常用于：
- **主机发现**：确定网络上哪些主机正在运行
- **端口扫描**：探测目标主机开放了哪些TCP/UDP端口
- **服务识别与版本检测**：确定开放端口上运行的服务及其版本
- 既是网络管理员的重要工具，也可能被攻击者用于信息收集

#### 入侵检测的关键指标

| 指标 | 含义 |
|------|------|
| **真阳性(TP, True Positive)** | 正确识别的攻击 |
| **假阳性(FP, False Positive)** | 正常行为被误报为攻击(误报) |
| **真阴性(TN, True Negative)** | 正常行为被正确识别为正常 |
| **假阴性(FN, False Negative)** | 攻击未被检测出来(漏报) |
| **检测率 = TP/(TP+FN)** | 检测到的攻击占所有攻击的比例(越高越好) |
| **误报率 = FP/(FP+TN)** | 误报占所有正常行为的比例(越低越好) |

---

### 📝 本节例题-6

**例题 7-13**：某公司内网使用状态检测防火墙。允许内网主机访问外网Web服务(80端口)，禁止外网主动访问内网。某内网员工访问了一个恶意网站，该网站的恶意JavaScript脚本试图从外网反向连接到该员工电脑的TCP 5555端口。防火墙能否阻止这个反向连接？为什么？

**解析**：状态检测防火墙维护连接状态表。

**答案**：
**可以阻止。**
- 状态检测防火墙为每个内网出去的连接在状态表中创建条目(如`192.168.1.5:xxxxx → 恶意网站IP:80`)。
- 当恶意网站试图从外网发起连接`恶意网站IP:yyyy → 192.168.1.5:5555`时，防火墙检查状态表中没有此连接的对应该条目。
- 防火墙默认策略：**所有从外网主动发起的TCP连接请求(SYN包)一律丢弃**。
- 只有已存在连接的回程流量(目的端口80，源端口已记录在状态表中)才会被放行。外网对5555端口的连接是全新请求，不符合任何已记录连接→被丢弃。

**但是**：如果浏览器/proxy配置了到外网的反向隧道，或员工电脑本身已有后门程序从内网主动向外网建立了连接，防火墙对这类"内网主动出去"的连接可能无能为力——这就是为什么防火墙不是万能的，需要IDS/端点防护等深度防御。

---

**例题 7-14**：对比包过滤防火墙、状态检测防火墙和应用网关防火墙的安全级别和应用场景。

**解析**：三种防火墙的主要区别在于工作层次和是否维护状态信息。包过滤防火墙只看IP头和TCP/UDP头，无状态记忆；状态检测防火墙在此基础上增加了连接状态表，能跟踪TCP连接生命周期；应用网关（代理防火墙）工作在应用层，完全隔离内外网的直接连接，对每类应用有专门代理程序进行深度内容检查。安全性和性能开销成正比——越安全的方案性能开销越大。

**答案**：

| 维度 | 包过滤 | 状态检测 | 应用网关 |
|------|--------|---------|---------|
| **安全级别** | ★★☆☆☆ | ★★★☆☆ | ★★★★★ |
| **工作层次** | 网络层+传输层 | 网络层+传输层 | 应用层 |
| **速度** | 快 | 快 | 慢 |
| **状态跟踪** | 无 | 有 | 有 |
| **内容检查** | 无 | 无 | 深度检查 |
| **部署场景** | 边界路由器ACL | 企业网络边界 | DMZ边界/高安全需求 |
| **典型产品** | iptables, ACL | Cisco ASA, pfSense | Blue Coat, Squid Proxy |

---

**PDF例题7-15**(单选题)：下面对于个人防火墙的描述，不正确的是？

A. 为防护接入互联网的单机操作系统而出现 B. 功能与企业级防火墙类似而配置管理相对简单 C. 所有的单机杀病毒软件都具有个人防火墙的功能 D. 配置方法相对简单

**答案**：C。个人防火墙和杀病毒软件是两种不同的安全产品——并非所有杀病毒软件都内置个人防火墙功能。

---

**PDF例题7-16**(填空题)：在网络中，(\_\_\_\_\_\_)是一个或一组在两个网络之间执行访问控制策略的系统，包括硬件和软件，目的是保护网络不被可疑入侵扰。(入侵检测 / RSA / 防火墙 / DN)S

**答案**：防火墙。

---

**PDF例题7-17**(单选题)：以下关于防火墙的说法正确的是？

A. 防火墙能防范新的网络安全问题 B. 防火墙不能防范不通过防火墙的连接攻击 C. 防火墙能完全阻止病毒的传播 D. 防火墙能防止来自内部网的攻击

**答案**：B。防火墙只能控制通过它的流量，无法防范绕过的攻击(如通过U盘、拨号连接等)。防火墙也"不能防范新的网络安全问题"(无法检测零日攻击)，"不能完全阻止病毒传播"(病毒可通过邮件等合法通道进入)，"不能防止来自内部网的攻击"(防火墙部署在内外网边界)。

---

**PDF例题7-18**(多选题)：包过滤防火墙工作在OSI模型的哪些层次？

A. 网络层 B. 运输层 C. 会话层 D. 应用层

**答案**：A, B。包过滤防火墙检查IP头和TCP/UDP头信息，工作在**网络层和运输层**。

---

**PDF例题7-19**(多选题)：基于网络的入侵检测系统(NIDS)采用的基本识别技术包括？

A. 模式匹配 B. 混杂模式 C. 频率或阈值 D. 事件的相关性

**答案**：A, C, D。NIDS的三种基本识别技术是模式匹配、频率/阈值和事件相关性。"混杂模式(Promiscuous Mode)"是网卡的一种工作模式(用于抓包)，不是入侵检测技术。

---

## 附录：第7章完整定义与公式汇总

### 一、完整定义汇总

#### 🔒 网络安全概述

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.1 | **网络安全 (Network Security)** | 保护网络系统及其数据免受非授权访问、使用、泄露、修改、破坏的综合性技术和管理体系 |
| 7.2 | **被动攻击 (Passive Attack)** | 攻击者只观测和监听通信而不干扰通信过程(窃听、流量分析)，难以检测，以防为主 |
| 7.3 | **主动攻击 (Active Attack)** | 攻击者主动修改、伪造或中断通信(三类：篡改、恶意程序、拒绝服务Do)S，难以预防，以检测和恢复为主 |
| 7.4 | **恶意程序 (Rogue Program/Malware)** | 侵入系统、破坏数据或窃取信息的恶意软件，包括计算机病毒、蠕虫、特洛伊木马、逻辑炸弹、后门入侵、流氓软件六种类型 |
| 7.5 | **DDoS (Distributed DoS)** | 分布式拒绝服务攻击：从成百上千的网站集中攻击一个目标，又称网络带宽攻击或连通性攻击 |
| 7.6 | **安全的计算机网络的四个目标** | 保密性(Confidentiality)、端点鉴别(End-point Authentication)、信息的完整性(Message Integrity)、运行的安全性(Operational Security/Access Control) |
| 7.7 | **网络安全的不可判定性** | 网络安全没有绝对标准，不存在绝对安全的系统，只能在安全性和可用性之间寻求平衡 |
| 7.8 | **数据加密模型 (Data Encryption Model)** | 形式化加密框架：Y=E_K(X)，D_K(Y)=X。密钥K由密钥中心通过安全信道提供 |
| 7.9 | **无条件安全 (Unconditionally Secure)** | 无论截获多少密文、拥有多少计算资源，都无法从密文中获得任何明文信息；一次一密是唯一实例 |
| 7.10 | **计算上安全 (Computationally Secure)** | 破解代价远超信息价值或破解时间远超信息有效期，现代实用密码体制均属此类 |
| 7.11 | **代换 (Substitution)** | 将明文中的元素按规则映射为另一个元素(如凯撒密码：a→D, b→E...) |
| 7.12 | **置换 (Permutation)** | 保持明文元素不变，重新排列顺序(如 attack→KACAT)T |
| 7.13 | **Vigenère密码 (Vigenère Cipher)** | 多表代换密码，使用关键词作为密钥，同一字母在不同位置被加密为不同字母，抵抗统计分析 |

#### 🔑 密码体制

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.14 | **密码学(Cryptography)** | 研究如何在敌手存在的情况下实现安全通信的学科 |
| 7.15 | **Kerckhoffs原则** | 密码系统的安全性应完全依赖密钥保密，而非算法保密——算法可以也应该公开 |
| 7.16 | **对称密钥密码体制** | 加解密使用相同密钥(或可互推)的密码体制，速度快但密钥分发困难 |
| 7.17 | **分组密码(Block Cipher)** | 将明文分成固定长度块再逐块加密的对称密码(如DES, AE)S |
| 7.18 | **流密码(Stream Cipher)** | 逐位(bit)加密明文流的对称密码(如RC4) |
| 7.19 | **DES** | 数据加密标准：64位分组，56位有效密钥，16轮Feistel网络，已有穷举破解能力 |
| 7.20 | **3DES** | 三重DES，执行三次DES操作(EDE模式)，密钥总长128位(K1=K3时)或192位(K1≠K2≠K3) |
| 7.21 | **AES** | 高级加密标准：128位分组，128/192/256位密钥，SPN结构，10/12/14轮，全球广泛使用 |
| 7.22 | **ECB模式** | 电子密码本：每块独立加密，同明文→同密文，不建议使用 |
| 7.23 | **CBC模式** | 密码分组链接：每块先与前一块密文异或再加密，需初始向量IV |
| 7.24 | **CFB模式 (Cipher Feedback)** | 密码反馈模式：将分组密码转为流密码，有误差传播，加密不可并行 |
| 7.25 | **OFB模式 (Output Feedback)** | 输出反馈模式：密钥流独立生成与明文无关，无误差传播，不可并行 |
| 7.26 | **CTR模式 (Counter Mode)** | 计数器模式：用递增计数器值加密产生密钥流与明文异或，无误差传播，可并行加解密 |
| 7.27 | **公钥密码体制** | 使用一对数学相关的密钥：公钥加密(可公开)，私钥解密(严格保密)，基于数学难题 |
| 7.28 | **RSA** | 第一个实用的公钥密码算法，基于大整数分解难题，可用于加密和签名 |
| 7.29 | **欧拉函数 φ(n)** | 小于n且与n互质的正整数个数；若n=pq(p,q素数)，则φ(n)=(p-1)(q-1) |

#### ✍️ 数字签名与报文鉴别

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.30 | **报文鉴别(Message Authentication)** | 验证报文确实来自声称的发送方(真实性)且在传输中未被修改(完整性) |
| 7.31 | **报文鉴别码(MAC)** | 发送方用共享密钥K和报文M生成固定长度鉴别码，接收方用同一K验证 |
| 7.32 | **密码散列函数** | 将任意长输入→固定长输出的单向函数：单向性+抗碰撞性+雪崩效应 |
| 7.33 | **MD5** | 128位输出散列函数，已被破解找到碰撞，不推荐使用 |
| 7.34 | **SHA-256** | SHA-2家族成员，256位输出，当前广泛使用的安全散列函数 |
| 7.35 | **数字签名(Digital Signature)** | 签名者用私钥对报文(或其散列值)签名，验证者用公钥验证。提供完整性+认证+不可否认性 |
| 7.36 | **HMAC** | 带密钥的散列函数：`H((K⊕opad) ∥ H((K⊕ipad) ∥ M))`，比MAC更安全高效 |

#### 🎫 认证协议

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.37 | **实体认证(Entity Authentication)** | 验证通信对方身份的过程，在通信开始时建立身份信任 |
| 7.38 | **现时值(Nonce)** | 一次性使用的随机数，用于防止认证协议中的重放攻击 |
| 7.39 | **重放攻击(Replay Attack)** | 攻击者截获合法的认证报文后重新发送以冒充合法用户 |
| 7.40 | **中间人攻击(MITM)** | 攻击者插入通信双方之间，分别对双方冒充对方 |
| 7.41 | **双因子认证(2FA)** | 使用两种不同类别的认证因子(知识+持有物，或知识+生物特征) |
| 7.42 | **KDC(密钥分发中心)** | 可信第三方，每方与之共享主密钥，KDC生成会话密钥分发双方 |
| 7.43 | **Kerberos** | 基于KDC+票据的网络认证协议，使用TGT和会话票据实现安全的单点登录 |

#### 🏢 公钥基础设施

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.44 | **PKI(公钥基础设施)** | 通过CA颁发的数字证书绑定公钥与身份，使通信方能安全验证对方公钥真实性的安全架构 |
| 7.45 | **数字证书(X.509)** | 经CA签名的电子文档，包含主体身份信息+公钥+有效期+CA签名，绑定公钥与身份 |
| 7.46 | **CA(证书认证中心)** | PKI核心：颁发、管理、吊销数字证书的权威机构 |
| 7.47 | **证书链(Trust Chain)** | 从终端证书→中间CA→根CA的签名验证路径 |
| 7.48 | **根CA(Root CA)** | 证书链的最终信任锚，自签名证书，通常离线安全保存 |
| 7.49 | **CRL(证书吊销列表)** | CA周期性发布的已吊销证书列表 |
| 7.50 | **OCSP** | 在线证书状态协议，实时查询证书吊销状态 |

#### 🌐 网络安全协议

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.51 | **IPSec** | 网络层安全协议套件：AH(认证)、ESP(加密+认证)；支持传输模式和隧道模式 |
| 7.52 | **SA(安全关联)** | IPSec通信双方的单向安全参数约定，由(SPI, 目的IP, 安全协议)三元组标识 |
| 7.53 | **IKE** | 互联网密钥交换协议，自动协商建立IPSec SA，使用DH交换密钥 |
| 7.54 | **TLS/SSL** | 传输层安全协议，在TCP之上为应用提供加密+认证+完整性，HTTPS的基础 |
| 7.55 | **TLS握手** | TLS连接的建立阶段，协商加密套件、交换密钥、完成身份认证 |
| 7.56 | **前向安全(PFS)** | 长期私钥泄露后，过去已截获的加密会话仍然不可被解密的安全属性 |
| 7.57 | **PGP** | 应用层安全软件，使用混合密码系统实现电子邮件的加密和签名 |
| 7.58 | **信任网(Web of Trust)** | PGP的去中心化信任模型：用户自行决定信任谁签名过的密钥 |

#### 🔥 防火墙与入侵检测

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 7.59 | **防火墙(Firewall)** | 部署在网络边界上的访问控制设备，按预设策略检查、过滤进出网络的数据包 |
| 7.60 | **包过滤防火墙** | 检查IP头和TCP/UDP头信息，按静态规则放行或丢弃数据包 |
| 7.61 | **状态检测防火墙** | 维护连接状态表，只允许符合正常TCP状态机的包通过 |
| 7.62 | **应用网关/代理防火墙** | 充当代理完全隔离内外网连接，在应用层深度检查内容 |
| 7.63 | **HIDS(基于主机的ID)S** | 监控单台主机的系统日志、文件变更、进程行为，检测Rootkit和可疑文件修改 |
| 7.64 | **NIDS(基于网络的ID)S** | 监控流经网段的所有流量，通过抓包分析、特征匹配和异常检测发现攻击 |
| 7.65 | **DIDS(分布式ID)S** | 结合HIDS和NIDS，中央管理站+多个检测节点协调，可检测分布式攻击 |
| 7.66 | **IDS(入侵检测系统)** | 监控网络/系统活动，在入侵造成危害前及时检测，可告警或执行阻断(因误报率高通常不自动阻断) |
| 7.67 | **NIDS三种识别技术** | 模式匹配(Pattern Matchin)g、频率或阈值(Frequency/Threshol)d、事件的相关性(Event Correlatio)n |
| 7.68 | **零日攻击(Zero-Day Attack)** | 利用尚未公开的软件漏洞进行的攻击，特征检测无法防御 |

---

### 二、核心公式汇总

| 编号 | 公式 | 说明 |
|------|------|------|
| F-1 | `C = E_K(M)`, `M = D_K(C)` | 对称加解密通用公式 |
| F-2 | `C = M^e mod n` (RSA加密) | RSA: 用公钥{PK=(e,n)}加密 |
| F-3 | `M = C^d mod n` (RSA解密) | RSA: 用私钥{SK=(d,n)}解密 |
| F-4 | `ed ≡ 1 (mod φ(n))` | RSA密钥生成：公钥e和私钥d互为模逆 |
| F-5 | `φ(n) = (p-1)(q-1)` | 欧拉函数：n为两素数乘积时 |
| F-6 | `S = M^d mod n` (RSA签名) | RSA签名：用发送方私钥签名(实际中签名H(M)) |
| F-7 | `h = H(M)` | 散列函数：任意长M→固定长h |
| F-8 | `MAC = C(K, M)` | 报文鉴别码：密钥K+报文M→固定长度MAC |
| F-9 | `HMAC(K, M) = H((K⊕opad) ∥ H((K⊕ipad) ∥ M))` | HMAC标准定义 |
| F-10 | 对称密钥数量: `N(N-1)/2` | N用户所需的对称密钥对数 |
| F-11 | AES轮数: 10(128), 12(192), 14(256) | AES不同密钥长度的轮数 |
| F-12 | `检测率 = TP/(TP+FN)` | IDS: 检测到的攻击占全部攻击比例 |
| F-13 | `误报率 = FP/(FP+TN)` | IDS: 正常行为被误报为攻击的比例 |

---

### 三、重要辨析汇总

| 编号 | 辨析对象 | 核心区别 |
|------|---------|---------|
| D-1 | **被动攻击 vs 主动攻击** | 被动只监听(预防)，主动修改/阻断(检测恢复) |
| D-2 | **对称密码 vs 公钥密码** | 对称：速度快但密钥分发难；公钥：速度慢但密钥分发方便，两者混合使用 |
| D-3 | **分组密码 vs 流密码** | 分组：逐块加密(AES/DE)S；流：逐位加密(RC4)，块模式CFB/OFB/CTR可将分组转为流 |
| D-4 | **ECB vs CBC** | ECB每块独立→同明文=同密文(不安全)；CBC链接前块→同明文≠同密文(安全) |
| D-5 | **DES vs AES** | DES: 64位块/56位密钥(已破)；AES: 128位块/128-256位密钥(安全) |
| D-6 | **RSA加密 vs RSA签名** | 加密：接收方公钥加密→接收方私钥解密；签名：发送方私钥签名→发送方公钥验证 |
| D-7 | **散列函数 vs MAC** | 散列无需密钥(只能验完整性)；MAC需要密钥(验完整性+来源认证) |
| D-8 | **数字签名 vs MAC** | 数字签名：非对称，可提供不可否认性；MAC：对称，速度更快但无法用于第三方验证 |
| D-9 | **报文鉴别 vs 实体认证** | 报文鉴别验证单个报文；实体认证验证通信对方的身份 |
| D-10 | **KDC vs PKI** | KDC：对称体制，第三方生成分发密钥；PKI：非对称体制，CA只绑定身份和公钥 |
| D-11 | **IPSec vs TLS** | IPSec：网络层安全(保护所有上层流量，对应用透明)；TLS：传输层安全(应用需集成，更细粒度) |
| D-12 | **传输模式 vs 隧道模式** | 传输：只加密数据部分(端到端)；隧道：整个IP包加密(站点到站点VP)N |
| D-13 | **防火墙 vs IDS** | 防火墙：基于规则控制访问("放谁进来")；IDS：基于行为检测攻击("进来的人在干什么") |
| D-14 | **包过滤 vs 状态检测 vs 应用网关** | 安全级别依次递增，性能开销也依次递增。企业边界标准=状态检测防火墙 |
| D-15 | **X.509 PKI vs PGP信任网** | PKI：层级CA信任；PGP：去中心化"信任网"，用户自定信任谁 |

---

### 四、常见端口速查

| 协议/服务 | 端口 | 传输层 | 说明 |
|----------|------|--------|------|
| IPSec (ESP) | IP协议50 | — | 不使用端口，直接封装在IP中 |
| IPSec (AH) | IP协议51 | — | 同上 |
| IKE | UDP 500 | UDP | IPSec密钥交换 |
| HTTPS (TLS) | TCP 443 | TCP | HTTP over TLS |
| SMTPS | TCP 465 | TCP | SMTP over TLS |
| IMAPS | TCP 993 | TCP | IMAP over TLS |
| POP3S | TCP 995 | TCP | POP3 over TLS |
| OpenVPN | UDP 1194 | UDP | VPN |
| Kerberos | TCP/UDP 88 | — | 认证协议 |
| SSH | TCP 22 | TCP | 安全远程登录 |
| RADIUS | UDP 1812/1813 | UDP | 远程认证拨号 |
| LDAPS | TCP 636 | TCP | LDAP over TLS |

---

> 📚 **文档生成信息**：本文档基于《计算机网络》第7章网络安全教材PDF，按"第一性原理→严格定义→体系化讲解→例题解析→逐页核验"流程生成。核心定义以引用块`>`标注，辅助术语以表格呈现，公式以代码块呈现。定义了68个核心术语、13条核心公式、15项重要辨析，收录12道讲解例题 + 22道PDF原题(含答案和解析)。
