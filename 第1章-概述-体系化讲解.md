# 计算机网络 第1章 概述 — 体系化讲解

> 从第一性原理出发，逐层构建知识体系，涵盖全部7个小节及所有例题。

---

## 目录

- [1.1 计算机网络在信息时代中的作用](#11-计算机网络在信息时代中的作用)
- [1.2 互联网概述](#12-互联网概述)
- [1.3 互联网的组成](#13-互联网的组成)
- [1.4 计算机网络在我国的发展](#14-计算机网络在我国的发展)
- [1.5 计算机网络的类别](#15-计算机网络的类别)
- [1.6 计算机网络的性能](#16-计算机网络的性能)
- [1.7 计算机网络的体系结构](#17-计算机网络的体系结构)

---

## 1.1 计算机网络在信息时代中的作用

### 一、第一性原理：为什么需要计算机网络？

人类社会的核心活动之一就是**信息交换**。从烽火台到电报电话，再到互联网，本质上都是解决同一个问题：**让处于不同地理位置的实体能够互相传递信息**。

计算机网络是信息时代的**核心基础设施**，如同工业时代的电力网络。它的地位源于一个根本事实：**单个计算机的计算和存储能力有限，但通过网络互连后，能力可无限扩展**。

### 二、三大传统网络

在互联网普及之前，社会上有三大类通信网络：

| 网络类型 | 提供的主要服务 |
|----------|---------------|
| **电信网络** | 电话、电报、传真等服务 |
| **有线电视网络** | 向用户传送各种电视节目 |
| **计算机网络** | 计算机之间传送数据文件 |

**核心变化**：计算机网络是三者中发展最快、并起到核心作用的网络。这是因为计算机网络的**数字化、分组化**技术优势，使其能够承载语音、视频、数据等所有类型的业务。

### 三、"三网融合"趋势

随着电信和信息技术的发展，国际上出现了 **"三网融合"** 的趋势——将传统电信网、计算机网（主要指互联网）、有线电视网融合到现代计算机网络技术中。

- **本质**：用统一的 IP 技术承载所有类型的业务（语音、视频、数据）
- **为什么可能**：所有信息都可以数字化为比特流，而计算机网络天然擅长传输比特流

### 四、互联网的关键概念辨析

#### Internet（互联网/因特网）

- **专有名词**，首字母大写
- 指当前全球最大的、开放的、由众多网络相互连接而成的**特定互连网**
- 采用 **TCP/IP 协议族**作为通信规则
- 前身是美国的 **ARPANET**
- 推荐译名"因特网"长期未得到推广，**"互联网"** 成为事实标准

#### internet（互连网）

- **通用名词**，泛指由多个计算机网络互连而成的计算机网络
- 网络之间使用的协议**可以任意选择**（不一定是 TCP/IP）
- 即：互联网 ⊂ 互连网（互联网是互连网的一种特例）

### 五、互联网的两个基本特点

这两个特点是 Internet 提供所有服务的基础：

| 特点 | 含义 |
|------|------|
| **连通性 (Connectivity)** | 上网用户之间可以非常便捷、经济地交换信息，仿佛彼此直接连通 |
| **资源共享 (Sharing)** | 信息共享、软件共享、硬件共享，资源仿佛就在用户身边 |

### 六、互联网的地位与影响

**正面**：
- 融入生活、工作、学习和交往的方方面面
- 成为社会最重要的基础设施之一
- **"互联网+"**：将互联网创新成果深度融合于各领域（工业、农业、教育、医疗、商业、交通、金融、政务等），形成新的经济形态

**负面**：
- 传播病毒、窃取数据与钱财
- 散布谣言、不良信息
- 欺诈、网瘾等问题

> 结论：需要加强对互联网的管理。

### 七、互联网的多维理解

互联网可以从不同维度理解：
- **应用和服务层面**：游戏、视频、社交、电子邮件、购物、网银、数字钱包、数字货币……
- **工作原理层面**：互连结构、交换技术、TCP/IP 体系结构与协议……

---

### 📝 本节例题

#### 例题1：三网融合
> 随着电信和信息技术的发展，国际上出现了所谓"三网融合"的趋势，下列不属于三网之一的是（　　）。
> A.传统电信网 B.计算机网(主要指互联网) C.有线电视网 D.卫星通信网

**答案：D**。三网指传统电信网、计算机网（互联网）、有线电视网。

#### 例题2：计算机网络的本质
> 计算机网络是能够相互（　　）的互连起来的自治计算机系统的集合。

**答案：资源共享**。连通性和资源共享是计算机网络两个最基本的特点，但填空题常考"资源共享"——这是计算机网络的最根本目的。

#### 例题3：共享资源
> 计算机网络中可以共享的资源包括（　　）。
> A.硬件、软件、数据 B.主机、外设、软件 C.硬件、程序、数据 D.主机、程序、数据

**答案：A**。计算机网络可共享的资源包括**硬件、软件、数据**。

---

## 1.2 互联网概述

### 1.2.1 网络的网络

#### 第一性原理：从简单到复杂

**网络 (Network)**：
- 由若干**节点 (Node)** 和连接这些节点的**链路 (Link)** 组成
- 节点可以是计算机、集线器、交换机或路由器等
- 与网络相连的计算机常称为**主机 (Host)**

**互连网 (internetwork / internet)**：
- 多个网络通过**路由器**相互连接起来，构成覆盖范围更大的计算机网络
- 本质是 **"网络的网络" (network of networks)**

**云表示法**：用"云"表示网络——主机在"云"外，云内部是复杂的互连结构。

### 1.2.2 互联网基础结构发展的三个阶段

| 阶段 | 时间 | 主要特征 |
|------|------|---------|
| **第一阶段** | 1969–1990 | 从单个网络 ARPANET 向互联网发展 |
| **第二阶段** | 1985–1993 | 建成三级结构的互联网（主干网、地区网、校园网） |
| **第三阶段** | 1993–现在 | 全球范围的多层次 ISP 结构的互联网 |

#### 第一阶段详解（1969–1990）

- **1969年**：ARPANET 诞生，最初只是单个分组交换网（不是互连网），仅有4个节点（UCLA, UCSB, SRI, UTAH）
- **1970年代**：ARPA 开始研究多种网络互连技术，互连网络出现
- **1983年**：TCP/IP 协议成为 ARPANET 标准协议——**1983年被公认为互联网的诞生时间**
- **1990年**：ARPANET 正式关闭，完成历史使命

#### 第二阶段详解（1985–1993）

- **NSFNET**（美国国家科学基金网）成为主干
- 采用**三级结构**：主干网 → 地区网 → 校园网（或企业网）
- 覆盖全美主要大学和研究所
- **1991年**开始将主干网络交给私人公司运营，接入收费

#### 第三阶段详解（1993–现在）

核心特征：**多层次 ISP 结构**

**ISP (Internet Service Provider，互联网服务提供者)**：
- 提供接入互联网的服务，收取费用
- 分为：**主干 ISP**、**地区 ISP**、**本地 ISP**（依据覆盖面积和 IP 地址数）

**通信路径示例**：
```
主机A → 本地ISP → 地区ISP → 主干ISP → 地区ISP → 本地ISP → 主机B
```

**IXP (Internet eXchange Point，互联网交换点)**：
- 允许两个网络**直接相连**并快速交换分组
- 作用：优化数据流量分布、减少转发延时、降低费用
- 常使用工作在数据链路层的网络交换机
- 世界上较大的 IXP 峰值吞吐量在 **Tbit/s** 量级

**内容提供者 (Content Provider)**：
- 在互联网上向用户提供视频等内容的公司（如 Google、Netflix）
- 不提供互联网的转接服务
- 拥有独立于互联网的专门网络，能与各级 ISP 和 IXP 相连

#### 万维网 WWW 的推动

- **20世纪90年代**：由欧洲原子核研究组织 CERN 开发
- 成为互联网**指数级增长**的主要驱动力
- 2019年3月底，全球互联网用户数超过 **43.8亿**

### 1.2.3 互联网的标准化工作

#### 为什么标准化重要？

互联网是全球性的，不同厂商的设备必须能够互通。标准化使这一切成为可能。

#### 组织架构

```
            ISOC (互联网协会)
              │
        IAB (互联网体系结构研究委员会)
         │            │
    IRTF (研究部)   IETF (工程部)
         │            │
      IRSG (研究指导) IESG (工程指导)
         │            │
       RG (研究组)   WG (工作组)
```

- **ISOC**：国际性组织，负责全面管理
- **IAB**：架构委员会
- **IRTF**：研究长期问题（协议、应用、体系结构）
- **IETF**：研究短期/中期工程问题（协议开发和标准化）
- **IAB** 下面有两个工程部：**IRTF**（互联网研究部）和 **IETF**（互联网工程部）

#### RFC 文档

所有互联网标准都以 **RFC (Request For Comments，请求评论)** 形式发表。

关键特征：
- 面向公众，**免费下载**
- 任何人都可以电子邮件发表意见
- **并非所有 RFC 都是互联网标准**，只有很少部分最终成为标准
- 按时间顺序编号，如 RFC9293 是 TCP 的标准

#### 标准化过程

```
RFCxxxx
  │
互联网草案 (Internet Draft)
  │
建议标准 (Proposed Standard)
  │
草案标准 (Draft Standard)   ← 标准轨道
  │
互联网标准 (Internet Standard，记为 STDxx)

非标准轨道：历史(Historic)、信息(Informational)、实验(Experimental)
```

---

### 📝 本节例题

#### 例题1：Internet 的协议基础
> Internet是建立在（　　）协议集上的国际互联网络。
> A.IPX B.NetBEUI C.TCP/IP D.AppleTalk

**答案：C**。Internet 采用 TCP/IP 协议族。

#### 例题2：因特网前身
> 因特网的前身是（　　）。
> A.NSFNET B.TCP/IP C.ARPANET D.UCAID

**答案：C**。ARPANET 是互联网的前身。

#### 例题3：第一个网络诞生时间
> 世界上第一个网络是在（　　）年诞生。
> A.1946 B.1969 C.1977 D.1973

**答案：B**。1969年 ARPANET 诞生。

#### 例题4：Internet 最初目的
> INTERNET最初创建的目的是用于（　　）。
> A.政治 B.经济 C.教育 D.军事

**答案：D**。ARPANET 最初由美国国防部高级研究计划署（ARPA）建立，用于军事目的。

#### 例题5：Internet 标准的发表形式
> 所有的Internet标准都是以（　　）的形式发表。
> A.ISO B.RFC C.HTM D.IEEE

**答案：B**。所有互联网标准都以 RFC 形式发表。

---

## 1.3 互联网的组成

### 核心思想：边缘与核心的划分

从**工作方式**上看，互联网可以划分为两大块：

| 部分 | 组成 | 作用 |
|------|------|------|
| **边缘部分** | 所有连接在互联网上的**主机**（端系统） | 用户直接使用，进行通信和资源共享 |
| **核心部分** | 大量**网络**和连接这些网络的**路由器** | 为边缘部分提供服务（连通性和交换） |

```
┌──────────────────────────────────┐
│        互联网的核心部分            │
│    ┌────┐    ┌────┐    ┌────┐   │
│    │路由器│──│路由器│──│路由器│   │
│    └────┘    └────┘    └────┘   │
│       │         │         │      │
├───────┼─────────┼─────────┼──────┤
│   互联网的边缘部分                │
│  ┌──┴──┐   ┌──┴──┐   ┌──┴──┐   │
│  │主机  │   │主机  │   │主机  │   │
│  └─────┘   └─────┘   └─────┘   │
└──────────────────────────────────┘
```

### 1.3.1 互联网的边缘部分

#### 端系统 (End System)

边缘部分由所有连接在互联网上的主机组成，这些主机称为**端系统**。

端系统的多样性：
- **小型**：普通个人电脑、智能手机、网络摄像头
- **大型**：昂贵的大型计算机或服务器
- **拥有者**：个人、单位或 ISP

#### "计算机之间通信"的真正含义

实际是指：**主机 A 的某个进程和主机 B 上的另一个进程进行通信**。

#### 端系统之间的两种通信方式

| 方式 | 特点 |
|------|------|
| **客户/服务器方式 (C/S)** | 客户是服务请求方，服务器是服务提供方 |
| **对等方式 (P2P)** | 两台主机不区分服务方和请求方，平等对等通信 |

#### C/S 方式详解

```
客户A ──①请求服务──→ 服务器B
客户A ←─②得到服务── 服务器B

通信建立后，双向均可发送和接收数据
```

**客户程序特点**：
- 被用户调用后运行，主动发起通信（请求服务）
- **必须知道服务器程序的地址**
- 不需要特殊硬件和复杂操作系统

**服务器程序特点**：
- 专门用来提供某种服务，可**同时处理多个客户请求**
- **一直不断运行**，被动等待客户请求
- **不需要知道客户程序的地址**
- 一般需要强大硬件和高级操作系统

#### P2P 方式详解

- 两台主机通信时**不区分服务方和请求方**
- 双方都运行 P2P 软件，进行**平等对等连接**
- **本质**上仍使用客户/服务器方式——每个主机既是客户又是服务器
- 例如：BT下载、P2P文件共享

### 1.3.2 互联网的核心部分

#### 核心问题：数据如何从一端传输到另一端？

这是互联网中最复杂的部分。核心部分是向边缘部分中的主机提供**连通性**。

#### 路由器 (Router)

- 在网络核心部分起**特殊作用**
- 是实现**分组交换 (Packet Switching)** 的关键构件
- 任务：**转发收到的分组**
- **分组转发**是网络核心部分最重要的功能

#### 三种交换技术

| 交换方式 | 核心思想 |
|----------|---------|
| **电路交换** | 建立专用物理通路，独占资源 |
| **报文交换** | 整个报文为单位的存储转发 |
| **分组交换** | 将报文分割为分组，逐个存储转发 |

> 互联网核心部分采用**分组交换**技术。

---

#### 1. 电路交换 (Circuit Switching)

**来源**：电话网络

**核心特点**：通话的两个用户**始终占用端到端的通信资源**。

**三个阶段**：
```
建立连接（占用通信资源）
    ↓
通话（一直占用通信资源）
    ↓
释放连接（归还通信资源）
```

**为什么不适合计算机数据**：
- 计算机数据具有**突发性**
- 真正传送数据的时间往往不到 10%，甚至不到 1%
- 通信线路资源绝大部分时间**空闲**

| 优点 | 缺点 |
|------|------|
| 通信时延小 | 建立连接时间长 |
| 有序传输 | 线路独占，使用效率低 |
| 没有冲突 | 灵活性差 |
| 适用范围广 | 难以规格化 |
| 实用性强 | — |
| 控制简单 | — |

> 模拟信号，独占 → 电路交换是面向连接的

---

#### 2. 分组交换 (Packet Switching)

**这是互联网的核心技术。** 需要重点理解。

##### 第一步：分割报文

要发送的整块数据称为**报文 (Message)**。

在发送端，把较长的报文划分成更小的**数据段**。

##### 第二步：添加首部，构成分组

```
报文：110100011010101011010101110001001101001011
              ↓ 分割
    ┌─────────┼─────────┐
 数据段1   数据段2   数据段3
    │         │         │
 添加首部  添加首部  添加首部
    │         │         │
 分组1     分组2     分组3
```

- **分组 (Packet)** = 数据段 + 首部（包头）
- 首部包含**必要的控制信息**（目的地址、源地址等）
- 分组是互联网中传送的**数据单元**

##### 第三步：存储转发

每一个分组在互联网中**独立选择传输路径**。

路由器处理分组的过程：
```
收到分组 → 暂存 → 检查首部 → 查找转发表 → 按目的地址转发到合适接口
```

**关键**：
- 每个分组**独立选择**传输路径（不同分组可能走不同路径）
- 路由器需要**创建和动态维护转发表**
- **存储转发机制**是分组交换技术的核心

##### 第四步：接收端还原

接收端收到分组后**剥去首部**，还原成原来的报文（假定没有差错和丢失）。

##### 分组交换的优缺点

| 优点 | 缺点 |
|------|------|
| 无需建立连接 | 排队延迟 |
| 动态分配线路 | 不保证带宽 |
| 线路利用率高 | 增加开销 |
| 提高线路可靠性 | 可能出现失序/丢失/重复分组 |
| 简化存储管理 | 虚电路服务需建立连接 |
| 加速传输 | — |
| 减少出错概率和重发数据量 | — |

---

#### 3. 报文交换 (Message Switching)

- 20世纪40年代电报通信采用的技术
- 以**整个报文**为单位进行存储转发
- 时延较长（每个节点需缓存整个报文后转发），从几分钟到几小时
- **现在已很少使用**

| 优点 | 缺点 |
|------|------|
| 无需建立连接 | 转发时延大 |
| 动态分配线路 | 需较大存储缓存 |
| 提高线路可靠性 | 需传输额外信息量 |
| 提高线路利用率 | — |
| 提供多目标服务 | — |

---

#### 三种交换方式的可视化比较

```
电路交换：连接建立 → 数据传送（比特流直达终点）→ 连接释放
报文交换：报文 → 存储转发 → 报文 → 存储转发 → 报文 → 存储转发
分组交换：P1→P2→P3→... 逐个分组独立的存储转发
```

#### 三种交换方式的比较总结

| 因素 | 电路交换 | 报文交换 | 分组交换 |
|------|---------|---------|---------|
| 连接建立 | 需要 | 不需要 | 不需要 |
| 传输单位 | 比特流 | 整个报文 | 分组 |
| 线路利用 | 低（独占） | 高 | 高 |
| 时延 | 小（建立后） | 大 | 较小 |
| 适用场景 | 连续大量数据 | 已淘汰 | 突发数据 |
| 灵活性 | 差 | 一般 | 好 |

**选择依据**：
- 若需**连续传送大量数据**且传送时间远大于连接建立时间 → 电路交换传输速率快
- 若传送**突发数据** → 分组交换和报文交换可提高信道利用率
- **分组交换比报文交换时延小，灵活性更好**（一个分组远小于整个报文）

#### 补充：通信子网与资源子网

从逻辑功能看，计算机网络由两部分构成：

| 子网 | 组成 | 任务 |
|------|------|------|
| **通信子网** | 通信线路、网络连接设备、通信协议、通信控制软件 | 将计算机互连，完成数据交换和通信处理 |
| **资源子网** | 计算机系统、终端设备、软件、可共享数据 | 全网面向应用的数据处理，向用户提供各类资源 |

---

### 📝 本节例题

#### 例题1：多目标服务
> 在三种交换方式中，不能提供多目标服务的是（　　）。
> A.电路交换 B.报文交换 C.分组交换 D.直通交换

**答案：A**。电路交换是点对点的独占连接，不能同时向多个目标发送。

#### 例题2：电路交换与面向连接
> 判断：电路交换必定是面向连接的。（　　）

**答案：正确**。电路交换必须经过"建立连接→通信→释放连接"三个阶段，因此必定是面向连接的。

#### 例题3：电话网络与计算机网络的交换技术
> 电话网络采用（　　）交换技术，计算机网络采用（　　）交换技术。

**答案：电路交换；分组交换**。

#### 例题4：主要应用模型
> 目前网络应用系统采用的主要模型是（　　）。
> A.离散个人计算模型 B.主机计算模型 C.客户/服务器计算模型 D.网络/文件服务器计算模型

**答案：C**。客户/服务器（C/S）计算模型是目前网络应用的主要模型。

#### 例题5：电路交换与分组交换对比（多选）
> 以下关于电路交换和分组交换的描述正确的是（　　）。
> A.电路交换延迟小，传输实时性强 B.电路交换网络资源利用率高
> C.分组交换延迟大，传输实时性差 D.分组交换网络资源利用率低

**答案：AC**。
- 电路交换建立连接后延迟小、实时性强（A正确），但线路独占导致资源利用率**低**（B错误）
- 分组交换有排队延迟，实时性相对差（C正确），但动态分配线路所以资源利用率**高**（D错误）

#### 例题6：传输延迟长且不固定
> 在下列数据交换方式中，数据经过网络的传输延迟长而且是不固定的，不能用于语音数据传输的是（　　）。
> A.电路交换 B.报文交换 C.数据报交换 D.虚电路交换

**答案：B**。报文交换以整个报文为单位存储转发，延迟最长且不固定，不适合语音传输。

---

## 1.4 计算机网络在我国的发展

### 时间线

| 时间 | 事件 |
|------|------|
| **1980年** | 铁道部开始计算机联网实验 |
| **1989年11月** | 我国第一个公用分组交换网 **CNPAC** 建成运行 |
| **1994年4月20日** | 用 64 kbit/s 专线正式连入互联网，我国被国际上正式承认为**接入互联网的国家** |
| **1994年5月** | 中国科学院高能物理研究所设立我国**第一个万维网服务器** |
| **1994年9月** | 中国公用计算机互联网 **CHINANET** 正式启动 |

### 目前最大规模的五大公用计算机网络

| 网络 | 归属 |
|------|------|
| **CHINANET** | 中国电信互联网 |
| **UNINET** | 中国联通互联网 |
| **CMNET** | 中国移动互联网 |
| **CERNET** | 中国教育和科研计算机网 |
| **CSTNET** | 中国科学技术网 |

> 以上五大网络均基于互联网技术并能与互联网互连。

---

## 1.5 计算机网络的类别

### 1.5.1 计算机网络的定义

#### 较好的定义

> 计算机网络主要是由一些**通用的、可编程的硬件**互连而成的，这些硬件并非专门用来实现某一特定目的。这些可编程的硬件能够用来传送**多种不同类型的数据**，并能支持广泛的和日益增长的应用。

**本质**：计算机网络 = 计算机技术 + 通信技术，是自主计算机的**互连集合**。

关键要素：
- **自主计算机**：每台计算机有自己的操作系统，能独立运行
- **互连**：通过通信链路连接
- **通用的、可编程的**：不是专用系统，能支持多种应用

### 1.5.2 几种不同类别的计算机网络

#### 分类方法一：按网络作用范围

| 类别 | 缩写 | 作用范围 |
|------|------|---------|
| **广域网** | WAN | 几十到几千公里，是互联网的核心部分 |
| **城域网** | MAN | 一般一个城市，约 5~50 公里 |
| **局域网** | LAN | 较小范围（约 1 公里左右），通常采用高速通信线路 |
| **个人区域网** | PAN | 约 10 米左右，也称无线个人区域网 WPAN |

> 若中央处理机之间距离非常近（如 1 米甚至更小），一般称为**多处理机系统**而不称为计算机网络。

#### 分类方法二：按网络使用者

| 类别 | 特点 |
|------|------|
| **公用网 (Public Network)** | 按规定交费即可使用 |
| **专用网 (Private Network)** | 为特殊业务工作需要而建造 |

> 两者都可以传送多种业务。传送计算机数据时分别称为公用计算机网络和专用计算机网络。

#### 分类方法三：接入网 (Access Network, AN)

- 又称本地接入网或居民接入网
- 用于将**用户接入互联网**
- 既不是核心部分，也不是边缘部分
- 是从某个用户端系统到本地 ISP 的**第一个路由器（边缘路由器）** 之间的一种网络
- 从覆盖范围看，很多接入网属于局域网

---

### 📝 本节例题

#### 例题1：分类依据
> 计算机网络分为广域网、城域网和局域网，其划分的主要依据是（　　）。
> A.网络的作用范围 B.网络的拓扑结构 C.网络的通信方式 D.网络的传输介质

**答案：A**。按作用范围（覆盖距离）划分。

#### 例题2：局域网与广域网的本质差异
> 局域网和广城网的差异不仅在于它们所覆盖的范围不同，还主要在于它们（　　）。
> A.所提供的服务不同 B.所使用的介质不同 C.所能支持的通信量不同 D.所使用的协议不同

**答案：D**。局域网和广域网的根本区别在于使用的协议不同，因为覆盖范围不同决定了协议设计的不同（如广域网需考虑长距离的传播时延、路由选择等问题）。

#### 例题3：覆盖范围最小
> 在计算机网络分类中，覆盖范围最小的网络类型是（　　）。
> A.广域主干网 B.高速局域网 C.个人区域网 D.宽带城域网

**答案：C**。个人区域网 (PAN) 覆盖范围约 10 米。

#### 例题4：三类网络
> 计算机网络可分为三类，它们是（　　）。
> A.Internet、Intranet、Extranet B.广播式网络、移动网络、点-点式网络
> C.X.25、ATM、B-ISDN D.LAN、MAN、WAN

**答案：D**。按作用范围分为局域网 (LAN)、城域网 (MAN)、广域网 (WAN)。

---

## 1.6 计算机网络的性能

### 1.6.1 计算机网络的性能指标

理解性能指标是理解网络设计的基础。性能从不同维度度量计算机网络的"好坏"。

---

#### 1. 速率 (Rate)

**定义**：数据的传送速率，也称数据率 (Data Rate) 或比特率 (Bit Rate)。

**单位**：bit/s（bps），及 kbit/s、Mbit/s、Gbit/s 等。

**单位换算**（注意！与存储容量的区别）：

| 前缀 | 符号 | 数值（速率） | 数值（存储容量） |
|------|------|-------------|-----------------|
| 千 | K/k | 10³ = 1,000 | 2¹⁰ = 1,024 |
| 兆 | M | 10⁶ = 1,000,000 | 2²⁰ = 1,048,576 |
| 吉 | G | 10⁹ | 2³⁰ |
| 太 | T | 10¹² | 2⁴⁰ |

> **注意**：1 字节 (Byte) = 8 比特 (bit)。速率通常指**额定速率**（标称速率），非实际运行速率。

---

#### 2. 带宽 (Bandwidth)

带宽有两种含义，本质相同：

| 视角 | 含义 | 单位 |
|------|------|------|
| **频域**（通信领域） | 信号具有的频带宽度 | 赫兹 (Hz) |
| **时域**（计算机领域） | 单位时间内信道能通过的"最高数据率" | bit/s |

**核心理解**：一条通信链路的带宽越宽，其所能传输的最高数据率也越高。

---

#### 3. 吞吐量 (Throughput)

**定义**：单位时间内通过某个网络（或信道、接口）的**实际数据量**。

**关键特征**：
- 受带宽或额定速率的限制——额定速率是**绝对上限**
- 实际值可能**远小于**额定速率，甚至下降到零！
- 有时用每秒传送的字节数或帧数来表示

---

#### 4. 时延 (Delay / Latency) ⭐重点

**定义**：数据从网络（或链路）的一端传送到另一端所需的时间。

**总时延公式**：
```
总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延
```

##### (1) 发送时延（传输时延）

```
发送时延 = 数据帧长度 (bit) / 发送速率 (bit/s)
```

- 主机或路由器**发送数据帧**所需的时间
- 从第一个比特发送开始，到最后一个比特发送完毕
- 发生在机器**内部的发送器**中

##### (2) 传播时延

```
传播时延 = 信道长度 (m) / 信号在信道上的传播速率 (m/s)
```

- 电磁波在信道中**传播一定距离**所需的时间
- 发生在机器**外部的传输信道媒体**上
- 电磁波传播速率：
  - 自由空间（光速）：3.0 × 10⁵ km/s
  - 铜线电缆：约 2.3 × 10⁵ km/s
  - 光纤：约 2.0 × 10⁵ km/s

##### 🚨 关键辨析：发送时延 vs 传播时延

| 维度 | 发送时延 | 传播时延 |
|------|---------|---------|
| 发生位置 | 机器内部（发送器） | 机器外部（传输媒体） |
| 决定因素 | 数据帧长度 + 发送速率 | 信道长度 + 传播速率 |
| 与距离的关系 | **无关** | 距离越远越大 |
| 与发送速率的关系 | 速率越高越小 | **无关** |

> 🚨 **常见错误**："在高速链路上，比特会传送得更快些。"—这是**错误**的！
>
> 高速链路提高的仅仅是**数据的发送速率**，而不是比特在链路上的**传播速率**。提高发送速率只是减小了**发送时延**。

##### (3) 处理时延

主机或路由器收到分组后，为处理分组（分析首部、提取数据、差错检验、查找路由）所花费的时间。

##### (4) 排队时延

分组在路由器输入输出队列中排队等待处理和转发所经历的时延。

- 长短取决于网络中当时的**通信量**
- 通信量很大时会发生**队列溢出**，使分组丢失 → 等价于**排队时延为无穷大**

##### 四种时延产生的位置

```
结点A（发送方）                   结点B（接收方）
┌──────────┐                    ┌──────────┐
│ 发送器    │                    │ 接收器    │
│ ↑发送时延 │                    │          │
│ 队列      │                    │ ↑处理时延 │
│ ↑排队时延 │────链路────────→ │ ↑排队时延 │
│          │    ↑传播时延        │          │
└──────────┘                    └──────────┘
```

##### 分析举例1

> 结点A要将 100000 B 数据块通过 **100000 km** 光纤发送给结点B，带宽 **100 Mbit/s**。
> 结点C要将 100000 B 数据块通过 **1 km** 光纤发送给结点D，带宽 **10 Mbit/s**。
> 忽略处理时延和排队时延。

**解**：
- 情况(1)：传播时延 = 100000 ÷ (2.0×10⁵) = 500 ms，发送时延 = (100000×8) ÷ 10⁸ = 8 ms，总时延 = **508 ms**
- 情况(2)：传播时延 = 1 ÷ (2.0×10⁵) = 5 ms，发送时延 = (100000×8) ÷ 10⁷ = 80 ms，总时延 = **85 ms**

> 结论：**不能笼统地说"数据的发送速率越高，其传送的总时延就越小"**。总时延受多种因素决定，必须具体分析。当传播时延占主导时，提高发送速率对总时延改善很小。

##### 分析举例2（存储转发时延）

> 所有链路 100Mbps，分组 1000B（头部 20B），文件 980000B，H1→H2 经过两个交换机。求最短时间。

解：
- 分组数据部分：1000B - 20B = 980B
- 分组数量：980000B ÷ 980B = **1000** 个
- 单个分组转发时延：(1000 × 8) ÷ 100Mbps = **0.08 ms**
- 经过 **2** 个交换设备，产生 **2** 个分组的额外转发延迟
- 总时间：0.08 ms × (1000 + 2) = **80.16 ms**

---

#### 5. 时延带宽积

```
时延带宽积 = 传播时延 × 带宽
```

**物理含义**：链路的时延带宽积又称**以比特为单位的链路长度**。

**管道比喻**：
- 链路像一根**空心管道**
- 时延 = 管道长度
- 带宽 = 管道截面积
- 时延带宽积 = 管道的**容积**（能容纳的比特数）

```
管道中的比特数 = 从发送端发出但尚未到达接收端的比特
只有当管道充满比特时，链路才得到充分利用。
```

---

#### 6. 往返时间 RTT (Round-Trip Time)

**定义**：从发送方发送完数据，到发送方收到来自接收方的确认总共经历的时间。

```
RTT = 结点A到B的传播时延 tP
    + 结点B处理+排队时延 tPQB
    + 结点B发送时延 tTB
    + 结点B到A的传播时延 tP
    ≈ 2 × 传播时延 + 结点B处理/排队/发送时延
```

**注意**：
- 在互联网中，RTT 还包括各**中间结点**的处理时延、排队时延和转发时延
- 使用**卫星通信**时，RTT 相对较长——是重要的性能指标

##### 分析举例：有效数据率

> 结点A以 100 Mbit/s 发送 100 MB 数据给结点B，RTT = 2 s。B 收完后立即确认（确认信息很短），A 收到确认后才能继续发送。求有效数据率。

**解**：
- 发送时延 = (100 × 2²⁰ × 8) ÷ (100 × 10⁶) ≈ 8.39 s
- 有效数据率 = 数据长度 ÷ (发送时延 + RTT) = (100 × 2²⁰ × 8) ÷ (8.39 + 2) ≈ **80.7 Mbit/s**

> 虽然链路速率是 100 Mbit/s，但因需要等待确认（RTT = 2s），有效数据率只有约 80.7 Mbit/s。

---

#### 7. 利用率

| 概念 | 定义 |
|------|------|
| **信道利用率** | 某信道有百分之几的时间被利用（有数据通过） |
| **网络利用率** | 全网络的信道利用率的加权平均值 |

**利用率是否越高越好？不是！**

根据排队论，当利用率增大时，时延会迅速增加：

```
D = D₀ / (1 - U)
```

其中 D₀ 是空闲时延，D 是当前时延，U 是利用率 (0~1)。

```
时延 D
  ↑
  │              ·
  │            ·
  │         ·
  │      ·
  │   ·
  │·
  └──────────────→ 利用率 U
  D₀          1.0
```

> 当 U → 1 时，D → ∞。因此网络设计应在利用率与时延之间取得平衡。

---

### 1.6.2 计算机网络的非性能特征

这些特征与性能指标密切相关，但并非直接反映网络性能：

| 非性能特征 | 说明 |
|-----------|------|
| **费用** | 网络建设和运营成本 |
| **质量** | 网络设备和服务的质量 |
| **标准化** | 遵循标准的程度 |
| **可靠性** | 网络长期稳定运行的能力 |
| **可扩展性和可升级性** | 网络能否方便地扩展和升级 |
| **管理和维护** | 网络运维的便捷性 |

---

### 📝 本节例题

#### 例题1：文件传递时延计算
> 假设两个节点间的链路长6,000千米，网络带宽为10Mbps，传播速度为 2×10⁸ 米/秒。现在在两个节点间传递一个10Mbit的文件，则整个文件的传递时延估计是（　　）。

**解**：
- 发送时延 = 10 Mbit ÷ 10 Mbps = **1 s**
- 传播时延 = 6,000,000 m ÷ (2 × 10⁸ m/s) = **0.03 s**
- 总时延 = 1 + 0.03 = **1.03 s**

> 答案：**1.03秒**

#### 例题2：网络时延不包括
> 网络时延不包括（　　）。
> A.传输时延 B.传播时延 C.排队时延 D.服务时延

**答案：D**。网络时延 = 发送（传输）时延 + 传播时延 + 处理时延 + 排队时延。不包括"服务时延"。

---

## 1.7 计算机网络的体系结构

### 1.7.1 计算机网络体系结构的形成

#### 第一性原理：为什么要分层？

两台计算机要互相传送文件，需解决大量复杂问题：

1. 必须有一条传送数据的通路
2. 发起方必须激活通路
3. 要告诉网络如何识别接收方
4. 发起方要清楚对方是否已开机且与网络连接正常
5. 对方是否准备好接收和存储文件
6. 若文件格式不兼容，要完成格式转换
7. 要处理各种差错和意外事故，保证收到正确的文件

**分层的本质**：将庞大而复杂的问题，转化为若干**较小的局部问题**逐个解决。

#### 历史发展

| 时间 | 事件 |
|------|------|
| **ARPANET 设计之初** | 提出分层的设计方法 |
| **1974年** | IBM 提出系统网络体系结构 SNA |
| **此后** | 各公司推出不同名称的体系结构（但互不兼容） |
| **1983年** | ISO 形成 OSI/RM 七层协议体系结构（ISO 7498 国际标准） |

#### OSI/RM 七层模型

```
应用层   (7)  Application
表示层   (6)  Presentation
会话层   (5)  Session
运输层   (4)  Transport
网络层   (3)  Network
数据链路层 (2)  Data Link
物理层   (1)  Physical
```

#### OSI 失败的原因

尽管 OSI 是法律上的国际标准，但最终失败：

1. OSI 专家**缺乏实际经验**，完成标准时**没有商业驱动力**
2. OSI 协议实现**过分复杂**，运行效率很低
3. OSI 标准制定**周期太长**，设备无法及时进入市场
4. OSI 层次划分**不太合理**，有些功能在多个层次中重复出现

#### 两种国际标准

| 标准 | 地位 |
|------|------|
| **OSI**（法律上的国际标准） | 概念清楚、理论完整，但**未获市场认可** |
| **TCP/IP**（事实上的国际标准） | 获得了**最广泛的应用** |

---

### 1.7.2 协议与划分层次

#### 网络协议

**定义**：为进行网络中的数据交换而建立的**规则、标准或约定**。

**三个组成要素**：

| 要素 | 含义 | 示例 |
|------|------|------|
| **语法** | 数据与控制信息的**结构或格式** | 分组首部的字段排列格式 |
| **语义** | 需要发出何种控制信息，完成何种动作，做出何种响应 | 请求/响应的含义 |
| **同步（时序）** | 事件实现顺序的详细说明 | 先建立连接，再传输数据，最后释放连接 |

> 网络协议是计算机网络**不可缺少的组成部分**。

#### 协议的两种形式

| 形式 | 目的 |
|------|------|
| **文字描述** | 便于人来阅读和理解 |
| **程序代码** | 让计算机能够理解 |

不论什么形式，都必须能对网络上的信息交换过程做出**精确的解释**。

#### 分层举例：传送文件

```
层次3：文件传送模块 — 好像可把文件直接传送到对方
层次2：通信服务模块 — 好像可直接把文件可靠地传送到对方
层次1：网络接入模块 — 负责与网络接口细节有关的工作，向上层提供接入和通信服务
```

#### 分层的优点与缺点

| 优点 | 缺点 |
|------|------|
| 各层之间**独立** | 有些功能重复出现，产生**额外开销** |
| **灵活性**好 | |
| 结构上可**分割开** | |
| 易于**实现和维护** | |
| 能**促进标准化**工作 | |

> 层数太少 → 每层协议太复杂
> 层数太多 → 描述和综合各层功能时困难
> **黄金法则：每一层的功能应非常明确。**

#### 各层需要完成的主要功能

| 功能 | 说明 |
|------|------|
| **差错控制** | 使对等层通信更可靠 |
| **流量控制** | 发送速率不要超过接收端处理能力 |
| **分段和重装** | 大数据块分段→接收端还原 |
| **复用和分用** | 多个高层会话复用一条低层连接 |
| **连接建立和释放** | 先建立逻辑连接再交换数据，结束后释放 |

#### 体系结构 vs 实现

| 概念 | 性质 |
|------|------|
| **体系结构 (Architecture)** | **抽象**的，是各层及其协议的集合，精确定义功能 |
| **实现 (Implementation)** | **具体**的，是真正运行的硬件和软件 |

---

### 📝 本节例题

#### 例题1：OSI 制定组织
> 在以下国际组织中，制定OSI参考模型的是（　　）。
> A.ISO B.IEEE C.IETF D.ARPA

**答案：A**。ISO（国际标准化组织）制定了 OSI/RM。

#### 例题2：OSI 层次数
> OSI参考模型将整个网络的功能划分（　　）个层次。
> A.1 B.3 C.5 D.7

**答案：D**。OSI 是七层协议体系结构。

#### 例题3：协议要素
> 关于网络协议的描述中，错误的是（　　）。
> A.由语法、语义与时序三个要素组成
> B.组织方式经常采用层次结构模型
> C.为网络数据交换制订的规则与标准
> D.语法是对事件的实现顺序的说明

**答案：D**。语法是**数据与控制信息的结构或格式**，对事件实现顺序的说明是**同步（时序）**。

---

### 1.7.3 具有五层协议的体系结构

#### 三种体系结构的对比

```
OSI七层          TCP/IP四层        五层协议（教学用）
─────────       ──────────        ──────────
应用层 (7)                       
表示层 (6)      应用层 (4)        应用层 (5)
会话层 (5)
运输层 (4)      运输层 (3)        运输层 (4)
网络层 (3)      网际层 (2)        网络层 (3)
数据链路层 (2)                    数据链路层 (2)
物理层 (1)      网络接口层 (1)     物理层 (1)
```

> TCP/IP 最下面的**网络接口层并没有具体内容**，因此教学中常使用**五层协议**的体系结构。

---

#### 各层功能详解

##### 第5层：应用层 (Application Layer)

- **任务**：通过应用进程间的交互来完成**特定网络应用**
- **协议**：定义应用进程间通信和交互的规则
- **数据单元**：**报文 (Message)**
- **常见协议**：DNS、HTTP、SMTP 等

##### 第4层：运输层 (Transport Layer)

- **任务**：负责向两台主机中**进程之间**的通信提供**通用的数据传输服务**
- **功能**：复用和分用
- **两种协议**：

| 协议 | 特点 | 数据单元 |
|------|------|---------|
| **TCP** | 面向连接、可靠传输 | 报文段 (Segment) |
| **UDP** | 无连接、尽最大努力（不保证可靠性） | 用户数据报 |

##### 第3层：网络层 (Network Layer)

- **任务**：为分组交换网上的**不同主机**提供通信服务
- **两个具体任务**：

| 任务 | 说明 |
|------|------|
| **路由选择** | 通过算法在每个路由器生成转发表 |
| **转发** | 依据转发表把分组转发到下一跳 |

- **协议**：IP (Internet Protocol)——无连接的网际协议
- **数据单元**：IP 数据报（简称数据报）
- 也称为**网际层**或 **IP 层**

##### 第2层：数据链路层 (Data Link Layer)

- **任务**：实现两个**相邻节点**之间的可靠通信
- **数据单元**：**帧 (Frame)**
- **处理差错**：简单丢弃出错帧；若需纠正，采用可靠传输协议（但这会使其复杂化）
- 也称为**链路层**

##### 第1层：物理层 (Physical Layer)

- **任务**：实现比特（0或1）的传输
- **主要考虑**：
  - 用什么信号（电压）表示数据
  - 连接电缆的插头引脚数及连接方式
- ⚠️ **注意**：传递信息的物理媒体（双绞线、同轴电缆、光缆、无线信道等）**不在物理层协议之内**，而是在物理层协议的**下面**

---

#### 数据在各层之间的传递过程（封装与解封装）

```
发送方（主机1）                       接收方（主机2）
┌─────────┐                          ┌─────────┐
│ 应用层   │  H5 + 数据              │ 应用层   │ → 取出数据
│ 运输层   │  H4 + H5 + 数据         │ 运输层   │ → 去掉H4
│ 网络层   │  H3 + H4+H5+数据        │ 网络层   │ → 去掉H3
│ 链路层   │  H2 + H3+H4+H5+数据 + T2│ 链路层   │ → 去掉H2,T2
│ 物理层   │  比特流 →→→→→→→→→→→→│ 物理层   │
└─────────┘                          └─────────┘
        封装（加首部/尾部）     解封装（剥首部/尾部）
```

**中间路由器**：最高只用到**网络层**（第3层），不需要运输层和应用层。

```
主机1 → 路由器（到达时：物理层→链路层→网络层→查看转发表→链路层→物理层）→ 主机2
```

#### 对等层通信

**协议数据单元 PDU (Protocol Data Unit)**：对等层次之间传送的数据单位。

关键理解：各层协议实际上就是在各个**对等层**之间传递数据时的各项规定。

---

### 1.7.4 实体、协议、服务和服务访问点

#### 核心概念

| 概念 | 定义 |
|------|------|
| **实体 (Entity)** | 任何可发送或接收信息的硬件或软件进程 |
| **协议 (Protocol)** | 控制两个**对等实体**进行通信的规则的集合 |
| **服务 (Service)** | 在协议控制下，两个对等实体间通信使得本层能向**上一层**提供服务 |

#### 协议 vs 服务的本质区别

| 维度 | 协议 | 服务 |
|------|------|------|
| 方向 | **水平的**（对等层之间） | **垂直的**（上下层之间） |
| 可见性 | 下层协议对上层是**透明**的 | 上层只能看见服务，**看不见**下层协议 |
| 关系 | 协议的实现保证能向上一层提供服务 | 上层使用服务原语获取下层提供的服务 |

> **"协议是水平的，服务是垂直的"** — 这是理解分层体系结构的关键。

#### 服务访问点 SAP (Service Access Point)

- 同一系统中**相邻两层**的实体进行交互（交换信息）的地方
- 是一个**抽象概念**，实际上是**逻辑接口**
- **比喻**：像一个封装好的函数——调用者只需知道功能与参数，不关心实现细节

#### SDU 与 PDU 的关系

- OSI 把层与层之间交换的数据单位称为**服务数据单元 SDU (Service Data Unit)**
- **SDU 可以与 PDU 不一样**
  - 本层的 PDU 可以转换为下层的 SDU
  - 可以多个 SDU 合成为一个 PDU
  - 也可以一个 SDU 划分为几个 PDU

```
┌──────────────────────────┐
│  第 n+1 层               │
│  实体(n+1) ←→ 实体(n+1)  │ ← 协议(n+1)
│    ↕ SAP         ↕ SAP   │
│  实体(n) ←→ 实体(n)      │ ← 协议(n)
│  第 n 层（服务提供者）    │
└──────────────────────────┘
```

---

### 1.7.5 TCP/IP 的体系结构

#### 四层结构

```
┌────────────┐
│  应用层     │  DNS, HTTP, SMTP, RTP ...
├────────────┤
│  运输层     │  TCP, UDP
├────────────┤
│  网际层     │  IP
├────────────┤
│ 网络接口层  │  （无具体内容）
└────────────┘
```

**关键要点**：
- 路由器转发分组时**最高只用到网际层**，没有使用运输层和应用层
- 现在某些应用可以直接使用 IP 层，甚至直接使用最下面的网络接口层

#### 沙漏计时器形状

```
         ┌──────────────┐
         │  HTTP SMTP DNS RTP ... │  ← 应用层（多种协议）
         ├──────────────┤
         │   TCP   UDP  │  ← 运输层
         │       │      │
         │    ┌──┴──┐   │
         │    │  IP   │   │  ← 网际层（核心）
         │    └──┬──┘   │
         ├───────┼──────┤
         │ 网络接口1 网络接口2 ... │  ← 网络接口层（多种）
         └──────────────┘
```

**IP 的设计哲学**：
- **IP over Everything**：IP 协议可以在多种类型的网络上运行
- **Everything over IP**：IP 层可以支持多种运输层协议
- **核心思想**：网络核心部分**越简单越好**——复杂留给边缘

#### 客户-服务器通信的全栈过程

```
客户A                                    服务器B
应用层   ─────发起连接建立请求─────→    应用层
运输层   ─────TCP连接─────────────→    运输层
网络层   ─────IP路由──────────────→    网络层
链路层   ─────帧传输──────────────→    链路层
物理层   ─────比特流──────────────→    物理层
```

所有这些通信实际上都需要使用**下面各层所提供的服务**。

---

### 📝 本节（1.7）例题汇总

#### 例题1：各层相互独立？
> 关于网络体系结构，正确的是（　　）。
> A.各层之间相互独立，没有联系
> B.相邻层实体之间通信要遵循共同的协议
> C.对等层实体之间通信要遵循共同的协议
> D.所有层次之间的通信都是逻辑通信

**答案：C**。对等层之间的通信遵循共同的协议。相邻层之间是**服务**关系，不是协议关系。各层之间有服务调用关系（不是没有联系）。物理层是**实际**通信，不是逻辑通信。

#### 例题2：数据链路层与运输层之间
> 在OSI七层结构模型中，处于数据链路层与运输层之间的是（　　）。
> A.物理层 B.网络层 C.会话层 D.表示层

**答案：B**。OSI 七层从下到上：物理层 → 数据链路层 → **网络层** → 运输层 → 会话层 → 表示层 → 应用层。

#### 例题3：OSI 各层顺序
> 下述哪个选择顺序描述了OSI模型的各层？（　　）
> A.物理，数据链路，网络，运输，系统，表示，应用
> B.物理，数据链路，网络，运输，表示，会话，应用
> C.物理，数据链路，网络，传输，会话，表示，应用
> D.表示，数据链路，网络，运输，系统，物理，应用

**答案：C**。OSI 七层顺序：物理→数据链路→网络→运输→会话→表示→应用。注意：选项B的"表示"和"会话"顺序反了。

#### 例题4：N层实体通信使用的服务
> 在开放系统互连环境中，两个N层实体进行通信，可能用到的服务是（　　）。
> A.N-1层提供的服务 B.N层提供的服务 C.N+1层提供的服务 D.以上都不对

**答案：A**。本层使用下层（N-1层）提供的服务。

#### 例题5：第N层与第N+1层的关系
> 在OSI模型中，一个层N与它的上层（第N+1层）的关系是什么？（　　）
> A.第N层为第N+1层提供服务
> B.第N+1层把从第N层接收到的信息添一个报头
> C.第N层使用第N+1层第通的服务
> D.第N层与第N+1层相互没有影响

**答案：A**。第N层为第N+1层提供服务。上层使用下层的服务。

#### 例题6：对等层首部读取
> 当数据从主机A到主机B时，A中第3层所添加的首部被B中第（　　）层读取。
> A.3 B.2 C.1 D.4

**答案：A**。对等层通信——发送方第N层添加的首部，由接收方第**同一层（N层）** 读取和处理。

#### 例题7：网络分层概念
> 关于网络分层的概念，下面说法错误的是（　　）。
> A.上层使用下层提供的服务
> B.网络中两台主机的对等层之间是直接进行通信的
> C.分层后各层易于实现和维护，能促进标准化工作
> D.协议是"水平"的，服务是"垂直"的

**答案：B**。对等层之间是**逻辑通信**（虚拟通信），不是直接物理通信。除物理层外，其他层的对等通信都是通过下层来实现的。

#### 例题8：OSI 各层功能
> 关于OSI参考模型各层功能的描述中，错误的是（　　）。
> A.物理层实现比特流传输 B.网络层实现介质访问控制
> C.传输层提供端到端服务 D.应用层提供各种网络应用

**答案：B**。介质访问控制（MAC）是**数据链路层**的功能，不是网络层的功能。网络层的功能是路由选择和转发。

#### 例题9：TCP/IP 模型（多选）
> 关于TCP/IP参考模型的描述中，错误的是（　　）。
> A.共分为7个层次 B.由ISO负责制定 C.最高层是应用层
> D.最低层是物理层 E.独立于特定的网络硬件

**答案：ABD**。
- A错误：TCP/IP 是**4层**，不是7层
- B错误：TCP/IP不是由ISO制定的
- D错误：最低层是**网络接口层**，不是物理层
- C正确：最高层是应用层
- E正确：TCP/IP 独立于特定网络硬件

#### 例题10：端到端应答、排序、流量控制
> 在OSI参考模型中，实现端到端的应答、分组排序和流量控制功能的协议层是（　　）。
> A.数据链路层 B.网络层 C.运输层 D.会话层

**答案：C**。**运输层**提供端到端的可靠通信服务，包括应答、排序、流量控制。

#### 例题11：PDU
> 在OSI参考模型中，对等实体在一次交互作用中传输的信息单位称为（　　），它包括控制信息和用户数据两部分。
> A.接口数据单元 B.服务数据单元 C.协议数据单元 D.交互数据单元

**答案：C**。对等层之间传输的数据单位是**协议数据单元 (PDU)**。

#### 例题12：表示层功能
> 下列功能中，属于OSI参考模型中的表示层提供的是（　　）。
> A.交互管理 B.透明传输 C.死锁管理 D.文本压缩

**答案：D**。表示层负责**数据格式转换**，包括文本压缩、加密、编码转换等。交互管理和死锁管理属于会话层的功能。

#### 例题13：体系结构正确说法
> 关于网络体系结构，正确的是（　　）。
> A.各层之间相互独立，没有联系
> B.相邻层实体之间通信要遵循共同的协议
> C.对等层实体之间通信要遵循共同的协议
> D.所有层次之间的通信都是逻辑通信

**答案：C**。（同例题1）

---

## 附录：全章知识体系总览

```
第1章 概述
├── 1.1 计算机网络在信息时代中的作用
│   ├── 三大传统网络（电信、有线电视、计算机网络）
│   ├── 三网融合（以计算机网络技术为核心）
│   ├── Internet vs internet（专有名词 vs 通用名词）
│   ├── 两大基本特点（连通性、资源共享）
│   ├── 互联网+：新经济形态
│   └── 负面影响与管理
│
├── 1.2 互联网概述
│   ├── 1.2.1 网络的网络
│   │   ├── 网络 = 节点 + 链路
│   │   └── 互连网 = 网络的网络
│   ├── 1.2.2 三个发展阶段
│   │   ├── 1969-1990：ARPANET → 互联网络
│   │   ├── 1985-1993：三级结构（NSFNET）
│   │   └── 1993-现在：多层次ISP + IXP + 内容提供者
│   └── 1.2.3 标准化工作
│       ├── ISOC/IAB/IETF/IRTF 组织架构
│       ├── RFC 形式发表
│       └── 标准化过程（草案→建议标准→草案标准→互联网标准）
│
├── 1.3 互联网的组成
│   ├── 1.3.1 边缘部分（端系统）
│   │   ├── C/S 方式（客户请求，服务器提供服务）
│   │   └── P2P 方式（对等通信，既是客户又是服务器）
│   └── 1.3.2 核心部分（路由器+分组交换）
│       ├── 电路交换：建立连接→通话→释放（独占资源）
│       ├── 报文交换：整个报文存储转发（已淘汰）
│       ├── 分组交换：分割为分组→独立选择路径→存储转发（互联网采用）
│       └── 通信子网 + 资源子网
│
├── 1.4 计算机网络在我国的发展
│   ├── 1980年：铁道部联网实验
│   ├── 1994年：正式接入互联网（64 kbit/s）
│   └── 五大公用网络（CHINANET, UNINET, CMNET, CERNET, CSTNET）
│
├── 1.5 计算机网络的类别
│   ├── 定义：通用可编程硬件互连 + 支持多种应用
│   └── 分类：
│       ├── 按作用范围：WAN / MAN / LAN / PAN
│       ├── 按使用者：公用网 / 专用网
│       └── 接入网 AN
│
├── 1.6 计算机网络的性能
│   ├── 性能指标
│   │   ├── 速率 (bit/s)
│   │   ├── 带宽（频域Hz / 时域bit/s）
│   │   ├── 吞吐量（实际数据量）
│   │   ├── 时延 = 发送 + 传播 + 处理 + 排队 ⭐
│   │   ├── 时延带宽积（管道容积）
│   │   ├── RTT（往返时间）
│   │   └── 利用率（不是越高越好！D = D₀/(1-U)）
│   └── 非性能特征：费用/质量/标准化/可靠性/扩展性/维护
│
└── 1.7 计算机网络的体系结构 ⭐
    ├── 1.7.1 体系结构形成（分层思想）
    ├── 1.7.2 协议（语法+语义+时序）与层次划分
    ├── 1.7.3 五层协议体系结构
    │   ├── 应用层（报文）
    │   ├── 运输层（TCP段/UDP数据报）
    │   ├── 网络层（IP数据报）
    │   ├── 数据链路层（帧）
    │   └── 物理层（比特流）
    ├── 1.7.4 实体/协议/服务/SAP
    │   ├── 协议是"水平"的（对等层之间）
    │   └── 服务是"垂直"的（上下层之间）
    └── 1.7.5 TCP/IP体系结构
        ├── IP over Everything
        └── Everything over IP
```

---

---

## 附录B：第1章完整定义与公式汇总

> 按知识点体系组织，方便系统复习和查阅。每个条目给出严格定义。

---

### 第一层：计算机网络的基本概念

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 1 | **网络 (Network)** | 由若干**节点 (Node)** 和连接这些节点的**链路 (Link)** 组成的集合。节点可以是计算机、集线器、交换机或路由器等。 |
| 2 | **节点 (Node)** | 网络中的连接点，可以是计算机、集线器、交换机或路由器等设备。 |
| 3 | **链路 (Link)** | 连接网络中两个节点的物理或逻辑通路。 |
| 4 | **主机 (Host)** | 与网络相连的计算机，也称为端系统。 |
| 5 | **互连网 (internet/internetwork)** | 多个网络通过**路由器**相互连接构成的范围更大的计算机网络，本质是"网络的网络" (network of networks)。协议可任意选择，不一定是 TCP/IP。 |
| 6 | **互联网/因特网 (Internet)** | 专有名词（首字母大写），指当前全球最大的、开放的、由众多网络相互连接而成的特定互连网。采用 **TCP/IP 协议族**作为通信规则，前身为美国的 ARPANET。Internet ⊂ internet（互联网是互连网的一种特例）。 |
| 7 | **计算机网络 (Computer Network)** | 由一些通用的、可编程的硬件互连而成的系统。这些硬件并非专门用来实现某一特定目的，能够用来传送多种不同类型的数据，并能支持广泛的和日益增长的应用。= 计算机技术 + 通信技术，是自主计算机的互连集合。 |

### 第二层：互联网的基本特点与概念

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 8 | **连通性 (Connectivity)** | 互联网的基本特点之一：使上网用户之间可以非常便捷、经济地交换各种信息，仿佛这些用户终端都彼此直接连通一样。 |
| 9 | **资源共享 (Sharing)** | 互联网的基本特点之一：实现信息共享、软件共享、硬件共享。由于网络的存在，这些资源好像就在用户身边一样方便地使用。 |
| 10 | **三网融合** | 传统电信网、计算机网（互联网）和有线电视网融合到现代计算机网络技术中的趋势。本质是用统一的 IP 技术承载所有类型的业务。 |
| 11 | **互联网+** | 指"互联网+各个传统行业"，即把互联网的创新成果深度融合于经济社会各领域（工业、农业、教育、医疗、商业、交通、金融、政务等），形成新的经济形态。 |

### 第三层：互联网的发展与标准化

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 12 | **ARPANET** | 美国高级研究计划署 (ARPA) 于 1969 年建立的第一个分组交换网，是互联网的前身。最初只有 4 个节点，1990 年正式关闭。 |
| 13 | **ISP (Internet Service Provider)** | 互联网服务提供者。提供接入互联网的服务并收取费用。分为主干 ISP、地区 ISP 和本地 ISP 三个层次。 |
| 14 | **IXP (Internet eXchange Point)** | 互联网交换点。允许两个网络直接相连并快速交换分组，用于优化数据流量分布、减少转发延时和降低费用。常采用工作在数据链路层的网络交换机。 |
| 15 | **内容提供者 (Content Provider)** | 在互联网上向所有用户提供视频等内容的公司。不提供互联网的转接服务。拥有独立于互联网的专门网络，能与各级 ISP 和 IXP 相连。 |
| 16 | **NSFNET** | 美国国家科学基金网，互联网发展第二阶段的主干网，采用三级结构（主干网、地区网、校园网）。 |
| 17 | **WWW (World Wide Web)** | 万维网。由欧洲原子核研究组织 CERN 于 20 世纪 90 年代开发，成为互联网指数级增长的主要驱动力。 |
| 18 | **RFC (Request For Comments)** | 互联网标准的发表形式。"请求评论"的缩写。所有 RFC 文档可从互联网免费下载，但只有很少部分最终成为互联网标准。 |
| 19 | **IETF (Internet Engineering Task Force)** | 互联网工程部。负责研究每个领域中某一特定的短期、中期的工程问题，包括协议的开发和标准化。 |
| 20 | **IRTF (Internet Research Task Force)** | 互联网研究部。负责研究需要长期考虑的问题，包括协议、应用、体系结构等。 |
| 21 | **IAB (Internet Architecture Board)** | 互联网体系结构研究委员会。下辖 IETF 和 IRTF 两个工程部。 |
| 22 | **ISOC (Internet Society)** | 互联网协会。国际性组织，负责互联网标准的全面管理。 |
| 23 | **互联网标准 (Internet Standard)** | 经过完整的标准化过程（互联网草案→建议标准→草案标准→互联网标准）的 RFC 文档，记为 STDxx。 |
| 24 | **1983年** | 公认的互联网诞生时间。此年 TCP/IP 协议成为 ARPANET 上的标准协议。 |

### 第四层：互联网的组成

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 25 | **边缘部分** | 互联网中由所有连接在互联网上的**主机（端系统）** 组成的部分。由用户直接使用，用来进行通信（传送数据、音频或视频）和资源共享。 |
| 26 | **核心部分** | 互联网中由大量**网络**和连接这些网络的**路由器**组成的部分。为边缘部分提供服务（提供连通性和交换）。 |
| 27 | **端系统 (End System)** | 处在互联网边缘部分的连接在互联网上的主机。在功能上可能有很大差别（从小型个人电脑到大型服务器）。 |
| 28 | **客户/服务器方式 (C/S)** | 端系统之间的通信方式之一。客户 (Client) 是服务的**请求方**，服务器 (Server) 是服务的**提供方**。客户必须知道服务器地址，主动发起通信；服务器一直运行，被动等待请求。 |
| 29 | **对等方式 (P2P)** | 端系统之间的通信方式之一。两台主机通信时不区分服务方和请求方，进行平等的、对等的连接通信。本质上看仍是 C/S 方式——每个主机既是客户又是服务器。 |
| 30 | **路由器 (Router)** | 网络核心部分起特殊作用的设备，是实现**分组交换 (Packet Switching)** 的关键构件，其任务是**转发收到的分组**。 |
| 31 | **通信子网** | 从逻辑功能上划分，计算机网络中负责将各种计算机互连、完成数据交换和通信处理的部分。主要包括通信线路、网络连接设备、通信协议和通信控制软件。 |
| 32 | **资源子网** | 从逻辑功能上划分，计算机网络中负责全网面向应用的数据处理工作的部分。由各计算机系统、终端设备、软件和可共享的数据组成。 |

### 第五层：三种交换技术

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 33 | **电路交换 (Circuit Switching)** | 一种交换方式。通信前必须**建立连接**（占用通信资源），通信过程中**一直占用**端到端的物理通路，通信结束后**释放连接**（归还资源）。适合连续大量数据传输，但线路利用率低。电话网络采用此方式。 |
| 34 | **报文交换 (Message Switching)** | 一种交换方式。以**整个报文**为单位进行**存储转发**。无需建立连接，但每个节点需要缓存整个报文，时延较长（从几分钟到几小时）。现在已很少使用。 |
| 35 | **分组交换 (Packet Switching)** | 一种交换方式。在发送端将报文划分为较小的**分组 (Packet)**，每个分组添加**首部**后，独立在互联网中选择传输路径，通过**存储转发**机制传递到接收端，接收端剥去首部还原报文。互联网核心部分采用此方式。 |
| 36 | **分组 (Packet)** | 互联网中传送的数据单元。由数据段 + 首部构成。首部包含必要的控制信息（目的地址、源地址等）。 |
| 37 | **存储转发 (Store and Forward)** | 分组交换技术的核心机制：路由器收到分组后先暂存，检查首部，查找转发表，再转发到合适接口。 |
| 38 | **报文 (Message)** | 要发送的整块数据。在分组交换中会被划分为更小的数据段。 |
| 39 | **首部 (Header)** | 添加在数据段前面的控制信息，包含目的地址、源地址等重要信息。也称为"包头"。 |
| 40 | **转发表** | 路由器中的表格，记录了应将分组转发到的接口。路由器需要动态维护转发表。 |

### 第六层：计算机网络的类别

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 41 | **广域网 WAN (Wide Area Network)** | 作用范围通常为几十到几千公里的计算机网络，是互联网的核心部分。有时也称为远程网。 |
| 42 | **城域网 MAN (Metropolitan Area Network)** | 作用范围一般是一个城市，作用距离约 5~50 公里的计算机网络。 |
| 43 | **局域网 LAN (Local Area Network)** | 局限在较小范围（约 1 公里左右）的计算机网络，通常采用高速通信线路。 |
| 44 | **个人区域网 PAN (Personal Area Network)** | 范围很小（约 10 米左右）的计算机网络，有时也称无线个人区域网 WPAN。 |
| 45 | **多处理机系统** | 中央处理机之间距离非常近（如仅 1 米甚至更小）的系统，不称为计算机网络。 |
| 46 | **公用网 (Public Network)** | 按规定交纳费用的人都可以使用的网络，也可称为公众网。 |
| 47 | **专用网 (Private Network)** | 为特殊业务工作的需要而建造的网络。 |
| 48 | **接入网 AN (Access Network)** | 又称本地接入网或居民接入网。用于将用户接入互联网。是从某个用户端系统到本地 ISP 的第一个路由器（边缘路由器）之间的一种网络。既不是核心部分也不是边缘部分。 |

### 第七层：计算机网络的性能指标

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 49 | **速率 (Rate)** | 数据的传送速率，也称数据率 (Data Rate) 或比特率 (Bit Rate)。单位：bit/s（bps）。通常指**额定速率**（标称速率），非实际运行速率。 |
| 50 | **带宽 (Bandwidth)** | 两种含义本质相同：(1) **频域**：信号具有的频带宽度，单位 Hz；(2) **时域**（计算机领域）：单位时间内信道能通过的"最高数据率"，单位 bit/s。 |
| 51 | **吞吐量 (Throughput)** | 单位时间内通过某个网络（或信道、接口）的**实际数据量**。受带宽或额定速率的限制（绝对上限），实际值可能远小于额定速率。 |
| 52 | **时延 (Delay/Latency)** | 数据从网络（或链路）的一端传送到另一端所需的时间。总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延。 |
| 53 | **发送时延（传输时延）** | 主机或路由器**发送数据帧**所需的时间。从第一个比特发送开始到最后一个比特发送完毕。发生在机器**内部**的发送器中。公式：发送时延 = 数据帧长度 (bit) ÷ 发送速率 (bit/s)。 |
| 54 | **传播时延** | 电磁波在信道中**传播一定距离**所需的时间。发生在机器**外部**的传输信道媒体上。与信号的发送速率**无关**。公式：传播时延 = 信道长度 (m) ÷ 信号传播速率 (m/s)。 |
| 55 | **处理时延** | 主机或路由器收到分组后，为处理分组（分析首部、提取数据、差错检验或查找路由）所花费的时间。 |
| 56 | **排队时延** | 分组在路由器输入输出队列中排队等待处理和转发所经历的时延。长短取决于网络中当时的通信量。通信量很大时发生**队列溢出**使分组丢失，等价于排队时延为无穷大。 |
| 57 | **时延带宽积** | 传播时延 × 带宽。又称以比特为单位的链路长度。物理含义：链路管道中能容纳的比特数。 |
| 58 | **往返时间 RTT (Round-Trip Time)** | 从发送方发送完数据，到发送方收到来自接收方的确认总共经历的时间。约等于 2 × 传播时延 + 接收端处理/排队/发送时延。卫星通信时 RTT 较长（约 250~300 ms）。 |
| 59 | **信道利用率** | 某信道有百分之几的时间是被利用的（有数据通过）。完全空闲的信道利用率为零。利用率不是越高越好——当 U→1 时，时延 D→∞（D = D₀/(1-U)）。 |
| 60 | **网络利用率** | 全网络的信道利用率的加权平均值。 |

### 第八层：非性能特征

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 61 | **非性能特征** | 与性能指标密切相关、但不是直接反映网络性能的特征：费用、质量、标准化、可靠性、可扩展性和可升级性、管理和维护。 |

### 第九层：计算机网络体系结构

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 62 | **网络协议 (Network Protocol)** | 为进行网络中的数据交换而建立的**规则、标准或约定**。由三个要素组成：**语法**（数据与控制信息的结构或格式）、**语义**（需要发出何种控制信息、完成何种动作及做出何种响应）、**同步/时序**（事件实现顺序的详细说明）。 |
| 63 | **语法 (Syntax)** | 协议三要素之一：数据与控制信息的**结构或格式**。 |
| 64 | **语义 (Semantics)** | 协议三要素之一：需要发出何种控制信息，完成何种动作以及做出何种响应。 |
| 65 | **同步/时序 (Timing)** | 协议三要素之一：事件实现顺序的详细说明。 |
| 66 | **网络体系结构 (Network Architecture)** | 计算机网络的各层及其协议的集合。是这个计算机网络及其构件所应完成的功能的精确定义（不涉及实现）。是**抽象**的。 |
| 67 | **实现 (Implementation)** | 在遵循体系结构的前提下，用何种硬件或软件完成功能的问题。是**具体**的，是真正在运行的计算机硬件和软件。 |
| 68 | **分层 (Layering)** | 将庞大而复杂的问题转化为若干较小的局部问题逐个解决的设计方法。各层之间独立、灵活、可分割、易于实现和维护、能促进标准化。 |
| 69 | **OSI/RM (Open Systems Interconnection Reference Model)** | 开放系统互连基本参考模型。由 ISO 提出的七层协议体系结构（物理层→数据链路层→网络层→运输层→会话层→表示层→应用层）。是法律上的国际标准，但未获市场认可。 |
| 70 | **TCP/IP 体系结构** | 事实上的国际标准。四层协议体系结构：应用层、运输层、网际层 (IP)、网络接口层。获得了最广泛的应用。 |
| 71 | **五层协议体系结构** | 教学中采用的折中方案，综合了 OSI 七层的概念清晰和 TCP/IP 四层的实用性：物理层、数据链路层、网络层、运输层、应用层。 |

### 第十层：五层协议各层定义

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 72 | **应用层 (Application Layer)** | 第五层。通过应用进程间的交互来完成特定网络应用。数据单元：**报文 (Message)**。协议如 DNS、HTTP、SMTP。 |
| 73 | **运输层 (Transport Layer)** | 第四层。负责向两台主机中**进程之间**的通信提供通用的数据传输服务。具有复用和分用功能。两种协议：TCP（面向连接、可靠、报文段）和 UDP（无连接、尽最大努力、用户数据报）。 |
| 74 | **网络层 (Network Layer)** | 第三层。为分组交换网上的不同主机提供通信服务。两个任务：**路由选择**和**转发**。核心协议：IP（无连接网际协议）。数据单元：IP 数据报。也称网际层或 IP 层。 |
| 75 | **数据链路层 (Data Link Layer)** | 第二层。实现两个**相邻节点**之间的可靠通信。数据单元：**帧 (Frame)**。处理差错时简单丢弃出错帧。也称链路层。 |
| 76 | **物理层 (Physical Layer)** | 第一层。实现比特（0 或 1）的透明传输。确定与传输媒体接口的机械、电气、功能、过程特性。传输媒体本身不在物理层协议之内。 |

### 第十一层：体系结构核心概念

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 77 | **实体 (Entity)** | 任何可发送或接收信息的硬件或软件进程。 |
| 78 | **协议 (Protocol)** | 控制两个**对等实体**进行通信的规则的集合。是**水平**的。 |
| 79 | **服务 (Service)** | 在协议的控制下，两个对等实体间的通信使得本层能够向**上一层**提供服务。是**垂直**的。 |
| 80 | **对等层 (Peer Layer)** | 两台主机上相同编号的层次。对等层之间的通信遵循共同的协议。 |
| 81 | **协议数据单元 PDU (Protocol Data Unit)** | 对等层次之间传送的数据单位。 |
| 82 | **服务数据单元 SDU (Service Data Unit)** | 层与层之间交换的数据单位。SDU 可以与 PDU 不一样。 |
| 83 | **服务访问点 SAP (Service Access Point)** | 同一系统中相邻两层的实体进行交互（交换信息）的地方。是一个抽象概念，实际上是一个逻辑接口。 |
| 84 | **封装 (Encapsulation)** | 发送方每层为上层数据添加本层首部（或尾部）的过程。 |
| 85 | **解封装 (Decapsulation)** | 接收方每层去除本层首部（或尾部）、还原上层数据的过程。 |
| 86 | **沙漏计时器形状** | TCP/IP 协议族的形状比喻。中间收窄的是 IP（核心简单），上下宽（多种应用层协议和网络接口）。体现 IP over Everything 和 Everything over IP 的设计哲学。 |
| 87 | **IP over Everything** | IP 协议可以在多种类型的网络上运行。 |
| 88 | **Everything over IP** | IP 层可以支持多种的运输层协议和各种应用层协议。 |
| 89 | **差错控制** | 使相应层次对等方的通信更加可靠的功能。 |
| 90 | **流量控制** | 发送端的发送速率必须使接收端来得及接收，不要太快的功能。 |
| 91 | **分段和重装** | 发送端将要发送的数据块划分为更小的单位，在接收端将其还原的功能。 |
| 92 | **复用和分用** | 发送端几个高层会话复用一条低层的连接，在接收端再进行分用的功能。 |
| 93 | **连接建立和释放** | 交换数据前先建立一条逻辑连接，数据传送结束后释放连接的过程。 |

---

### 核心公式汇总（第1章）

| 编号 | 公式 | 说明 |
|------|------|------|
| F1 | **发送时延 = 数据帧长度 (bit) ÷ 发送速率 (bit/s)** | 发生在机器内部发送器中 |
| F2 | **传播时延 = 信道长度 (m) ÷ 信号传播速率 (m/s)** | 发生在机器外部传输媒体上 |
| F3 | **总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延** | 四部分时延之和 |
| F4 | **时延带宽积 = 传播时延 × 带宽** | 以比特为单位的链路长度 |
| F5 | **RTT ≈ 2 × 传播时延 + 接收端处理/排队/发送时延** | 往返时间 |
| F6 | **有效数据率 = 数据长度 ÷ (发送时延 + RTT)** | 考虑确认等待的实际数据率 |
| F7 | **D = D₀ / (1 - U)** | 时延与利用率的关系（排队论） |
| F8 | **速率单位换算**：1 Kbps = 10³ bps，1 Mbps = 10⁶ bps，1 Gbps = 10⁹ bps | 注意与存储容量 K=2¹⁰ 的区别 |
| F9 | **1 Byte = 8 bit** | 字节与比特的换算 |

---

### 重要辨析（第1章）

| 辨析 | 要点 |
|------|------|
| **Internet vs internet** | Internet（大写）：专指采用 TCP/IP 的全球互联网；internet（小写）：通用名词，泛指任何互连网络 |
| **互联网 vs 互连网** | 前者是特定网络（Internet），后者是网络的网络（internet/generic term） |
| **连通性 vs 资源共享** | 连通性是手段/前提，资源共享是目的/结果 |
| **电路交换 vs 分组交换** | 电路交换 = 独占资源、面向连接、适合连续大数据；分组交换 = 共享资源、存储转发、适合突发数据 |
| **报文交换 vs 分组交换** | 报文交换以整个报文为单位 → 时延大；分组交换以短分组为单位 → 时延小、灵活 |
| **发送时延 vs 传播时延** | 发送时延在机器内（取决于数据量和发送速率）；传播时延在媒体上（取决于距离和传播速率） |
| **速率 vs 吞吐量** | 速率是标称值/额定值；吞吐量是实际值 |
| **频域带宽 vs 时域带宽** | 频域带宽 = Hz（信号频带宽度）；时域带宽 = bit/s（最高数据率）|
| **OSI vs TCP/IP** | OSI 七层 = 法律标准但失败；TCP/IP 四层 = 事实标准，广泛应用 |
| **协议 vs 服务** | 协议 = 水平的（对等层之间）；服务 = 垂直的（上下层之间） |
| **PDU vs SDU** | PDU = 对等层之间；SDU = 相邻层之间 |
| **体系结构 vs 实现** | 体系结构 = 抽象的（功能定义）；实现 = 具体的（硬件/软件落地） |
| **C/S vs P2P** | C/S = 客户请求→服务器响应（主从关系）；P2P = 每个主机既是客户又是服务器（对等关系） |
| **通信子网 vs 资源子网** | 通信子网 = 通信线路+设备+协议（完成数据交换）；资源子网 = 计算机+终端+软件+数据（提供数据处理能力） |

---

> 📌 **核心思想**：计算机网络第1章建立了整个学科的概念框架——从"网络是什么"（定义）到"网络怎么组织"（组成和类别），再到"网络能多好"（性能），最后到"网络怎么做"（体系结构）。掌握这些基础概念，是理解后续各章具体协议与技术的**必要前提**。
