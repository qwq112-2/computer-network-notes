# 计算机网络 第5章 运输层 — 体系化讲解

> 运输层是计算机网络体系结构中承上启下的关键层。从第一性原理出发，涵盖全部小节及所有例题。每个概念均给出严格定义，末尾附完整定义与公式汇总。

---

## 目录

- [5.1 运输层协议概述](#51-运输层协议概述)
- [5.2 用户数据报协议 UDP](#52-用户数据报协议-udp)
- [5.3 传输控制协议 TCP 概述](#53-传输控制协议-tcp-概述)
- [5.4 TCP 报文段的首部格式](#54-tcp-报文段的首部格式)
- [5.5 TCP 可靠传输的实现](#55-tcp-可靠传输的实现)
- [5.6 TCP 的流量控制](#56-tcp-的流量控制)
- [5.7 TCP 的拥塞控制](#57-tcp-的拥塞控制)
- [5.8 TCP 的传输连接管理](#58-tcp-的传输连接管理)
- [附录：第5章完整定义与公式汇总](#附录第5章完整定义与公式汇总)

---

## 5.1 运输层协议概述

### 第一性原理：运输层要解决什么问题？

网络层把分组从源主机送到目的主机，但两台主机上的通信实体不是"主机"本身，而是运行在主机上的**应用进程**。比如你同时开着微信、浏览器和QQ——网络层只知道把数据送到你的电脑，但不知道应该交给哪个程序。

这就是运输层的核心任务：**为运行在不同主机上的应用进程之间提供逻辑通信**。它是"面向通信部分的最高层，用户功能中的最低层"。路由器只用到网络层，不用运输层——运输层只在端系统（主机）中实现。

> **定义——运输层 (Transport Layer)**：为应用进程之间提供**端到端**的逻辑通信服务。屏蔽网络核心细节，使应用进程觉得好像在两个运输层实体之间有一条直接的逻辑通信信道。

**运输层的屏蔽作用**：屏蔽下层网络核心的细节（拓扑、路由协议等），向上提供统一的逻辑通信信道。

| 提供的信道类型 | 协议 | 特点 |
|-------------|------|------|
| **全双工可靠信道** | TCP | 面向连接，可靠交付 |
| **不可靠信道** | UDP | 无连接，尽最大努力交付 |

---

### 5.1.1 进程之间的通信

网络层为主机之间提供通信，运输层为**进程之间**提供通信。

> **定义——运输层复用**：发送方不同的应用进程都可以使用同一个运输层协议传送数据。

> **定义——运输层分用**：接收方运输层剥去报文首部后，根据目的端口号将数据交付给正确的应用进程。

---

### 5.1.2 运输层的两个主要协议

> **定义——UDP (User Datagram Protocol，用户数据报协议)**：无连接的运输层协议。传送数据前不需建立连接，收到后不需确认。不提供可靠交付，但最有效。

> **定义——TCP (Transmission Control Protocol，传输控制协议)**：面向连接的运输层协议。提供可靠的、全双工的数据传输服务。不提供广播或多播。开销较大。

| 对比 | UDP | TCP |
|------|-----|-----|
| 连接 | 无连接 | 面向连接 |
| 可靠性 | 不可靠（尽最大努力） | 可靠（确认+重传） |
| 通信方式 | 一对一、一对多、多对一、多对多 | 仅一对一（点对点） |
| 传输单位 | 用户数据报 | 报文段 (segment) |
| 拥塞控制 | 无 | 有 |
| 开销 | 小（首部8字节） | 大（首部最小20字节） |
| 适用场景 | DNS、视频、VoIP | HTTP、SMTP、FTP |

**使用UDP和TCP的典型应用**：
- **UDP**：DNS(53)、DHCP(67/68)、RIP(520)、TFTP(69)、SNMP(161)、流媒体
- **TCP**：HTTP(80)、HTTPS(443)、SMTP(25)、FTP(21/20)、Telnet(23)

---

### 5.1.3 运输层的端口

> **定义——端口 (Port)**：运输层使用的**软件端口**，是应用层协议进程与运输实体进行层间交互的**抽象地址**。用一个**16位端口号**标志（0~65535）。

端口号只具有**本地意义**——不同计算机的相同端口号之间没有任何联系。

**两大类、三种端口**：

| 类型 | 范围 | 说明 |
|------|------|------|
| **熟知端口** | 0~1023 | 全球通用，IANA分配。如HTTP=80, DNS=53 |
| **登记端口** | 1024~49151 | 在IANA登记，分配给特定应用 |
| **短暂端口** | 49152~65535 | 客户端通信时动态使用，通信结束归还 |

**常用熟知端口速查**：

| 端口 | 协议 | 应用 |
|------|------|------|
| 21 | TCP | FTP(控制连接) |
| 20 | TCP | FTP(数据连接) |
| 23 | TCP | Telnet |
| 25 | TCP | SMTP |
| 53 | UDP | DNS |
| 69 | UDP | TFTP |
| 80 | TCP | HTTP |
| 110 | TCP | POP3 |
| 161 | UDP | SNMP |
| 162 | UDP | SNMP(trap) |
| 443 | TCP | HTTPS |
| 520 | UDP | RIP |

> 🚨 注意：端口号解决的是**同一主机**内不同进程的区分问题，不是不同主机间的识别（主机间用IP地址）。

---

### 📝 本节例题

**例1**：运输层为（　　）之间提供逻辑通信。A.主机 B.进程 C.路由器 D.操作系统
**解析**：运输层的逻辑通信实体是**进程**。网络层才是主机间。**答案：B**

**例2**：运输层的复用和分用通过（　　）实现。
**解析**：运输层通过**端口号**实现复用（多进程共用运输层发送）和分用（到目的后按端口分发）。**答案：端口号**

**例3**：OSI标准中能表现端到端传输的是（　　）。A.数据链路层 B.网络层 C.运输层 D.会话层
**解析**：端到端=源主机进程→目的主机进程，是运输层的职责。网络层是主机到主机(非端到端)，数据链路层是相邻节点间，会话层负责会话管理。**答案：C**

**例4**：欲传输一个短报文，TCP和UDP哪个更快？A.TCP B.UDP C.两个都快 D.不能比较
**解析**：UDP不需建立连接→直接发送→更快。TCP需三次握手+确认→开销大。**答案：B**

**例5**：TCP和UDP哪个效率高？A.TCP B.UDP C.两个一样 D.不能比较
**解析**：效率不能脱离场景比较。UDP没有连接建立和确认开销→传输效率高；TCP有可靠性保障→对需要可靠传输的应用效率更高(省去应用层重传逻辑)。**答案：D**

**例6**：UDP在IP数据报服务之上增加了（　　）功能和（　　）功能。
**解析**：UDP在IP之上增加的仅有**复用分用**(端口号)和**差错检测**(检验和)。没有序号、确认、拥塞控制等。**答案：复用分用；差错检测**

**例7**(判断)：UDP是一种可靠、高效的传输协议。
**解析**：UDP是**不可靠**的(尽最大努力交付)，但确实高效(无连接、开销小)。**答案：错误**

**例8**：下列哪个运输层协议提供低开销传输可用于不需可靠传输的应用？
A.TCP B.UDP C.ICMP D.IP
**解析**：UDP开销最小(首部仅8B)，适合DNS、流媒体等不需可靠传输的场景。**答案：B**

**例9**：小于（　　）的TCP/UDP端口号已保留与现有服务一一对应。
A.199 B.100 C.1024 D.2048
**解析**：0~1023是熟知端口(系统保留)。**答案：C(1024)**

**例10**(多选)：运输层上实现不可靠传输的协议是（　　）。A.TCP B.UDP C.IP D.ARP
**解析**：UDP是不可靠运输层协议。TCP是可靠的，IP在网络层，ARP在链路层。**答案：B**

**例11**：与OSI第四层(运输层)对应的主要协议有（　　）和（　　），后者提供无连接不可靠传输。
**解析**：TCP/IP的运输层有两个协议：**TCP**(面向连接可靠)和**UDP**(无连接不可靠)。**答案：TCP；UDP**

**例12**(判断)：端口号分为两大类：熟知端口号和系统端口号。
**解析**：错误。端口分为三大类：熟知(0~1023)、登记(1024~49151)、短暂(49152~65535)。**答案：错误**





---

## 5.2 用户数据报协议 UDP

### 5.2.1 UDP 概述

> **定义——UDP**：只在IP数据报服务之上增加了**复用分用**和**差错检测**功能。最简单的运输层协议。

**UDP 主要特点**：无连接、尽最大努力交付、**面向报文**(不合并不拆分)、无拥塞控制、支持一对多/多对多、首部仅8字节。

---

### 5.2.2 UDP 的首部格式

| 字段 | 位数 | 说明 |
|------|------|------|
| 源端口 | 16 | 不需回信时可为全0 |
| 目的端口 | 16 | 终点交付时必须使用 |
| 长度 | 16 | UDP总长度(首部+数据), 最小值=8 |
| 检验和 | 16 | 可选(IPv4), 不用填全0。校验范围：首部+数据 |

**UDP 检验和**：计算时临时添加**12字节伪首部**(仅计算用，不传输)：源IP(4B)+目的IP(4B)+0(1B)+17(1B,协议号)+UDP长度(2B)。

### 📝 本节例题

**例1**：关于UDP校验的描述错误的是（　　）。
A.UDP校验和可选,不计算时填全0  B.计算时生成的伪首部需要发送给接收方
C.UDP和TCP校验和计算相同  D.UDP校验首部+数据
**解析**：B错——伪首部仅用于计算，不实际传输。A对(IPv4可选项), C对(都加伪首部), D对。**答案：B**

**例2**：UDP首部十六进制: 09 57 00 35 00 20 BD A2。求各字段和该数据报信息。
**解析**：源端口=0x0957=2391(>1023->客户端); 目的端口=0x0035=53(DNS); 总长=0x0020=32B; 数据=32-8=24B。方向=客户->DNS服务器。服务=DNS(53)。

**例3**：用UDP传输时可靠性工作由（　　）承担。A.TCP B.IP C.ICMP D.应用层
**解析**：UDP本身不可靠，可靠性必须由**应用层**自己实现。**答案：D**

**例4**：运输层协议使用（　　）把数据传送给上层应用。A.IP地址 B.MAC地址 C.端口号 D.套接字地址
**解析**：运输层通过**端口号**识别不同应用进程进行分用。**答案：C**

**例5**：UDP报文首部不包括（　　）。A.目的端口 B.源端口 C.长度 D.目的地址
**解析**：UDP首部=源端口+目的端口+长度+检验和(4字段各2字节=8B)。不含任何IP地址。**答案：D**

---

## 5.3 传输控制协议 TCP 概述

### 5.3.1 TCP 最主要的特点

> **定义——TCP (Transmission Control Protocol)**：面向连接的运输层协议。在不可靠IP服务之上提供可靠交付。

| 特点 | 说明 |
|------|------|
| **面向连接** | 传送前必须先三次握手建立连接 |
| **点对点** | 每条连接仅两个端点，仅一对一 |
| **可靠交付** | 无差错、不丢失、不重复、按序 |
| **全双工** | 双方可同时收发 |
| **面向字节流** | 不保留报文边界 |

**TCP面向流**：应用进程写入发送缓存->TCP取数据组段->接收方存入接收缓存->应用进程读取。收到的字节流与发出的完全一致，但每个数据块大小可能不同。

### 5.3.2 TCP 的连接

> **定义——套接字 (Socket)**：TCP连接的端点。socket = (IP地址 : 端口号)，如(192.169.1.20:2028)。

> **定义——TCP连接**：由两个端点唯一确定。TCP连接 ::= {socket1, socket2}。

### 📝 本节例题

**例1**：下列说法中错误的是（　　）。
A.TCP可提供可靠数据流 B.TCP可提供全双工数据流 C.TCP可提供面向连接数据流 D.TCP可提供面向报文数据流
**解析**：TCP是面向**字节流**的，不是面向报文。UDP才是面向报文。**答案：D**

**例2**：TCP协议软件收到TCP报文段后通过（　　）识别应交给哪个应用进程。
**解析**：通过**目的端口号**。端口告诉TCP交给哪个进程。**答案：目的端口号**

**例3**：一个连接由两个端点来标识，端点叫（　　）或（　　）。
**解析**：TCP连接端点=套接字(socket)或插口。**答案：套接字(socket)；插口**

**例4**：（　　）是指运输层端点到端点的连接地址。
**解析**：运输层连接地址=套接字(socket)=IP地址+端口号。**答案：套接字(socket)**

**例5**：关于TCP和UDP的说法正确的是（　　）。
A.TCP无连接，UDP面向连接 B.都面向连接 C.TCP面向连接，UDP无连接 D.都无连接
**解析**：TCP面向连接(三次握手)，UDP无连接。**答案：C**

---

## 5.4 TCP 报文段的首部格式

> **定义——TCP 报文段**：TCP 传送的数据单元。分为首部和数据两部分，首部前20字节固定，后面有4n字节选项。首部最小长度20字节。

TCP 的全部功能都体现在首部各字段的作用中。

```
0        4        8       16      19      24      31
┌──────┬──────┬──────────────────────┐
│  源端口          │      目的端口        │
├─────────────────┴─────────────────────┤
│                序号 (32位)              │
├───────────────────────────────────────┤
│               确认号 (32位)             │
├──────┬──────┬──────┬────────────────┤
│数据偏移│ 保留  │控制位  │    窗口(16位)    │
│(4位) │(6位) │(6位)  │                │
├──────┴──────┴──────┼────────────────┤
│  检验和(16位)       │  紧急指针(16位)   │
├────────────────────┴────────────────┤
│      选项(长度可变, 最长40B)  │ 填充   │
└───────────────────────────────────────┘
        ← 前20字节固定首部 →
```

### 各字段详解

| 字段 | 位数 | 说明 |
|------|------|------|
| **源端口** | 16 | 发送方端口号 |
| **目的端口** | 16 | 接收方端口号。运输层与应用层的服务接口 |
| **序号** | 32 | 本报文段所发数据的**第一个字节**的序号。TCP对每个字节编号 |
| **确认号** | 32 | 期望收到对方**下一个**报文段第一个数据字节的序号。**确认号=N → 序号N-1及之前所有数据已正确收到** |
| **数据偏移** | 4 | TCP首部长度，以**4字节(32位字)**为单位。最小=5(20B)，最大=15(60B) |
| **保留** | 6 | 保留为今后使用，置0 |
| **URG** | 1 | 紧急位。URG=1时**紧急指针**有效，紧急数据尽快传送 |
| **ACK** | 1 | 确认位。**ACK=1时确认号字段才有效**。TCP连接建立后所有报文段ACK必须置1 |
| **PSH** | 1 | 推送位。接收方收到PSH=1时应**尽快交付**应用进程，不等缓存填满 |
| **RST** | 1 | 复位位。RST=1表明TCP连接出现严重差错→释放连接重新建立 |
| **SYN** | 1 | 同步位。SYN=1,ACK=0→连接请求；SYN=1,ACK=1→连接接受 |
| **FIN** | 1 | 终止位。FIN=1表明数据已发送完毕，要求**释放连接** |
| **窗口** | 16 | 从确认号算起，允许对方发送的数据量(字节)。**窗口值经常动态变化** |
| **检验和** | 16 | 校验首部+数据。计算时添加**12字节伪首部**(协议号=**6**) |
| **紧急指针** | 16 | 指出紧急数据末尾位置。**即使窗口为0也可发送紧急数据** |
| **选项** | 可变 | 最长40字节。如MSS、窗口扩大、时间戳、SACK |
| **填充** | 可变 | 使整个首部为4字节的整数倍 |

**序号示例**：5000字节数据，MSS=1000，初始序号=1001：
报文段1:序号=1001(字节1001~2000)、报文段2:序号=2001(2001~3000)、...报文段5:序号=5001。

> **定义——SYN报文段**：SYN=1的报文段。**不能携带数据，但消耗一个序号**。

> **定义——ACK报文段**：ACK=1的报文段。**可携带数据。若不携带数据则不消耗序号**。

> **定义——FIN报文段**：FIN=1的报文段。**即使不携带数据也消耗一个序号**。

### MSS 最大报文段长度

> **定义——MSS (Maximum Segment Size)**：TCP报文段**数据字段**的最大长度。MSS = 报文段长度 - TCP首部长度。**MSS与接收窗口值没有关系**。

- MSS太小→网络利用率降低(仅1字节数据时利用率≤1/41≈2.4%)
- MSS太大→IP层分片→终点装配开销大，分片传输出错需重传整个分组
- **默认值=536字节**(使IP数据报=576字节，任何主机都能接受)

### TCP 检验和

与UDP类似，计算时需添加**12字节伪首部**(仅计算不传输)。伪首部格式：源IP(4B)+目的IP(4B)+0(1B)+**6(TCP协议号, 1B)**+TCP总长度(2B)。注意与UDP的区别：UDP协议号=17，TCP协议号=**6**。

### 📝 本节例题

**例1**：关于TCP首部校验和的叙述正确的是（　　）。
A.需伪首部，仅对首部校验 B.需伪首部，对整个报文段校验 C.不需伪首部 D.不校验
**解析**：TCP检验和需12字节伪首部(协议号6)，校验范围=首部+数据(整个报文段，这点与IP仅校验首部不同)。**答案：B**

**例2**(多选)：TCP首部固定部分有哪些字段？
A.源端口 B.目的端口 C.序号 D.确认号 E.选项 F.填充
**解析**：前20字节固定部分含源端口、目的端口、序号、确认号、数据偏移、保留、控制位、窗口、检验和、紧急指针。选项和填充属于可变部分(4n字节)。**答案：A,B,C,D**

---

## 5.5 TCP 可靠传输的实现

> ⚠️ TCP滑动窗口以**字节**为单位（数据链路层以帧为单位）。这是关键区别。

### 5.5.1 以字节为单位的滑动窗口

> **定义——TCP发送窗口**：在未收到确认前可连续发送的数据量(字节)。由接收方通告的窗口值(rwnd)决定。

**三个关键点**：1)窗口以字节为单位 2)发送窗口与接收窗口同时刻不一定相等(时间滞后) 3)不按序到达的数据通常暂存。

### 📝 本节例题

**例1**：发送窗口由2000变3000字节，在确认前可发送（　　）。
A.3000报文段 B.1000报文段 C.3000字节 D.1000字节
**解析**：TCP窗口以字节为单位(不是报文段个数)。窗口值从2000变为3000→意味着可以发送**3000字节**的数据。A错在混淆了"报文段个数"和"字节数"——报文段大小由MSS决定，一个报文段可能包含多个字节。**答案：C**

**例2**：已收到序号1携带1000字节数据，回复报文确认号为（　　）。
A.1 B.1001 C.1000 D.0
**解析**：确认号=期望收到下一个字节的序号=1+1000=**1001**。确认号=N意味着N-1及之前全部正确收到。**答案：B**

---

### 5.5.2 超时重传时间的选择

> **定义——RTO (Retransmission Time-Out)**：超时重传时间。太短->不必要重传；太长->效率低。

**自适应算法**：

```
RTTS = (1-α)×旧RTTS + α×新RTT样本  (α=1/8=0.125)
RTTD = (1-β)×旧RTTD + β×|RTTS-新样本|  (β=1/4=0.25)
RTO = RTTS + 4×RTTD
```

> **定义——RTTS (平滑往返时间)**：加权平均往返时间。RTTS = (1-α)×旧RTTS + α×新RTT样本（α=1/8）。初始值=第一个RTT样本。衡量当前RTT估计值。

> **定义——RTTD (往返时间偏差)**：RTT偏差的加权平均值。RTTD = (1-β)×旧RTTD + β×|RTTS-新样本|（β=1/4）。初始值=首个RTT样本的一半。用于计算RTO。

**Karn算法**：重传报文段的RTT样本不参与RTTS计算。重传后RTO加倍直到收到非重传确认。

---

### 5.5.3 选择确认 SACK

> **定义——SACK (Selective ACK)**：接收方告知哪些字节块已正确收到，发送方只重传真正丢失的部分。累积确认号用法不变。

---

## 5.6 TCP 的流量控制

### 5.6.1 利用滑动窗口实现流量控制

> **定义——流量控制 (Flow Control)**：让发送方速率不要太快，接收方来得及收。利用滑动窗口+rwnd实现。

接收方在确认报文中通告**接收窗口rwnd**。发送方据此调整，不超过rwnd。

> **定义——持续计时器 (Persistence Timer)**：收到零窗口(rwnd=0)后启动。到期发**零窗口探测报文段**(仅1字节数据)，防死锁。

---

### 5.6.2 TCP 的传输效率

**控制发送时机的三种机制**：
1. MSS限制：缓存达到MSS->组装发送
2. Push操作：应用进程要求立即发送
3. 计时器：到期发送当前缓存

> **定义——糊涂窗口综合症**：每次仅发极少字节->效率极低(1B数据+40B头部)。

> **定义——Nagle算法**：发送方先发第一个数据字节，缓存后续；收到确认后将缓存全部数据组装发送；收到确认后再发下一批。当到达数据达到发送窗口一半或MSS时立即发送。防止发送方糊涂窗口综合症。

> **定义——累积确认 (Cumulative ACK)**：确认号=N表示序号N-1及之前所有数据已正确收到。减小传输开销，即使个别确认丢失也不必重传。

> **定义——捎带确认 (Piggybacking)**：接收方在自己发送数据时报文中捎带确认信息，减少单独ACK报文段数量。不应过分推迟确认，否则引起不必要超时重传。

**接收方解决方案**：等缓存有足够空间(≥1MSS或一半空闲)才发确认通告窗口。

### 📝 本节例题

**例1**：TCP使用的流量控制协议是（　　）。
A.固定大小滑动窗口 B.可变大小滑动窗口 C.后退N帧ARQ D.选择重发ARQ
**解析**：TCP接收方在确认报文中动态通告rwnd(接收窗口)，发送方据此调整发送窗口大小——窗口大小是**可变**的。A错在窗口值会随接收方缓存变化。C和D是数据链路层的ARQ协议，不是流量控制机制。**答案：B**

**例2**：流量控制是用来防止（　　）。
A.发送方发送太快导致网络拥塞 B.接收方来不及收 C.接收缓冲区溢出 D.以上都不对
**解析**：流量控制防止**接收方来不及收**(接收缓冲区溢出)。拥塞控制才是防止网络过载。注意：B和C是同一回事(来不及收=缓冲区溢出)，PPT答案选C。**答案：C**

---

## 5.7 TCP 的拥塞控制

### 5.7.1 拥塞控制的一般原理

> **定义——拥塞 (Congestion)**：在某段时间，对网络中某资源的需求超过了该资源所能提供的可用部分，网络性能明显变坏的现象。最坏结果：死锁（吞吐量降到0）。

> **定义——拥塞控制**：防止过多的数据注入到网络中，避免路由器或链路过载。是一个**全局性**过程（vs 流量控制是端到端的）。

**拥塞产生的四个原因**：
1. 节点缓存容量太小→到达的分组被丢弃
2. 链路容量不足→分组排队等待
3. 处理机处理速率太慢→路由器CPU跟不上到达速率
4. **拥塞本身会进一步加剧拥塞**——丢包→源点重传→更多分组注入→拥塞更严重（恶性循环）

> ⚠️ 单纯增加资源**不能**解决拥塞。增大缓存但不提高处理速率→大量超时重传→更拥塞。拥塞控制是动态问题，很难设计。**分组的丢失是网络发生拥塞的征兆，而不是原因**。

---

### 5.7.2 TCP 的拥塞控制方法

> **定义——拥塞窗口 cwnd**：发送方根据拥塞程度动态维持。发送窗口上限 = **Min[rwnd, cwnd]**（取两者小值）。当rwnd < cwnd时→接收方接收能力限制（流量控制）；当cwnd < rwnd时→网络拥塞程度限制（拥塞控制）。

**判断拥塞**：1)超时->已拥塞 2)收到**3个重复ACK**->可能拥塞(快重传)。

#### 四种拥塞控制算法

**1. 慢开始**：cwnd从1起，每收到新确认cwnd+=1->指数增长(1,2,4,8,16...)

**2. 拥塞避免**：cwnd>=ssthresh后，每RTT cwnd+=1->线性增长

**3. 快重传**：收到**3个重复ACK**->立即重传缺失报文段，不等超时。**可使全网吞吐量提高约20%**。要求接收方即使收到失序报文段也立即发送重复确认。并非取消重传计时器，而是更早（更快）重传。

**4. 快恢复**：3-ACK后不降到cwnd=1，而是ssthresh=cwnd/2, cwnd=ssthresh->直接拥塞避免。ssthresh减半=**乘法减小MD (Multiplicative Decrease)**；拥塞避免线性增长=**加法增大AI (Additive Increase)**。MD+AI合称**AIMD算法**，使TCP性能有明显改进。

| 触发事件 | ssthresh | cwnd | 下一步 |
|---------|----------|------|--------|
| **超时** | max(cwnd/2, 2) | 1 | 慢开始 |
| **3-ACK** | cwnd/2 | ssthresh(即cwnd/2) | 拥塞避免 |

> **定义——传输轮次**：把cwnd允许的报文段全部发出并收到最后确认的时间->约等于RTT。

> **定义——慢开始门限 ssthresh**：cwnd<ssthresh->慢开始；cwnd>ssthresh->拥塞避免。

#### 经典例题（初始cwnd=1, ssthresh=16, 单位=MSS）

| 轮次 | cwnd变化 | 算法 | 说明 |
|------|---------|------|------|
| 1~4 | 1,2,4,8,16 | 慢开始 | 指数增长到ssthresh |
| 5~12 | 17,18,...,24 | 拥塞避免 | 线性增长，每RTT+1 |
| 第12轮后 | cwnd=24时**超时** | — | ssthresh=12, cwnd=1 |
| 13~16 | 1,2,4,8,12 | 慢开始(重新) | — |
| 17~20 | 13,14,15,16 | 拥塞避免 | — |
| 第20轮后 | cwnd=16时**3-ACK** | 快恢复 | ssthresh=8, cwnd=8 |

### 📝 本节例题

**例1**：MSS=1KB, cwnd=34KB时超时。接下来4个RTT都成功且均确认后cwnd大小（　　）。
A.8KB B.9KB C.16KB D.17KB
**解析**：超时->ssthresh=max(34/2,2)=17, cwnd=1->慢开始。RTT1:1->2, RTT2:2->4, RTT3:4->8, RTT4:8->16。4RTT后cwnd=**16KB**(仍未超过ssthresh，仍在慢开始)。**答案：C**

**例2**：同上但收到3个冗余ACK(非超时)。4RTT后cwnd（　　）。
A.8KB B.16KB C.21KB D.25KB
**解析**：3-ACK->快恢复: ssthresh=17, cwnd=17->拥塞避免。RTT1:17->18, RTT2:18->19, RTT3:19->20, RTT4:20->21。**答案：C(21KB)**

**例3**：TCP用什么机制提供流量控制（　　）。A.序列号 B.会话创建 C.窗口大小 D.确认
**解析**：接收方通过**窗口大小**字段通告可接收数据量->实现流量控制。**答案：C**

**例4**：甲以MSS=1KB发数据，乙确认窗口10KB。t时刻超时cwnd=8KB，此后不再超时，10RTT后发送窗口=?
**解析**：超时->ssthresh=max(8/2,2)=4, cwnd=1。慢开始:RTT1:1->2, RTT2:2->4(达ssthresh)。拥塞避免:RTT3:4->5,..., RTT10:4+8=12。cwnd=12KB。但发送窗口=Min[rwnd,cwnd]=Min[10,12]=**10KB**(rwnd限制)。**答案：10KB**

---

## 5.8 TCP 的传输连接管理

### 5.8.1 TCP 的连接建立——三报文握手

**TCP连接建立要解决的三个问题**：
1. 使每一方能够确知对方的存在
2. 允许双方协商一些参数（最大窗口值、是否使用窗口扩大选项和时间戳选项、服务质量等）
3. 能够对运输实体资源（缓存大小、连接表中的项目等）进行分配

TCP连接的建立采用**客户-服务器方式**：主动发起连接请求的应用进程=客户，被动等待连接请求的应用进程=服务器。

> **定义——三报文握手**：SYN(seq=x)->SYN+ACK(seq=y,ack=x+1)->ACK(seq=x+1,ack=y+1)。三次握手防止已失效连接请求。

### 5.8.2 TCP 的连接释放——四报文握手

> **定义——四报文握手**：FIN->ACK->FIN->ACK。全双工连接每方向单独关闭。最后A等待**2MSL**后关闭。

> **定义——MSL (Maximum Segment Lifetime)**：最长报文段寿命。RFC 793建议=2分钟。

**等待2MSL的两个原因**：1)保证最后一个ACK能到达B 2)防止旧连接请求出现在新连接中。

### 5.8.3 TCP 的状态机

TCP连接从建立到释放经历11种状态：

| 状态 | 说明 | 触发条件 |
|------|------|---------|
| **CLOSED** | 关闭 | 初始/最终状态 |
| **LISTEN** | 监听 | 服务器被动打开，等待连接请求 |
| **SYN-SENT** | 同步已发送 | 客户发出SYN后进入 |
| **SYN-RCVD** | 同步收到 | 服务器收到SYN、发出SYN+ACK后 |
| **ESTABLISHED** | 已建立连接 | 双方完成三次握手，可传输数据 |
| **FIN-WAIT-1** | 终止等待1 | 主动关闭方发出FIN后 |
| **FIN-WAIT-2** | 终止等待2 | 主动关闭方收到ACK(对FIN的确认)后 |
| **CLOSE-WAIT** | 关闭等待 | 被动关闭方收到FIN、发出ACK后 |
| **LAST-ACK** | 最后确认 | 被动关闭方发出FIN后 |
| **TIME-WAIT** | 时间等待 | 主动关闭方收到FIN、发出最后ACK后，等待**2MSL** |
| **CLOSING** | 同时关闭 | 双方同时发出FIN（较少见） |

关键状态转换：客户CLOSED->SYN-SENT->(收到SYN+ACK)->ESTABLISHED->(主动关闭)->FIN-WAIT-1->(收到ACK)->FIN-WAIT-2->(收到FIN)->TIME-WAIT(等2MSL)->CLOSED。服务器CLOSED->LISTEN->(收到SYN)->SYN-RCVD->(收到ACK)->ESTABLISHED->(收到FIN)->CLOSE-WAIT->(发FIN)->LAST-ACK->(收到ACK)->CLOSED。

### 5.8.4 保活计时器

> **定义——保活计时器**：防长时间空闲。默认**2小时**。超时后每75秒发探测，连续10次无响应->终止。

### 📝 本节例题

**例1**：TCP主动方发出SYN连接请求后，等待对方回答的是（　　）。
A.SYN,ACK B.FIN,ACK C.PSH,ACK D.RST,ACK
**解析**：三报文握手的第二步：服务器收到SYN后回复**SYN+ACK**(SYN=1,ACK=1)，既确认客户请求又发出自己的同步请求。B的FIN用于连接释放(第四步)。C的PSH是推送位。D的RST是复位位(拒绝连接)。**答案：A**

---

## 附录：第5章完整定义与公式汇总

### 核心定义(30条)

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 1 | **运输层** | 为应用进程间提供端到端逻辑通信服务。 |
| 2 | **UDP** | 无连接、不可靠、面向报文。首部8字节。 |
| 3 | **TCP** | 面向连接、可靠、面向字节流、全双工。 |
| 4 | **端口** | 运输层抽象地址。16位(0~65535)。 |
| 5 | **复用分用** | 复用=多进程共用运输层发送；分用=按端口分发。 |
| 6 | **套接字** | TCP端点。socket=(IP:Port)。 |
| 7 | **TCP连接** | {socket1, socket2}。 |
| 8 | **发送窗口** | 未确认前可连续发送的字节数。 |
| 9 | **RTT** | 往返时间。 |
| 10 | **RTTS** | 平滑往返时间。加权平均。 |
| 11 | **RTTD** | RTT偏差加权平均。 |
| 12 | **RTO** | 超时重传时间=RTTs+4×RTTD。 |
| 13 | **SACK** | 选择确认。只重传缺失部分。 |
| 14 | **MSS** | 最大报文段长度。 |
| 15 | **流量控制** | 防发送太快，rwnd通告实现。 |
| 16 | **持续计时器** | 零窗口后发探测防死锁。 |
| 17 | **糊涂窗口综合症** | 每次发极少字节->效率极低。Nagle算法解决。 |
| 18 | **拥塞** | 需求>可用资源->性能恶化。 |
| 19 | **拥塞控制** | 防过多数据注入网络。全局性。 |
| 20 | **cwnd** | 拥塞窗口。发送方根据拥塞动态维持。 |
| 21 | **慢开始** | cwnd从1指数增长。 |
| 22 | **ssthresh** | 慢开始与拥塞避免的分界线。 |
| 23 | **拥塞避免** | cwnd每RTT线性+1。 |
| 24 | **快重传** | 3重复ACK->立即重传。 |
| 25 | **快恢复** | 3-ACK后cwnd=ssthresh->直接拥塞避免。 |
| 26 | **传输轮次** | 发完cwnd允许段+收到最后确认->约等于RTT。 |
| 27 | **三报文握手** | SYN->SYN+ACK->ACK。建立连接。 |
| 28 | **四报文握手** | FIN->ACK->FIN->ACK。释放+2MSL等待。 |
| 29 | **MSL** | 最长报文段寿命->约2分钟。 |
| 30 | **保活计时器** | 2小时无数据->探测->10次无响应终止。 |
| 31 | **伪首部** | 计算UDP/TCP检验和时临时12字节。不传输。 |
| 32 | **Nagle算法** | 首字节后缓存，等确认后批量发送。 |

### 核心公式

| 编号 | 公式 | 说明 |
|------|------|------|
| F1 | 发送窗口上限 = Min[rwnd, cwnd] | 取小值 |
| F2 | RTO = RTTS + 4×RTTD | 超时重传 |
| F3 | 慢开始: 每确认cwnd+=1 | 指数增长 |
| F4 | 拥塞避免: 每RTT cwnd+=1 | 线性增长 |
| F5 | 超时: ssthresh=max(cwnd/2,2), cwnd=1 | 重新慢开始 |
| F6 | 3-ACK: ssthresh=cwnd/2, cwnd=ssthresh | 快恢复->拥塞避免 |

### 重要辨析

| 辨析 | 要点 |
|------|------|
| **UDP vs TCP** | UDP=无连接+不可靠+面向报文(8B)；TCP=面向连接+可靠+面向字节流(20B+) |
| **流量控制 vs 拥塞控制** | 流量=端到端(rwnd)；拥塞=全局(cwnd) |
| **慢开始 vs 拥塞避免** | 慢开始=指数(每ACK+1)；拥塞避免=线性(每RTT+1) |
| **超时 vs 3-ACK** | 超时->cwnd=1慢开始；3-ACK->快恢复->拥塞避免 |
| **三报文握手 vs 四报文握手** | 三次=建连接(SYN->SYN-ACK->ACK)；四次=释放(FIN->ACK->FIN->ACK)+2MSL |
| **面向报文 vs 面向字节流** | UDP=保留报文边界；TCP=字节流无边界 |
