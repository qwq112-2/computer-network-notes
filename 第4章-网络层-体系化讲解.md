# 计算机网络 第4章 网络层 — 体系化讲解

> 从第一性原理出发，逐层构建知识体系，涵盖全部9个大节及所有例题。每个概念均给出严格定义，末尾附完整定义与公式汇总。

---

## 目录

- [4.1 网络层的几个重要概念](#41-网络层的几个重要概念)
- [4.2 网际协议 IP](#42-网际协议-ip)
- [4.3 IP层转发分组的过程](#43-ip层转发分组的过程)
- [4.4 网际控制报文协议 ICMP](#44-网际控制报文协议-icmp)
- [4.5 IPv6](#45-ipv6)
- [4.6 互联网的路由选择协议](#46-互联网的路由选择协议)
- [4.7 IP 多播](#47-ip-多播)
- [4.8 虚拟专用网 VPN 和网络地址转换 NAT](#48-虚拟专用网-vpn-和网络地址转换-nat)
- [4.9 多协议标签交换 MPLS](#49-多协议标签交换-mpls)
- [附录：第4章完整定义与公式汇总](#附录第4章完整定义与公式汇总)

---

## 4.1 网络层的几个重要概念

### 第一性原理：网络层要解决什么问题？

数据链路层解决了**相邻节点**之间的通信。但互联网是全球范围的——两台主机可能隔着几十个路由器、穿越多种异构网络（以太网、WiFi、光纤、卫星……）。网络层的核心任务：**在任意两台主机之间找到一条路径，把分组从源端送到目的端。**

为此需要解决两个问题：(1) 网络应该提供怎样的服务？（虚电路还是数据报？）(2) 如何组织"转发"和"路由选择"这两个功能？

---

### 4.1.1 网络层提供的两种服务

#### 争论核心：可靠交付由谁负责？

| | 虚电路服务 | 数据报服务 |
|------|------|------|
| **设计思路** | 可靠通信由**网络**来保证 | 可靠通信由**用户主机**来保证 |
| **连接建立** | 必须有 | 不需要 |
| **终点地址** | 仅在连接建立阶段使用，通信中用虚电路号 | 每个分组携带完整终点地址 |
| **分组转发路径** | 同一条虚电路，所有分组走**相同路径** | 每个分组**独立选择**路径 |
| **节点故障** | 所有经过的虚电路均不能工作 | 出故障节点可能丢失分组，路由变化 |
| **分组顺序** | 总是按序到达 | 不保证按序 |
| **端到端差错处理** | 由网络负责 | 由主机负责 |

> **定义——虚电路 VC (Virtual Circuit)**：面向连接的通信方式。通信前先建立一条逻辑连接，通信双方所有分组沿**同一条虚电路**传送。分组按序到达。

> **定义——数据报服务 (Datagram Service)**：无连接通信方式。网络层向上只提供简单灵活的、尽最大努力交付的数据报服务。每个分组**独立发送**，与其前后分组无关。

> 🎯 **互联网采用数据报服务**。网络层设计得尽量简单，复杂留给端系统。这使得网络造价降低、灵活性强。

### 📝 本节例题

> 有关虚电路服务和数据报服务的特性，正确的是（　　）。
> A.虚电路和数据报服务都是无连接的服务
> B.数据报服务中，分组沿同一条路径传输且按顺序到达
> C.虚电路在建立连接后，分组只需携带虚电路号

**答案：C**。虚电路建立后使用短的虚电路号，不需完整地址。

---

### 4.1.2 网络层的两个层面

> **定义——数据层面 (Data Plane)**：路由器根据转发表把收到的分组从查找到的对应接口转发出去。独立工作，采用**硬件**转发，快。

> **定义——控制层面 (Control Plane)**：根据路由选择协议计算路由，创建出路由表。许多路由器协同工作，采用**软件**计算。

| | 数据层面 | 控制层面 |
|------|------|------|
| 功能 | 分组转发 | 路由选择 |
| 工作方式 | 独立、逐跳 | 协同、全局 |
| 实现 | 硬件（快） | 软件（灵活） |
| 依据 | 转发表 | 路由表→转发表 |

> **定义——SDN (Software Defined Network)**：软件定义网络。将控制层面从路由器中分离出来，由**远程控制器**统一计算路由并下发转发表，路由器只负责转发分组。

---

## 4.2 网际协议 IP

网际协议 IP 是 TCP/IP 体系中最核心的协议之一。配套三个协议：
- **ARP**：地址解析协议（IP→MAC）
- **ICMP**：网际控制报文协议（差错报告）
- **IGMP**：网际组管理协议（多播）

### 4.2.1 虚拟互连网络

> **定义——虚拟互连网络（IP网）**：使用 IP 协议将异构的物理网络互连起来，在网络层看起来就像一个统一的网络。屏蔽了下层异构细节。

**各层中间设备**：
| 层次 | 设备 |
|------|------|
| 物理层 | 转发器/中继器、集线器 |
| 数据链路层 | 网桥、交换机 |
| **网络层** | **路由器** |
| 运输层及以上 | 网关 |

> ⚠️ 转发器/集线器/网桥/交换机仅把网络扩大了，仍是一个网络。**真正的网络互连使用路由器。**

---

### 4.2.2 IP 地址

> **定义——IP地址**：TCP/IP体系中，给每个连接在互联网上的主机（或路由器）的每一个接口分配的唯一32位标识符。采用**点分十进制记法**：每8位一组，共4组。

```
IP地址 ::= { <网络号(网络前缀)>, <主机号> }
```

#### 分类的 IP 地址

| 类别 | 前缀 | 网络号位数 | 首个网络号 | 末个网络号 | 每网络最大主机数 |
|------|------|------|------|------|------|
| **A类** | 0 | 8 | 1 | 126 | 16777214 (2²⁴-2) |
| **B类** | 10 | 16 | 128.0 | 191.255 | 65534 (2¹⁶-2) |
| **C类** | 110 | 24 | 192.0.0 | 223.255.255 | 254 (2⁸-2) |
| **D类** | 1110 | — | 224.0.0.0 | 239.255.255.255 | 多播地址 |
| **E类** | 1111 | — | 240.0.0.0 | 255.255.255.255 | 保留 |

> **定义——CIDR (Classless Inter-Domain Routing，无分类域间路由)**：消除了传统 A/B/C 类地址划分。IP地址用网络前缀+主机号表示，记作 `地址/前缀位数`。

```
CIDR: IP地址 ::= { <网络前缀>, <主机号> }
```

> **定义——地址掩码（子网掩码）**：32位，一串1后接一串0。1的个数=网络前缀位数。`网络地址 = IP地址 AND 地址掩码`。

#### 路由聚合 (Route Aggregation)

> **定义——路由聚合（构造超网）**：将多个连续的子网合并为一个更大的CIDR地址块，减少路由表条目。每个CIDR地址块中的地址数一定是2的整数次幂。

#### 特殊IP地址

| 网络号 | 主机号 | 用途 |
|--------|--------|------|
| 全0 | 全0 | 本网络上的本主机（DHCP） |
| 全0 | X | 本网络上主机号为X的主机 |
| 全1 | 全1 | 本网络广播（受限广播） |
| 网络号 | 全1 | 向该网络广播 |
| 127 | 任意 | 环回测试 |

### 📝 本节例题（IP地址）

> C类子网掩码255.255.255.224，包含的子网位数、子网数目、每个子网主机数目正确的是？
> 224=11100000→子网位=3，子网数=2³=8，主机位=5，主机数=2⁵-2=30
> **答案：3, 8, 30**

---

### 4.2.3 IP 地址与 MAC 地址

| 维度 | IP地址 | MAC地址 |
|------|--------|---------|
| **性质** | 逻辑地址（软件地址） | 物理地址（硬件地址） |
| **使用层次** | 网络层及以上 | 数据链路层 |
| **位置** | IP数据报首部 | MAC帧首部 |
| **长度** | 32位(IPv4) | 48位 |
| **跨网络** | 源到终点不变 | 每跳都变（源MAC+目的MAC） |

**不同区间的地址变化规律**（H1→R1→R2→H2）：

| 区间 | IP源 | IP目的 | MAC源 | MAC目的 |
|------|------|--------|-------|--------|
| H1→R1 | IP1 | IP2 | MAC1 | MAC3 |
| R1→R2 | IP1 | IP2 | MAC4 | MAC5 |
| R2→H2 | IP1 | IP2 | MAC6 | MAC2 |

> 🎯 **IP地址在端到端不变（逻辑寻址），MAC地址每跳都变（物理寻址）。**

---

### 4.2.4 地址解析协议 ARP

> **定义——ARP (Address Resolution Protocol)**：解决"已知目标IP地址，求对应的MAC地址"的问题。在主机ARP高速缓存中查找，未命中则广播ARP请求。

**ARP工作流程**：
1. 查本机ARP高速缓存
2. 命中→直接用；未命中→本局域网**广播**ARP请求
3. 目标主机**单播**ARP响应
4. 存入ARP高速缓存（有过期时间）

> **定义——ARP高速缓存 (ARP Cache)**：存放最近获得的IP→MAC映射表，动态更新（新增/超时删除），减少ARP广播。

**使用ARP的四种典型情况**：
| 发送方 | 目标 | ARP找到谁的MAC |
|------|------|---------|
| 主机 | 同网主机 | 目的主机的MAC |
| 主机 | 跨网主机 | **本网路由器**的MAC |
| 路由器 | 本网主机 | 目的主机的MAC |
| 路由器 | 跨网转发 | **下一跳路由器**的MAC |

---

### 4.2.5 IP 数据报的格式

```
0        4        8       16      19      24      31
┌──────┬──────┬────────┬──────────────────────┐
│ 版本  │首部长度│区分服务│       总长度          │
├──────┴──────┼────────┴─────────┬────────────┤
│     标识     │    标志    │     片偏移        │
├────────────┼─────────────┼──────────────────┤
│  生存时间   │    协议     │     首部检验和      │
├────────────┴─────────────┴──────────────────┤
│                  源地址（32位）                │
├──────────────────────────────────────────────┤
│                 目的地址（32位）               │
├──────────────────────────────────────────────┤
│         可选字段（长度可变）      │    填充     │
└──────────────────────────────────────────────┘
```

**各字段详解**：

| 字段 | 位数 | 含义 |
|------|------|------|
| **版本** | 4 | IPv4=4, IPv6=6 |
| **首部长度** | 4 | 单位4字节，最小值5(20B)，最大值15(60B) |
| **区分服务** | 8 | QoS |
| **总长度** | 16 | 首部+数据总长，单位字节，最大65535B |
| **标识** | 16 | 分片后同一数据报的所有片标识相同 |
| **标志** | 3 | DF(不分片)、MF(还有分片) |
| **片偏移** | 13 | 单位8字节，该片在原数据报中的相对位置 |
| **生存时间 TTL** | 8 | 每跳减1，到0丢弃（防兜圈子） |
| **协议** | 8 | 上层协议：ICMP=1, TCP=6, UDP=17 |
| **首部检验和** | 16 | 只检验首部（反码算术运算求和取反） |
| **源地址** | 32 | 发送方IP |
| **目的地址** | 32 | 接收方IP |

> **定义——TTL (Time To Live)**：IP数据报的生存时间。每经过一个路由器减1，减到0时丢弃。防止分组在网络中无限兜圈子。

---

## 4.3 IP层转发分组的过程

### 4.3.1 基于终点的转发

分组在互联网中是**逐跳转发**。每跳路由器根据分组的目的地址查找转发表，从对应接口转发出去。

### 4.3.2 最长前缀匹配

> **定义——最长前缀匹配 (Longest Prefix Match)**：当转发表中有多条表项与目的IP匹配时，选择**网络前缀最长**的一项。前缀越长→地址块越小→越精确。

### 转发表中的特殊路由

> **定义——主机路由**：对特定主机IP设置的/32路由，放在转发表最前面。

> **定义——默认路由**：当找不到任何匹配前缀时使用的路由(0.0.0.0/0)。出网只有一个方向时配置。

---

## 4.4 网际控制报文协议 ICMP

> **定义——ICMP (Internet Control Message Protocol)**：允许主机或路由器报告差错情况和提供异常情况报告。是IP层协议，报文封装在IP数据报中。

### ICMP报文种类

| 类型 | 报文 | 类型值 |
|------|------|--------|
| **差错报告** | 终点不可达 | 3 |
| | 时间超过(TTL=0) | 11 |
| | 参数问题 | 12 |
| | 改变路由(Redirect) | 5 |
| **询问报文** | 回送请求/回答 | 8/0 |
| | 时间戳请求/回答 | 13/14 |

### ICMP应用

- **Ping**：测试连通性。使用回送请求(8)和回送回答(0)。**应用层直接使用网络层ICMP**，不经过运输层。
- **Traceroute (tracert)**：跟踪分组路径。利用TTL逐跳增加+ICMP时间超过报文。

---

## 4.5 IPv6

> **定义——IPv6**：下一代IP协议。地址从32位扩展到**128位**。首部固定**40字节**，取消了许多IPv4字段。

### IPv6主要变化

1. 地址空间：32位→128位（约3.4×10³⁸个）
2. 首部固定40字节，8个字段
3. 取消检验和（由链路层和上层负责）
4. 支持即插即用（自动配置）
5. IPsec内置支持

### IPv6地址表示

- **冒号十六进制记法**：16位一组，共8组。如 `12AB:0000:0000:CD30:0000:0000:0000:0000`
- **零压缩**：连续零可用`::`取代（**只能用一次**）
- 上例压缩为 `12AB:0:0:CD30::` 或 `12AB::CD30:0:0:0:0`

### IPv6地址类型

| 类型 | 前缀 | 说明 |
|------|------|------|
| 未指明地址 | ::/128 | 全0，只能做源地址 |
| 环回地址 | ::1/128 | 本地回环 |
| 多播地址 | FF00::/8 | 一对多 |
| 本地链路单播 | FE80::/10 | 同一链路内 |

### 从IPv4向IPv6过渡

1. **双协议栈**：主机/路由器同时运行IPv4和IPv6
2. **隧道技术**：将IPv6数据报封装在IPv4数据报中（协议字段=41）

---

## 4.6 互联网的路由选择协议

### 4.6.1 基本概念

> **定义——自治系统 AS (Autonomous System)**：在单一技术管理下的网络、IP地址和路由器的集合。AS内部使用统一的路由选择协议。

> **定义——IGP (Interior Gateway Protocol，内部网关协议)**：自治系统**内部**使用的路由选择协议。如 RIP, OSPF。

> **定义——EGP (External Gateway Protocol，外部网关协议)**：自治系统**之间**使用的路由选择协议。如 BGP-4。

### 4.6.2 RIP

> **定义——RIP (Routing Information Protocol)**：分布式、基于**距离向量**的IGP。距离=跳数(hop count)，直接连接=1，最大15跳(16=不可达)。

**三个特点**：
1. 仅和**相邻路由器**交换信息
2. 交换的是**完整路由表**
3. 按固定时间间隔（30秒）交换

**距离向量算法**：
```
收到邻居X发来的路由表 →
1. 所有项目的下一跳改为X，距离+1
2. 对每个项目：
   - 路由表中无目的网络N → 加入
   - 路由表中有N且下一跳=X → 更新
   - 路由表中有N且下一跳≠X → 比较距离，新距离<旧距离则更新
```

> ⚠️ **坏消息传播慢**：故障→距离逐步增加到16才知道不可达（慢收敛）。解决方法：水平分割、毒性逆转。

### 4.6.3 OSPF

> **定义——OSPF (Open Shortest Path First)**：基于**链路状态**的IGP，使用Dijkstra最短路径算法。克服了RIP的缺点。

**三个特点**：
1. 向**本AS所有路由器**发信息（洪泛法）
2. 发送的是**链路状态**（与相邻路由器的链路信息）
3. 链路状态变化时才发送（触发更新），定期(30分钟)刷新

**OSPF区域划分**：
| 区域类型 | 说明 |
|---------|------|
| 主干区域 | 0.0.0.0，所有区域必须连接到主干 |
| 普通区域 | 通过区域边界路由器ABR连接到主干 |

**五种分组**：Hello(问候)、Database Description(数据库描述)、Link State Request、Link State Update、Link State Acknowledgment。

### 4.6.4 BGP

> **定义——BGP (Border Gateway Protocol)**：不同自治系统间交换路由信息的EGP。使用**路径向量**算法。力求选择**可达且无环路**的路由，而非最优。

**BGP路由 = [前缀, AS-PATH, NEXT-HOP]**。通过TCP(179端口)建立BGP会话。

### 4.6.5 路由器的构成

```
┌──────────────────────┐
│  路由选择处理机(控制层面)│
│  路由协议 │ 路由表      │
└──────────────────────┘
        ↕
┌────┬────┬────┬────┐
│输入 │输入 │输出 │输出 │
│端口 │端口 │端口 │端口 │  ← 数据层面(硬件)
└────┴────┴────┴────┘
      交换结构
```

**三种交换结构**：
| 方式 | 特点 |
|------|------|
| 通过存储器 | 传统，慢（两次内存复制） |
| 通过总线 | 共享总线，一次一个分组 |
| 通过纵横交换结构 | 并行，N×N交叉矩阵，快 |

---

## 4.7 IP 多播

> **定义——IP多播 (IP Multicast)**：一对多通信。源点只发一次，路由器在分叉处复制。使用D类IP地址(224.0.0.0~239.255.255.255)。

> **定义——IGMP (Internet Group Management Protocol)**：使多播路由器知道本地网络上多播组成员信息。

**多播MAC地址**：IANA以太网地址块高24位=00-00-5E。D类IP地址的低23位映射到多播MAC地址的低23位。

---

## 4.8 虚拟专用网 VPN 和 NAT

### 4.8.1 VPN

> **定义——专用地址 (Private Address)**：仅在机构内部使用，不需向互联网申请。RFC 1918规定三个块：
> - 10.0.0.0/8 (A类)
> - 172.16.0.0/12 (B类)
> - 192.168.0.0/16 (C类)

> **定义——VPN (Virtual Private Network)**：利用公用互联网作为通信载体，通过**隧道技术**加密传输内部数据。效果上等同于专用网。

### 4.8.2 NAT

> **定义——NAT (Network Address Translation)**：在NAT路由器上将内部专用地址转换为全球IP地址，实现专用网主机访问互联网。

> **定义——NAPT (Network Address and Port Translation)**：使用运输层端口号让多个内部主机**共用一个全球IP地址**。出方向：替换源IP+源端口；入方向：替换目的IP+目的端口。

| 方向 | 操作 |
|------|------|
| 离开专用网 | 替换**源**地址→全球地址 |
| 进入专用网 | 替换**目的**地址→内部地址 |

---

## 4.9 多协议标签交换 MPLS

> **定义——MPLS (Multi-Protocol Label Switching)**：给IP数据报打上固定长度**标签**，在第二层用硬件按标签转发。支持QoS、流量工程、VPN。

> **定义——FEC (Forwarding Equivalence Class)**：路由器按同样方式对待的分组集合。同一FEC→同一标签。FEC和标签一一对应。

**MPLS工作过程**：入口打标签→中间标签对换(swapping)→出口去标签。

---

## 附录：第4章完整定义与公式汇总

### 核心定义（64条）

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 1 | **虚电路 VC** | 面向连接。通信前建立逻辑连接，所有分组沿同一路径传送。 |
| 2 | **数据报服务** | 无连接。每个分组独立发送，不同分组可能走不同路径。互联网采用。 |
| 3 | **数据层面** | 根据转发表将分组从对应接口转发出去，硬件实现。 |
| 4 | **控制层面** | 根据路由协议计算路由，软件实现。 |
| 5 | **SDN** | 控制层面集中到远程控制器，路由器仅转发。 |
| 6 | **IP地址** | 32位唯一标识符，分配给主机/路由器的每个接口。 |
| 7 | **CIDR** | 无分类域间路由。IP={网络前缀,主机号}，记作地址/N。 |
| 8 | **地址掩码** | 32位，1的个数=前缀长度。网络地址=IP AND 掩码。 |
| 9 | **路由聚合** | 合并连续子网为更大CIDR块，减少路由条目。 |
| 10 | **ARP** | IP→MAC的地址解析协议。广播请求+单播响应。 |
| 11 | **TTL** | 生存时间，每跳减1，到0丢弃。 |
| 12 | **最长前缀匹配** | 多条匹配时选前缀最长的一项。 |
| 13 | **默认路由** | 0.0.0.0/0。无匹配时的兜底路由。 |
| 14 | **ICMP** | 差错报告/询问协议。Ping和Traceroute使用。 |
| 15 | **IPv6** | 128位地址，首部40B，支持即插即用。 |
| 16 | **AS** | 自治系统。在单一技术管理下的网络集合。 |
| 17 | **IGP** | 内部网关协议。AS内路由（RIP, OSPF）。 |
| 18 | **EGP** | 外部网关协议。AS间路由（BGP）。 |
| 19 | **RIP** | 距离向量IGP。跳数=距离，最大15跳。 |
| 20 | **OSPF** | 链路状态IGP。洪泛法+SPF算法。 |
| 21 | **BGP** | 路径向量EGP。AS间路由。 |
| 22 | **IP多播** | 一对多。D类地址。路由器复制。 |
| 23 | **IGMP** | 管理多播组成员关系。 |
| 24 | **专用地址** | 机构内部使用。10/8, 172.16/12, 192.168/16。 |
| 25 | **VPN** | 隧道加密，公用网作专用网载体。 |
| 26 | **NAT** | 专用地址↔全球地址转换。 |
| 27 | **NAPT** | 共用1个全球IP，用端口号区分。 |
| 28 | **MPLS** | 标签交换。第二层硬件转发。 |
| 29 | **FEC** | 按相同方式转发的分组集合。 |

### 核心公式

| 编号 | 公式 | 说明 |
|------|------|------|
| F1 | 网络地址 = IP地址 AND 子网掩码 | 基础操作 |
| F2 | 可用主机数 = 2^主机位数 - 2 | 减网络地址和广播地址 |
| F3 | 子网数 = 2^借位数 | 从主机位借给网络位 |
| F4 | 检验和 = 反码算术求和取反 | IP首部检验 |
| F5 | RIP最大距离=15，16=不可达 | RIP限制 |
| F6 | OSPF Cost = Σ(接口代价) | 参考带宽÷接口带宽 |
| F7 | 片偏移 = 数据起始位置÷8 | IP分片 |

### 重要辨析

| 辨析 | 要点 |
|------|------|
| **虚电路 vs 数据报** | 虚电路=网络保证可靠(连接+固定路径)；数据报=端系统保证(无连接+独立转发) |
| **IP地址 vs MAC地址** | IP=逻辑寻址(端到端不变)；MAC=物理寻址(每跳改变) |
| **数据层面 vs 控制层面** | 数据层面=转发(硬件)；控制层面=路由选择(软件) |
| **RIP vs OSPF vs BGP** | RIP=距离向量/DV/跳数15/IGP；OSPF=链路状态/LS/SPF/IGP；BGP=路径向量/PV/EGP |
| **IGP vs EGP** | IGP=AS内部(RIP/OSPF)；EGP=AS之间(BGP) |
| **NAT vs NAPT** | NAT=一对一地址转换；NAPT=多对一+端口转换 |
| **IPv4 vs IPv6** | IPv4=32位+检验和+选项；IPv6=128位+无检验和+固定首部40B |
| **专用地址 vs 全球地址** | 专用=内网用(10/8,172.16/12,192.168/16)；全球=互联网唯一 |
| **ARP请求 vs ARP响应** | 请求=广播(不知道MAC)；响应=单播(告知MAC) |
