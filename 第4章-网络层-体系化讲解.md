# 计算机网络 第4章 网络层 — 体系化讲解

> 从第一性原理出发，逐层构建知识体系。本章是全书最大章节(317页)，涵盖4.1~4.10。每个概念均给出严格定义，所有例题收录并附完整解析。末尾附完整定义与公式汇总。

---

## 目录

- [4.1 网络层的几个重要概念](#41-网络层的几个重要概念)
- [4.2 网际协议 IP](#42-网际协议-ip)
- [4.3 IP层转发分组的过程](#43-ip层转发分组的过程)
- [4.4 网际控制报文协议 ICMP](#44-网际控制报文协议-icmp)
- [4.5 IPv6](#45-ipv6)
- [4.6 互联网的路由选择协议](#46-互联网的路由选择协议)
- [4.7 IP 多播](#47-ip-多播)
- [4.8 虚拟专用网 VPN 和网络地址转换 NAT](#48-虚拟专用网-vpn-和网络地址转换-nat)
- [4.9 多协议标签交换 MPLS](#49-多协议标签交换-mpls)
- [4.10 软件定义网络 SDN](#410-软件定义网络-sdn)
- [附录：第4章完整定义与公式汇总](#附录第4章完整定义与公式汇总)

---

## 4.1 网络层的几个重要概念

### 第一性原理：网络层要解决什么问题？

数据链路层只解决**相邻节点**通信。互联网全球范围——任意两台主机可能隔着几十个路由器、穿越以太网/WiFi/光纤/卫星多种异构网络。网络层的核心任务：**在任意两台主机间找到路径，把分组从源送到目的。**

两个根本问题：(1)网络提供怎样的服务——虚电路还是数据报？(2)如何组织"转发"和"路由选择"？

---

### 4.1.1 网络层提供的两种服务

#### 争论核心：可靠交付由谁负责？

> **定义——虚电路 VC (Virtual Circuit)**：面向连接。通信前建立逻辑连接，双方所有分组沿**同一条虚电路**顺序传送。分组按序到达，不需携带完整目的地址(只带虚电路号)。

> **定义——数据报服务 (Datagram Service)**：无连接。网络层向上只提供简单灵活、尽最大努力交付的数据报服务。每个分组**独立发送**，不同分组可走不同路径。互联网采用此方式。

| 对比维度 | 虚电路服务 | 数据报服务 |
|---------|---------|---------|
| **设计思路** | 可靠通信由**网络**保证 | 可靠通信由**用户主机**保证 |
| **连接建立** | 必须有 | 不需要 |
| **终点地址** | 仅连接建立时用，通信中用虚电路号 | 每个分组携带完整终点地址 |
| **分组转发路径** | 同一虚电路走**相同路径** | 每个分组**独立选路** |
| **分组顺序** | 总是按序到达 | 不保证按序 |
| **节点故障** | 所有经过的虚电路均不可用 | 出故障节点可能丢分组，路由可变化 |
| **端到端差错处理** | 由网络负责 | 由主机负责 |
| **拥塞控制** | 可预先分配资源 | 较困难 |

> 🎯 **互联网采用数据报服务**：网络层设计得尽量简单，复杂留给端系统。

### 📝 本节例题

**例1**：有关虚电路服务和数据报服务的特性，正确的是（　　）。
A.虚电路和数据报服务都是无连接 B.数据报服务中分组沿同一条路径按序到达
C.虚电路在建立连接后，分组只需携带虚电路号

**答案：C**。虚电路建立后使用短虚电路号。

**例2**（多选）：关于虚电路的正确说法是（　　）。
A.虚电路与电路交换中的电路没有实质不同 B.通信两站间只能建立一条虚电路
C.虚电路有连接建立、数据传输、连接拆除三阶段 D.虚电路各结点不需要为每个分组做路径选择

**答案：C,D**。虚电路是逻辑连接(≠物理电路)，两站间可建多条。

---

### 4.1.2 网络层的两个层面

> **定义——数据层面 (Data Plane)**：路由器根据转发表把收到的分组从查找到的对应接口转发出去。独立工作，**硬件**转发，快。

> **定义——控制层面 (Control Plane)**：根据路由选择协议计算路由，创建路由表。多个路由器协同工作，**软件**计算。

| | 数据层面 | 控制层面 |
|------|------|------|
| 功能 | 分组转发 | 路由选择 |
| 工作方式 | 独立、逐跳 | 协同、全局 |
| 实现 | 硬件(快) | 软件(灵活) |
| 依据 | 转发表 | 路由表→转发表 |
| 时间尺度 | 纳秒级 | 秒/分钟级 |

> **定义——SDN (Software Defined Network)**：将控制层面集中到**远程控制器**，路由器只负责数据层面转发。控制器计算最佳路由并下发正确的转发表。

### 📝 本节例题

> 在OSI参考模型中，提供路由选择功能的层次是（　　）。A.物理层 B.会话层 C.网络层 D.表示层 → **答案：C**

---

## 4.2 网际协议 IP

网际协议IP是TCP/IP体系中最核心的两个协议之一。配套三个协议：
- **ARP**：地址解析协议(IP→MAC)
- **ICMP**：网际控制报文协议(差错/询问)
- **IGMP**：网际组管理协议(多播)

### 4.2.1 虚拟互连网络

> **定义——虚拟互连网络（IP网）**：使用IP协议将异构物理网络互连，在网络层看起来像一个统一网络，屏蔽下层异构细节。

异构网络需要解决：寻址方案、最大分组长度、网络接入机制、超时控制、差错恢复、状态报告、路由选择技术、用户接入控制等差异。

**两种互连方案**：(1)全部使用相同网络→不能满足不同需求、不适应技术发展；(2)**使用中间设备**→实用。路由器实现真正的网络互连。

**各层中间设备**：
| 层次 | 设备 | 是否称为"网络互连" |
|------|------|:--:|
| 物理层 | 转发器/中继器、集线器 | ❌ 仅扩大网络 |
| 数据链路层 | 网桥、交换机 | ❌ 仅扩大网络 |
| **网络层** | **路由器** | ✅ 真正的网络互连 |
| 运输层及以上 | 网关 | ✅ |

> ⚠️ 转发器/集线器/网桥/交换机仅扩大一个网络，仍是一个网络。**真正的网络互连使用路由器**。

> **定义——直接交付**：源主机和目的主机在**同一网络**上，不需路由器。

> **定义——间接交付**：源主机和目的主机在**不同网络**上，需经过路由器转发。每个路由器收到分组后查转发表→间接交付(转发给下一跳)→最终直接交付到目的主机。

> **定义——跳 (Hop)**：分组从一个设备到另一个设备的传递单位。**两路由器相邻的接口必须在同一网络**。路由器间的间接交付通常在分组到达目的主机前的最后一跳变为直接交付。

### 📝 本节例题

**例1**：数据链路层实现网络互连的设备是（　　）。→ **答案：网桥**（数据链路层）

**例2**：路由器运行于OSI模型的（　　）。→ **答案：网络层**

**例3**：在LAN之间存储转发数据的设备是（　　）。→ **答案：网桥**

**例4**（多选）：常见网络互连设备有（　　）、（　　）和（　　）。→ **答案：路由器、网桥、网关**。A集线器是物理层，不实现网络互连。

**例5**：路由器通过（　　）层进行网络互连，功能与（　　）有关，一般做成（　　）转换。→ **答案：网络层；协议；多协议**

---

### 4.2.2 IP 地址

> **定义——IP地址**：TCP/IP体系中给每个连接在互联网上的主机(或路由器)的**每一个接口**分配的唯一32位标识符。由ICANN负责分配。采用**点分十进制记法**：每8位一组，共4组，如129.52.6.0。

```
IP地址 ::= { <网络号(网络前缀)>, <主机号> }
```

**IP地址的特点**（4条）：
1. **分等级结构**：网络前缀+主机号。好处：方便分配管理、实现路由聚合减小转发表
2. **标志接口**：IP地址标志主机/路由器的**接口**(非设备本身)。多归属主机(multihomed host, 连接多个网络)需多个IP。路由器至少两个IP地址
3. **相同网络号的设备在同一个网络**：转发器/交换机连接的同一网络号主机构成网络
4. **所有网络平等**：互联网同等对待局域网和广域网

**点分十进制记法举例**：
| 二进制 | 十进制 |
|--------|--------|
| 10000001 00110100 00000110 00000000 | 129.52.6.0 |
| 11000000 00000101 00110000 00000011 | 192.5.48.3 |
| 00001010 00000010 00000000 00100101 | 10.2.0.37 |
| 10000000 00001010 00000010 00000011 | 128.10.2.3 |
| 10000000 10000000 11111111 00000000 | 128.128.255.0 |

---

#### 分类的 IP 地址

| 类别 | 前缀 | 网络号位数 | 首个网络号 | 末个网络号 | 每网络最大主机数 |
|------|------|------|------|------|------|
| **A类** | 0 | 8位 | 1 | 126 (2⁷-2) | 16777214 (2²⁴-2) |
| **B类** | 10 | 16位 | 128.0 | 191.255 | 65534 (2¹⁶-2) |
| **C类** | 110 | 24位 | 192.0.0 | 223.255.255 | 254 (2⁸-2) |
| **D类** | 1110 | — | 224.0.0.0 | 239.255.255.255 | 多播地址 |
| **E类** | 1111 | — | 240.0.0.0 | 255.255.255.255 | 保留 |

> A类网络号0和127保留不指派。最大可指派网络数：A类126(2⁷-2)、B类16384(2¹⁴)、C类2097152(2²¹)。

**分类IP地址优缺点**：✅管理简单、使用方便、转发分组迅速、划分子网灵活；❌大地址块浪费、设计不合理、划分子网也无法解决地址枯竭。

#### 特殊IP地址

| 网络号 | 主机号 | 可作源 | 可作目的 | 含义 |
|--------|--------|:--:|:--:|------|
| 全0 | 全0 | ✅ | ❌ | 本网络上本主机(DHCP) |
| 全0 | X | ✅ | ❌ | 本网络上主机号X的主机 |
| 全1 | 全1 | ❌ | ✅ | 本网络受限广播 |
| 网络号 | 全1 | ❌ | ✅ | 向该网络定向广播 |
| 127 | 任意 | ✅ | ✅ | 环回测试 |

### 📝 本节例题(分类地址)

**例1**：190.233.27.13是（　　）类地址。→ 190∈[128,191]→**B类**

**例2**：以下哪个IP能用于主机？（　　）
A.29.9.255.15 B.127.21.19.109 C.192.5.91.255 D.220.103.256.56
→ **答案A**。B是环回(127.x)、C是广播(主机号全1)、D含256超范围。

---

#### 子网划分

> **定义——子网划分**：将原属主机位的比特"借"过来作为子网号。让多个物理网络共用一个IP网络号，减少地址浪费、方便管理。

**核心公式**：2^N ≥ 所需子网数，N为需借用的主机位数。

**划分子网后**：IP地址 = 网络地址 + 子网地址 + 主机地址

> 例：B类128.1.0.0，划分7个子网→2^N≥7, N=3→借3位→子网号128.1.0/32/64/96/128/160/192/224.0，掩码255.255.224.0(/19)

> RFC 950曾规定全0全1子网号保留不用；**RFC 1878废止了该限制**。

---

#### 无分类编址 CIDR

> **定义——CIDR (Classless Inter-Domain Routing)**：消除了传统A/B/C类地址划分。用`地址/前缀位数`表示。IP地址 ::= {<网络前缀>, <主机号>}。

> **定义——地址掩码（子网掩码）**：32位，连续1后接连续0。1的个数=网络前缀位数。**网络地址 = IP地址 AND 地址掩码**。

**默认地址掩码**：A类255.0.0.0、B类255.255.0.0、C类255.255.255.0。

**网络地址算例**：128.14.35.7/20 → 128.14.32.0。计算：35 AND 240(掩码第四段)=32。

> ⚠️ **地址块与主机地址的辨析**：128.14.35.7/20是主机地址同时指明前缀；128.14.32.0/20是地址块也是主机号全0地址。不指明前缀时无法确定网络地址。

**地址块数据**：128.14.32.0/20共2¹²=4096个地址，最小128.14.32.0，最大128.14.47.255，可指派2¹²-2=4094个。

#### 常用的CIDR地址块

| 前缀长度 | 掩码 | 包含地址数 | 相当于分类网络数 |
|---------|------|---------|--------------|
| /13 | 255.248.0.0 | 512K | 8个B类/2048个C类 |
| /14 | 255.252.0.0 | 256K | 4个B类/1024个C类 |
| /15 | 255.254.0.0 | 128K | 2个B类/512个C类 |
| /16 | 255.255.0.0 | 64K | 1个B类/256个C类 |
| /17 | 255.255.128.0 | 32K | 128个C类 |
| /18 | 255.255.192.0 | 16K | 64个C类 |
| /19 | 255.255.224.0 | 8K | 32个C类 |
| /20 | 255.255.240.0 | 4K | 16个C类 |
| /21 | 255.255.248.0 | 2K | 8个C类 |
| /22 | 255.255.252.0 | 1K | 4个C类 |
| /23 | 255.255.254.0 | 512 | 2个C类 |
| /24 | 255.255.255.0 | 256 | 1个C类 |
| /25 | 255.255.255.128 | 128 | 1/2个C类 |
| /26 | 255.255.255.192 | 64 | 1/4个C类 |
| /27 | 255.255.255.224 | 32 | 1/8个C类 |

#### 三个特殊 CIDR 地址块

| 前缀 | 掩码 | 用途 |
|------|------|------|
| /32 | 255.255.255.255 | **主机路由**（单个IP地址） |
| /31 | 255.255.255.254 | **点对点链路**（仅主机号0和1，无需广播） |
| /0 | 0.0.0.0 | **默认路由** |

---

#### 路由聚合（构造超网）

> **定义——路由聚合（构造超网）**：将多个连续子网合并为更大CIDR地址块，减少路由表条目。每个CIDR地址块中地址数一定是2的整数次幂。文献中也称"构造超网"。

**算例**：200.14.0.0~200.14.15.0的16个C类 → 200.14.0.0/20（16条路由→1条）

**更大算例**：ISP 206.0.64.0/18 → 大学206.0.68.0/22 → 四个系：一系206.0.68.0/25(128地址)、二系206.0.68.128/25、三系206.0.71.128/26(64地址)、四系206.0.71.192/26。**64个C类→转发表1行**。

### 📝 本节例题(IP地址/子网/CIDR)

**例1**：点对点链路为减少地址浪费，掩码应为（　　）。→ **255.255.255.254(/31)**

**例2**：C类掩码255.255.255.248，每个子网可用主机数是（　　）。→ 248=11111000, 主机位3, 2³-2=**6**

**例2b**：C类掩码255.255.255.248可提供多少子网？→ 248=11111000, 子网位5, 2^5=**32个子网**

**例2c**：C类最大可能子网位数是（　　）。→ 主机位最少2位(需留网络+广播), C类主机位8, 最多借8-2=**6位**

**例3**：已知IP 128.14.35.7/20，网络地址为（　　）。→ 35 AND 240=32, **128.14.32.0**

**例4**：将138.10.0.0(B类)分成6个子网，掩码？→ 2^N≥6, N=3, 借3位, 掩码255.255.224.0

**例5**：CIDR错误说法是（　　）。→ **"网络前缀越短，地址越少"是错的**(前缀越短→地址越多)

**例6**：C类225.255.255.224掩码，子网位数、子网数、每子网主机数？
→ 224=11100000, 子网位3, 子网数2³=8, 主机位5, 2^5-2=30 → **3, 8, 30**

**例7**：C类分成8个子网，掩码应为（　　）。→ 2^N≥8, N=3, 掩码255.255.255.224

**例8**：路由聚合 170.18.129.0/24, 170.18.130.0/24, 170.18.132.0/24, 170.18.133.0/24 → 取公共前缀。129=10000001, 130=10000010, 132=10000100, 133=10000101 → 170.18.128.0/21

**例9**：C类掩码255.255.255.240可划分多少子网？→ 240=11110000, 借4位, **2^4=16个子网**

**例10**：B类140.111.0.0切9个子网求掩码。→ 2^N≥9, N=4, 掩码255.255.255.240
解析：借位4→掩码第四段11110000=240

**例11**：222.205.74.56/255.255.240.0的子网地址？→ 74 AND 240=64, **222.205.64.0**

**例12**：在下图网络配置中，总共（　　）个广播域，（　　）个冲突域。
（图：路由器连接2个交换机，每个交换机下接若干主机）
→ 路由器隔离广播域：每端口一个广播域→**2个广播域**；交换机每端口一个冲突域：共**7个冲突域**。

---

### 4.2.3 IP 地址与 MAC 地址

| 维度 | IP 地址 | MAC 地址 |
|------|--------|---------|
| **性质** | 逻辑地址(软件) | 物理地址(固化在网卡ROM中) |
| **使用层次** | 网络层及以上 | 数据链路层 |
| **位置** | IP数据报首部 | MAC帧首部 |
| **长度** | 32位(IPv4) | 48位 |
| **跨越网络时** | 源→终点不变 | 每跳都变 |

> 🎯 IP地址放在IP数据报首部，MAC地址放在MAC帧首部。IP地址提供端到端逻辑寻址**不变**；MAC地址提供每跳物理寻址**逐跳改变**。

**不同区间的地址变化**（H1→R1→R2→H2）：

| 区间 | IP源 | IP目的 | MAC源 | MAC目的 |
|------|------|--------|-------|--------|
| H1→R1 | IP1 | IP2 | MAC1 | MAC3 |
| R1→R2 | IP1 | IP2 | MAC4 | MAC5 |
| R2→H2 | IP1 | IP2 | MAC6 | MAC2 |

> 尽管互联的MAC地址体系各不相同，IP层抽象互联网屏蔽了这些差异。在IP层讨论问题用统一的抽象IP地址。

### 📝 本节例题

**例1**：关于IP地址与硬件地址的叙述错误的是（　　）。
A.硬件地址=物理地址=MAC地址 B.硬件地址是数据链路层和物理层使用
C.IP地址是网络层及以上使用 D.RARP是地址解析协议

→ **答案：D**。RARP是逆地址解析协议，将MAC→IP(已淘汰)。

**例1b**(多选)：IP协议组包括哪些协议？→ **答案：A.IP B.ICMP C.ARP D.RARP(全选)**

**例2**：路由器两端口分别接网络110.25.53.1和110.24.52.6，掩码255.255.255.0，可分配给两端口的IP是（　　）。→ 端口须与所接网络同网段且分属不同网络。**答案C**

---

### 4.2.4 地址解析协议 ARP

> **定义——ARP (Address Resolution Protocol)**：解决"已知目标IP，求对应MAC"问题。工作在网络层，封装在数据链路层帧中。先在ARP高速缓存中查找，未命中则**广播ARP请求**，目标主机**单播ARP响应**。

> **定义——ARP高速缓存 (ARP Cache)**：存放最近获得的IP→MAC映射表，映射表动态更新(新增/超时删除)，减少ARP广播通信量。

**ARP请求/响应分组内容**：发送方硬件地址、发送方IP地址、目标方硬件地址(未知时填0)、目标方IP地址。**路由器不转发ARP请求**。

**减少ARP通信量的技巧**：主机A发送ARP请求时将自己的IP→MAC写入请求分组。收方B收到后将A的映射写入自己的高速缓存，不必再发送ARP请求。

**使用ARP的四种典型情况**：

| 发送方 | 目标 | 用ARP找谁的MAC |
|------|------|---------|
| 主机 | 同网主机 | 目的主机的MAC |
| 主机 | 跨网主机 | **本网路由器**的MAC |
| 路由器 | 本网主机 | 目的主机的MAC |
| 路由器 | 跨网转发 | **下一跳路由器**的MAC |

**为什么要两种地址(IP+MAC)**：不同网络MAC地址体系不同且极其复杂；对以太网MAC直接寻址极其困难；IP编址将此问题解决。多次调用ARP由软件自动完成→用户透明。

### 📝 本节例题

> 主机A给B发IP分组，共经过5个路由器，假设不会出错，使用ARP的最大次数是（　　）。A.0 B.1 C.6 D.7
> **答案：C(6次)**。A→路由器1(1次) + 路由器1→路由器2(1次) + ... + 路由器5→B(1次) = A一次(发到R1)+R1到R2+R2到R3+R3到R4+R4到R5+R5到B = 6次。

---

### 4.2.5 IP 数据报的格式

```
0        4        8       16      19      24      31
┌──────┬──────┬────────┬──────────────────────┐
│ 版本  │首部长度│区分服务│       总长度          │
├──────┴──────┼────────┴─────────┬────────────┤
│     标识     │    标志    │     片偏移        │
├────────────┼─────────────┼──────────────────┤
│  生存时间   │    协议     │     首部检验和      │
├────────────┴─────────────┴──────────────────┤
│                  源地址（32位）                │
├──────────────────────────────────────────────┤
│                 目的地址（32位）               │
├──────────────────────────────────────────────┤
│     可选字段(长度可变, 1~40B)     │    填充     │
└──────────────────────────────────────────────┘
```

**首部固定部分20字节，各字段详解**：

| 字段 | 位数 | 含义/数据点 |
|------|------|-----------|
| **版本** | 4位 | IPv4=4。确保通信双方使用相同IP协议版本 |
| **首部长度** | 4位 | 单位4字节。最小值5(20B)，**最大值15(60B)**。可表示的最大十进制数值=15 |
| **区分服务** | 8位 | 使用DiffServ时才起作用，一般不用 |
| **总长度** | 16位 | 首部+数据字节数。**必须≤MTU**。最大65535B |
| **标识** | 16位 | 计数器产生。分片后同一数据报所有片**标识相同** |
| **标志** | 3位 | DF(Don't Fragment)=1禁止分片；**MF(More Fragments)=1后面还有分片，MF=0最后一片**。**只有DF=0时才允许分片** |
| **片偏移** | 13位 | 单位**8字节**。该片在原数据报中的相对位置。除最后一片外每片数据长须为8的倍数 |
| **生存时间 TTL** | 8位 | 每经过一个路由器减1，**减到0时丢弃**。防止分组兜圈子 |
| **协议** | 8位 | 指出应将数据部分交给哪个上层协议 |
| **首部检验和** | 16位 | 只检验**首部**(数据部分不参与)。每经路由器TTL变→**必须重新计算** |
| **源地址** | 32位 | — |
| **目的地址** | 32位 | — |

**协议字段值完整表**：

| 协议 | ICMP | IGMP | IP | TCP | EGP | IGP | UDP | IPv6 | ESP | AH | ICMPv6 | OSPF |
|------|------|------|----|-----|-----|-----|-----|------|-----|-----|--------|------|
| 值 | 1 | 2 | 4 | **6** | 8 | 9 | **17** | 41 | 50 | 51 | 58 | 89 |

> 🚨 易错：协议字段值6是**TCP**(不是UDP)，17是**UDP**。

**检验和计算**（反码算术运算）：
1. 发送端检验和字段**置全0**
2. 将首部按每16位分组
3. 所有16位字进行**反码算术加法**(进位回卷到最低位)
4. 求和结果**取反码**→填入检验和字段
5. 接收端对首部(含检验和)反码求和取反→**结果为0则保留，否则丢弃**

**例题**：二进制串1101011110101100求8比特检验和。
- 分两组：11010111 + 10101100 = 1 10000011(进位1回卷)→10000100
- 取反码→**01111011** = 检验和
- 练习：1010110111101011→检验和=**01100110**

**可选项字段**：排错、测量、安全。长度1~40字节，须填充至4字节整数倍。实际很少使用→增加路由器处理开销。IPv6取消了选项字段(改用扩展首部)。

### 📝 本节例题

**例1**(判断)：IP数据报首部协议字段值=6时对应UDP协议。→ **错误**(6=TCP, 17=UDP)

**例2**(判断)：首部长度占4位，可表示的最大十进制数值是15。→ **正确**(4位最大=1111=15, 15×4=60字节)

---

## 4.3 IP层转发分组的过程

### 4.3.1 基于终点的转发

分组在互联网中是**逐跳转发**。每跳路由器根据目的地址查转发表从对应接口转发。转发表中最主要的路由是(目的网络地址, 下一跳地址)，而不是(目的主机地址, 下一跳地址)。查找转发表=逐行寻找前缀匹配。

**转发算例**（R2转发表）：
| 目的网络 | 下一跳 |
|---------|--------|
| 20.0.0.0/16 | 直接交付，接口0 |
| 30.0.0.0/16 | 直接交付，接口1 |
| 15.0.0.0/16 | 经20.0.0.7，接口0 |
| 40.0.0.0/16 | 经30.0.0.1，接口1 |

### 4.3.2 最长前缀匹配

> **定义——最长前缀匹配 (Longest Prefix Match)**：当转发表中有多条表项与目的IP匹配时，选择**网络前缀最长**的一项。又称最长匹配或最佳匹配。前缀越长→地址块越小→路由越具体。

**例题**：路由器R1有转发表：128.1.3.64/26→R2接口1；128.1.2.128/25→直接接口1；128.1.2.192/26→直接接口0。目的128.1.2.196匹配哪条？

```
128.1.2.196 AND 255.255.255.128 = 128.1.2.128 → 匹配/25 ✓
128.1.2.196 AND 255.255.255.192 = 128.1.2.192 → 匹配/26 ✓
最长前缀是/26 → 经接口0直接交付
```

### 4.3.3 转发表中的特殊路由

> **定义——主机路由**：对特定主机IP的/32路由，放在转发表最前面。

> **定义——默认路由**：0.0.0.0/0。无任何匹配时使用。可减少路由表空间和搜索时间。

### 路由器分组转发算法

```
提取目的IP地址D
    ↓
查特定主机路由 → 命中？ → 转发到下一跳
    ↓ 否
查最长前缀匹配 → 命中？ → 转发到下一跳
    ↓ 否
查默认路由 → 命中？ → 转发到下一跳
    ↓ 否
丢弃分组
```

### 📝 本节例题

**例1**：路由信息中不包含的是（　　）。→ **源地址**。路由信息含目标网络、路由权值、下一跳。

**例2**：内部网络对外出口只有一个，最好配置（　　）。→ **默认路由**

**思考题1**（P131）：已知R2转发表（同4.3.1算例表），请写出R3路由器的路由表。→ 以R3视角：直连网络30.0.0.0/16和40.0.0.0/16距离=1直接交付；到达20.0.0.0/16经30.0.0.2；到达15.0.0.0/16经30.0.0.2。

**思考题2**（P145）：N2网络上的主机H2的路由表应如何配置？→ H2只需一条默认路由指向R2，因为H2所有出网流量都需经R2转发。

---

## 4.4 网际控制报文协议 ICMP

> **定义——ICMP (Internet Control Message Protocol)**：允许主机或路由器报告差错和提供异常报告。是**IP层协议**(非高层协议)，报文封装在IP数据报中(协议字段=1)。

### ICMP 报文格式

```
0        8       16       31
┌──────┬──────┬──────────────┐
│ 类型  │ 代码  │  检验和       │  ← 前4字节统一格式
├──────┴──────┴──────────────┤
│      其余部分取决于类型      │
│    (最短为4个全0字节)        │
└────────────────────────────┘
```

- **类型**(1B)：表明ICMP消息类型
- **代码**(1B)：类型的附加信息(子类型)
- **检验和**(2B)：对整个ICMP报文进行检验

### ICMP 报文种类

| 报文种类 | 类型值 | 说明 |
|---------|--------|------|
| **差错报告报文** | | |
| 终点不可达 | **3** | 编码0网络不可达/1主机不可达/2协议不可达/3端口不可达/4需分片但DF=1/5源路由失败 |
| 源站抑制 | **4** | 网络阻塞时产生，请求降低发送速率 |
| 改变路由(Redirect) | **5** | 编码0网络重定向/1主机重定向 |
| 时间超过 | **11** | TTL=0时产生(编码0分片重组超时) |
| 参数问题 | **12** | 编码0缺少参数/1参数出错 |
| **询问报文** | | |
| 回送请求/回答 | 8/0 | Ping使用 |
| 时间戳请求/回答 | 13/14 | 32位存自1900.1.1起秒数，时钟同步 |

**ICMP差错报告报文的数据字段**：装入原IP数据报的**首部+前8字节数据**。

**不应发送ICMP差错报告报文的4种情况**：
1. 对ICMP差错报告报文不再发送ICMP差错报告报文
2. 对第一个分片后的所有后续分片不发送
3. 对具有多播地址的数据报不发送
4. 对特殊地址(127.0.0.0或0.0.0.0)不发送

### ICMP 应用

**Ping**：测试连通性。使用回送请求(8)和回送回答(0)。**应用层直接使用网络层ICMP**(不经过运输层)。

**Traceroute (tracert)**：跟踪分组路径。Windows为tracert。发送ICMP回显请求报文，利用**TTL字段+ICMP时间超过报文+ICMP终点不可达报文**三种机制实现路径跟踪。

### 📝 本节例题

**例1**：关于ICMP的论述不正确的是（　　）。
A.ICMP典型用途是差错报告 B.ICMP报文封装在IP分组中，因此和TCP一样都是运输层协议
C.ping程序向目标主机发送了ICMP回送请求报文 D.ICMP是IP层协议
→ **答案：B**。ICMP不是运输层协议，是IP层协议。

**例2**(填空)：ICMP属于（**网际层**）协议，ICMP报文封装在（**IP**）协议数据单元中传送。ICMP有**9种**报文，常用的PING使用了（**回送请求和应答**）报文。TTL=0→路由器发（**超时**）报文，网络阻塞→路由器产生（**源站抑制**）报文。

---

## 4.5 IPv6

> **定义——IPv6**：下一代IP协议。地址从32位→**128位**。首部固定**40字节**(基本首部)，8个字段。取消检验和(由链路层和上层负责)。

### IPv6的主要变化(8条)

1. **更大的地址空间**：128位(约3.4×10³⁸)
2. **扩展的地址层次结构**：可划分更多层次
3. **灵活的首部格式**：用扩展首部替代IPv4选项字段
4. **改进的选项**：扩展首部放在有效载荷中
5. **允许协议继续扩充**：适应新应用
6. **即插即用(自动配置)**：不需DHCP
7. **支持资源预分配**：实时视频等固定带宽/时延需求
8. **首部8字节对齐**：优化处理效率

### IPv6 数据报结构

```
┌─────────────────────┐
│   基本首部 (40B)     │
├─────────────────────┤
│   扩展首部(0或多个)   │
├─────────────────────┤
│   数据部分           │
└─────────────────────┘
      有效载荷
```

**IPv6对IPv4首部的更改**：取消首部长度(定长40B)、取消服务类型(→通信量类+流标号)、取消总长度(→有效载荷长度)、TTL→跳数限制、协议→下一个首部、取消检验和、取消选项字段(→扩展首部)。

### IPv6 基本首部格式(40字节, 8字段)

| 字段 | 位数 | 含义 |
|------|------|------|
| **版本** | 4 | IPv6=6 |
| **通信量类** | 8 | 区分不同IPv6数据报的类别/优先级 |
| **流标号** | 20 | "流"=特定源→特定目的的数据报序列。实时数据传输用 |
| **有效载荷长度** | 16 | 扩展首部+数据的字节数，最大64KB |
| **下一个首部** | 8 | 相当于IPv4的协议字段(或指出扩展首部类型) |
| **跳数限制** | 8 | 源站设定，每转发减1，到0丢弃。相当于IPv4的TTL |
| **源地址** | 128 | — |
| **目的地址** | 128 | — |

### IPv6 地址表示

- **冒号十六进制记法**：16位一组，8组。如`68E6:8C64:FFFF:FFFF:0:1180:960A:FFFF`
- **零压缩**：连续零用`::`取代(**只能用一次**)。如`FF05:0:0:0:0:0:0:B3`→`FF05::B3`
- **混合记法**：`::128.10.2.1`(最后两组用点分十进制)
- 🚨 错误示例：`1080::800:200C::417A`(两次`::`→歧义)
- **CIDR记法**：`12AB::CD30:0:0:0:0/60`。IPv6取消了子网掩码

### IPv6 地址类型

| 类型 | 二进制前缀 | IPv6记法 | 说明 |
|------|---------|---------|------|
| 未指明地址 | 全0(128位) | ::/128 | 只能作源地址 |
| 环回地址 | 00...1(128位) | ::1/128 | 本地回环 |
| 多播地址 | 11111111(8位) | FF00::/8 | 一对多 |
| 本地链路单播 | 1111111010(10位) | FE80::/10 | 同一链路内(≈IPv4 169.254.0.0/16) |
| 本地站点单播 | 1111111011(10位) | FEC0::/10~FEFF:: | 私有地址(≈IPv4专用地址) |
| 全球单播 | 其余前缀 | — | 全球唯一 |

> IPv6**没有广播地址**，广播由多播实现。

**三种基本地址类型**：单播(unicast, 点对点)、多播(multicast, 一对多)、**任播(anycast, 交付到一组中距离最近的一个)**。

**节点与接口**：IPv6中主机和路由器统称**节点**。IPv6地址分配给节点上的**接口**。多接口节点可有多个单播地址，任一可作目的地址。

### 从 IPv4 向 IPv6 过渡

1. **双协议栈**：主机/路由器同时运行IPv4和IPv6。收发两种数据报。
2. **隧道技术**：将IPv6数据报**封装在IPv4数据报中**(IPv4协议字段=41)，穿越IPv4网络。

过渡原则：**逐步演进，向后兼容**(IPv6系统必须能接收/转发IPv4分组并为IPv4选路)。

### ICMPv6

> **定义——ICMPv6**：IPv6环境下的控制报文协议。**合并了ARP和IGMP的功能**。

ICMPv6报文分类：差错报文、信息报文、组成员关系报文(MLD协议)、**邻站发现报文(ND协议)**。ND=Neighbor-Discovery邻站发现，MLD=Multicast Listener Delivery多播听众交付。

### 📝 本节例题

> IPv6地址`12AB:0000:0000:CD00:0000:0000:0000:0000`的不正确压缩写法是（　　）。
A.12AB::CD00:0:0:0:0 B.12AB:0:0:CD00:: C.12AB:0:0:CD00:0:0:0:0 D.12AB::CD30::
→ **答案：D**。`12AB::CD30::`使用两次零压缩有歧义。全0组须显式用`:0:`表示。

---

## 4.6 互联网的路由选择协议

### 4.6.1 基本概念

> **定义——路由选择协议**：规定互联网中有关路由器如何相互交换信息并生成路由表。属于网络层**控制层面**。

**理想路由算法的6个特性**：正确完整、计算简单、自适应、稳定、公平、最佳。

> ⚠️ 不存在绝对的最佳路由算法。"最佳"是相对于某种特定要求的较合理选择。路由选择非常复杂——需所有节点协调、环境动态变化、拥塞时难获得路由信息。

**路由算法分类**：
| 策略 | 说明 | 优缺点 |
|------|------|--------|
| **静态(非自适应)** | 人工配置路由 | 简单开销小；不能及时适应变化 |
| **动态(自适应)** | 路由器自动交换更新 | 较好适应变化；实现复杂开销大 |

> 互联网采用**自适应的、动态的、分布式**路由选择协议。

**分层次的原因**：(1)互联网规模非常大；(2)**保密原因**。

> **定义——自治系统 AS (Autonomous System)**：在单一技术管理下的网络、IP地址和路由器集合。AS内部使用统一路由选择协议和**共同度量**，每个AS对其他AS表现出**单一和一致的路由选择策略**。

**两大路由选择协议**：

> **定义——IGP (Interior Gateway Protocol，内部网关协议)**：AS**内部**使用的路由选择协议。如RIP、OSPF。对应**域内路由选择 (intradomain routing)**。

> **定义——EGP (External Gateway Protocol，外部网关协议)**：AS**之间**使用的路由选择协议。如BGP-4。对应**域间路由选择 (interdomain routing)**。

### 📝 本节例题

> IGP的作用范围是（　　）。A.区域内 B.自然子网 C.局域网内 D.自治系统内 → **答案：D**

---

### 4.6.2 内部网关协议 RIP

> **定义——RIP (Routing Information Protocol)**：分布式、基于**距离向量**的IGP。互联网标准协议。度量=**跳数**。直接连接的网络距离=1，每经一个路由器跳数+1。最大允许15跳，**16=不可达**。

> ⚠️ 从封装角度看RIP是**应用层协议**(运输层使用UDP端口520)，但为网络层提供服务。

**RIP三个特点**：
1. 仅和**相邻路由器**交换信息
2. 交换的是**完整路由表**
3. 按**固定时间间隔**(默认30秒)交换

**好路由=距离短的路由**。最佳路由=距离最短。RIP不能在两个网络间同时使用多条路由(只选距离最短)。

**收敛**：自治系统中所有结点都得到正确路由信息的过程。

**路由表建立过程**：空表→获得直连网络距离=1→与相邻路由器交换更新→若干次后所有路由器知道到达本AS内各网络的最短距离和下一跳。

**RIP路由表内容**：目的网络、距离(最短)、下一跳地址。

**距离向量算法**：
```
收到相邻路由器X发来的RIP报文:
1. 所有项目下一跳改为X，距离+1
2. 对每个项目：
   - 路由表中无目的网络N → 加入
   - 有N且下一跳=X → 不论距离大小都更新
   - 有N且下一跳≠X → 新距离<旧距离才更新
   - 有N且下一跳≠X → 新距离≥旧距离不变
3. 若3分钟未收到相邻路由器的更新路由表 → 标记该路由器不可达(距离=16)
```

**距离向量算例**（表4-8）：

R6原路由表：Net2→3→R4; Net3→4→R5。收到R4更新(经修改后下一跳R4,距离+1)：Net2→4→R4; Net3→8→R4; Net6→4→R4; Net8→3→R4; Net9→5→R4。

逐项判断：Net2(同下一跳R4→更新距离4)；Net3(新距离8>旧距离4→不变)；Net6(无此项→加入距离4)；Net8(下一跳不同但新距离3<旧→更新)；Net9(无→加入距离5)。最终R6：Net2→4→R4; Net3→4→R5; Net6→4→R4; Net8→3→R4; Net9→5→R4。

**RIP报文格式**：UDP端口520。每路由信息20字节(可重复出现**最多25条**)。最大长度=4字节首部(命令1B+版本1B+保留2B)+20×25=**504字节**。命令字段：1=请求，2=响应。RIP2支持CIDR、鉴别、多播。地址类别字段IP=2，路由标记=AS号。

**慢收敛问题(坏消息传播慢)**：R1到网1故障→R1标记距离16。但在R2收到R1更新前仍发旧报文(距离2→R1)。R1收到R2报文后误以为可经R2(距离=3)到达网1→更新距离为3。R2又收R1报文(距离3)→更新距离4→循环递增直到16双方才知道不可达。根本原因：更新路由信息时未判断下一站具体信息。

**慢收敛解决方案**：
| 方法 | 定义 |
|------|------|
| **水平分割** | 从某接口收到的更新信息不能再从该接口发回 |
| **毒性逆转** | 路由失效时先设距离16(中毒)，再发布出去。邻居收到后回发毒性逆转确认 |
| **触发更新** | 路由表发生变化时立即发送更新(不等30秒周期) |

**RIP优缺点**：✅实现简单、开销小。❌规模仅限15跳；交换完整路由表开销大；坏消息传播慢收敛时间长；度量过于简单(不考虑带宽/时延)。

### 📝 本节例题(RIP)

**例1**(多选)：关于RIP协议说法正确的有（　　）。
A.RIP是IGP B.RIP是EGP C.RIP是距离矢量路由协议 D.RIP是链路状态路由协议 → **答案：A,C**

**例2**(多选)：解决慢收敛的策略有（　　）。→ **水平分割、毒性逆转、触发刷新**

---

### 4.6.3 内部网关协议 OSPF

> **定义——OSPF (Open Shortest Path First)**：基于**链路状态**的IGP。1989年为克服RIP缺点开发。原理简单但实现复杂。现在使用OSPFv2。**是网络层协议**(直接封装在IP数据报中，协议字段=89)。使用Dijkstra最短路径优先(SPF)算法。

**三个主要特点**：
1. 采用**洪泛法**向本AS**所有路由器**发送信息(**多播方式**)
2. 发送的信息是**链路状态**(和哪些路由器相邻、链路代价)
3. 链路状态变化时才发送(**触发更新**)，定期(30分钟)刷新一次

**链路代价**：Cost=参考带宽(100Mbps)÷接口带宽。FE接口默认cost=1。沿途**所有入接口Cost累加**，Cost越小路径越优。

> **定义——链路状态数据库**：每个路由器最终建成的**全网拓扑图**(在全网范围内一致=同步)。用链路状态数据库+Dijkstra算法→生成路由表。

**划分区域**：为减少洪泛通信量和加快收敛速度。OSPF将AS分为主干区域(0.0.0.0)和普通区域。**所有区域必须连接到主干区域**。区域内路由器**不超过200个**。非主干区域之间**不允许直接发布区域间路由信息**。区域间只传递抽象路由信息。

**OSPF中的路由器类型**：
| 类型 | 说明 |
|------|------|
| **区域边界路由器 ABR** | 连接不同区域与主干 |
| **主干路由器 BR** | 至少有一个接口在主干区域 |
| **自治系统边界路由器 ASBR** | 连接OSPF AS到其他AS |

**OSPF支持**：多路径负载均衡(到同一目的多条等代价路径可分流)；不同业务可算不同路由(TOS)；分组**鉴别**(简单口令/MD5)；VLSM和CIDR。32位序号600年内不重复。

**五种分组类型**：
| 类型 | 名称 | 功能 |
|------|------|------|
| 1 | **Hello(问候)** | 发现/维持邻站可达性，每**10秒**交换一次，**40秒**未收到即认为不可达 |
| 2 | **数据库描述** | 向邻居发送链路状态摘要+序号 |
| 3 | **链路状态请求** | 请求缺失的链路状态详细信息 |
| 4 | **链路状态更新** | 洪泛法发送新状态，**触发+30分钟定期刷新** |
| 5 | **链路状态确认** | 对更新分组的确认 |

**OSPF工作过程**：Hello确定可达→数据库描述交换摘要→链路状态请求获取缺失细节→链路状态更新/确认同步→用SPF算法生成路由表。OSPF没有"坏消息传播得慢"问题，规模大时远优于RIP。

### 📝 本节例题(OSPF)

**例1**：OSPF协议使用（　　）。→ **链路-状态算法**

**例2**：关于RIP和OSPF叙述中错误的是（　　）。
A.RIP使用距离向量算法 B.OSPF使用链路状态算法
C.RIP和OSPF都是外部网关协议 D.OSPF仅与相邻路由器交换信息
→ **答案：C,D**。C两者都是IGP(内部)；D OSPF向全区域洪泛(非仅相邻)。

---

### 4.6.4 外部网关协议 BGP

> **定义——BGP (Border Gateway Protocol)**：不同AS间交换路由信息的EGP。使用**路径向量**算法。最新版本BGP-4(2006年1月, RFC 4271~4278)。从封装角度看是**应用层协议**(使用TCP 179端口)。

**为什么不追求最佳路由(选可达无环路)**：
1. 互联网规模太大→AS间路由选择非常困难
2. 必须考虑策略(价格、权限等)

BGP交换路由信息的结点数量级是**AS数的量级**。路由选择只能寻找一条**能够到达且较好**的路由(不兜圈子)。

> **定义——BGP发言者 (BGP Speaker)**：运行BGP的边界路由器。对等BGP发言者通过TCP 179建立BGP会话。

**BGP路由 = [前缀, BGP属性] = [前缀, AS-PATH, NEXT-HOP]**：
- 前缀：CIDR记法指明子网
- AS-PATH：经过的AS序列，防环路+路由策略
- NEXT-HOP：下一跳路由器IP

**算例**：路由1=[X, AS2 AS3, IP2a] vs 路由2=[X, AS3, IP3c]。AS-PATH较短的路由2优先。对应转发表：匹配前缀X→下一跳R1b→转发接口0。

### 📝 本节例题

> BGP属于哪种类型协议（　　）。A.内部路由 B.外部路由 C.混合路由 D.边界路由 → **答案：B**(EGP/外部路由协议)

---

### 4.6.5 路由器的构成

```
┌──────────────────────────┐
│  路由选择处理机(控制层面)    │
│  路由协议 │ 路由表          │
└──────────┬───────────────┘
           ↕
┌────┬────┬────┬──────┐
│输入 │输入 │输出 │输出   │
│端口 │端口 │端口 │端口   │ ← 数据层面(硬件)
└────┴────┴────┴──────┘
         交换结构
```

**"转发" vs "路由选择"**：
| | 转发 | 路由选择 |
|------|------|------|
| 涉及 | 一个路由器 | 多个路由器 |
| 依据 | 转发表(从路由表得出) | 路由表 |
| 内容 | 目的网络→输出端口+MAC地址信息 | 目的网络→下一跳IP |

**输入端口处理**：物理层收比特→数据链路层剥帧→网络层查表转发(交换路由信息分组→交路由选择处理机；数据分组→排队进交换结构)。

**输出端口处理**：接收交换结构传来的分组→缓存管理→排队→加链路层首部/尾部→发到线路上(交换速率>发送速率时排队，队列满丢弃)。

**三种交换结构**：
| 方式 | 机制 | 特点 |
|------|------|------|
| **通过存储器** | 中断通知处理机→复制到内存→查表→复制到输出口 | 交换速率<M/2(先读后写) |
| **通过总线** | 不需处理机干预，共享总线直接传送 | 总线忙则阻塞；带宽受总线速率限制 |
| **纵横交换结构** | 2N条总线，交叉节点连接输入/输出 | 无阻塞，常称为互连网络 |

### 📝 本节例题

**例1**：路由器不具备的是（　　）。→ **减少冲突和碰撞**(这是交换机/网桥功能而非路由器主干功能)

**例2**：路由器正确说法（　　）。→ **路由器可支持多种网络层协议并提供协议间分组转换**

**例3**：计算路由信息的是（　　）。→ **路由选择处理机**

---

## 4.7 IP 多播

> **定义——IP多播 (IP Multicast)**：一对多通信。源点只发一次，路由器在分叉处复制。1988年Steve Deering首次提出。使用D类IP地址(224.0.0.0~239.255.255.255)。

**资源节约**：单播向90台主机需发送90个；多播只需在分叉点复制，大幅节约带宽。

> **定义——多播路由器 (Multicast Router)**：能运行多播协议的路由器，也可同时转发普通单播。1992年起试验MBONE(多播主干网)。

**多播数据报特点**：
- 目的地址=D类IP(224.0.0.0~239.255.255.255)
- **多播地址只能用于目的地址，不能用于源地址**
- 协议字段=2表示IGMP
- **多播应用仅支持UDP**
- 尽最大努力交付，**不产生ICMP差错报文**(Ping多播地址永远无响应)
- 多播组成员是**主机内的一个进程**

### 多播 MAC 地址映射

IANA以太网多播地址块：**01-00-5E**-00-00-00~01-00-5E-7F-FF-FF。MAC第一字节最低位=1(I/G位)→多播MAC。前25位固定不变，只有后23位可用(2²³=8M个映射)。

D类IP→以太网多播MAC映射：取IP地址低23位放入MAC地址的23位。**D类IP有5位不使用→32个多播IP映射到同一个以太网多播地址**。收到的主机必须在IP层再次过滤。

> 📐 **算例**：224.215.145.230→多播MAC。215.145.230的低23位→十六进制→MAC=**01-00-5E-57-91-E6**。

### IGMP

> **定义——IGMP (Internet Group Management Protocol)**：使多播路由器知道本地网络上多播组成员信息。使用IP数据报传递报文，是IP的组成部分(协议字段=2)。

**版本**：IGMPv1(RFC 1112, 1989, 互联网标准)、IGMPv2(RFC 2236, 1997)、IGMPv3(RFC 3376, 2002)。

**两个工作阶段**：
1. **加入/离开多播组**：主机发IGMP报文声明→本地多播路由器把组成员关系转发给其他多播路由器。加入后主机同时拥有主机IP和多播IP。
2. **探询组成员变化**：多播路由器为**每个网络接口维护一张组成员表**，定期(默认**125秒**/次)发送成员关系查询报文。只要有一个主机响应，就认为该组活跃。经几次探询无响应→不再转发该组关系。

**IGMP降开销措施**：
- 所有通信用IP多播(尽量硬件多播)；对**所有组只发一个询问报文**(125秒)
- 同一网络多个多播路由器时迅速选一个负责探询
- **分散响应**：询问报文中有N(默认10秒)，主机在0~N随机选响应时延(最小时延先发)。同一组内主机**监听其他主机响应**→若已有响应则自己不再发送(抑制机制)
- 参加多个组时每组选不同的随机响应时延

### 多播路由选择协议

> **定义——多播路由选择协议**：使多播路由器协同工作，用最小代价将多播数据报传送给组内所有成员。

**多播转发**：以源主机为根的多播转发树。不同多播组→不同转发树。同一组不同源点→不同转发树。M个源+N个多播组→最多M×N棵树。

**何时停止转发**：路由器某接口接的网络若无某多播组成员→不向该方向转发。多播转发须：(1)动态适应组成员变化(拓扑未变)；(2)不仅依据目的地址，还要考虑数据报从哪来、到哪去；(3)数据报可由未加入组的主机发出或经过无组成员的网络。

### 📝 本节例题

**例1**：多播服务多用的IP类型是（　　）。→ **D类**

**例2**：224.215.145.230→多播MAC? → 215.145.230低23位→**01-00-5E-57-91-E6**

---

## 4.8 虚拟专用网 VPN 和网络地址转换 NAT

### 4.8.1 VPN

**背景**：IP地址紧缺(机构申请的地址数远小于主机数)；互联网不安全(不需全部主机接入外网)；机构内部通信可用本地地址自行分配。

> **定义——本地地址 vs 全球地址**：本地地址仅机构内部使用，可自行分配不需申请；全球地址全球唯一，需向互联网管理机构申请。专用地址只能作本地地址。**互联网中所有路由器对目的地址为专用地址的数据报一律不转发**。

> **定义——专用地址 (Private Address, RFC 1918)**：
> - 10.0.0.0/8 (A类, 1个地址块, 10.0.0.0~10.255.255.255)
> - 172.16.0.0/12 (B类,**连续16个**, 172.16.0.0~172.31.255.255)
> - 192.168.0.0/16 (C类,**连续256个**, 192.168.0.0~192.168.255.255)

专用地址=**可重用地址 (reusable address)**。采用专用地址的网络=**专用网(本地互联网)**。

> **定义——VPN (Virtual Private Network)**：利用公用互联网作为通信载体，通过**隧道技术**加密传输内部数据。效果上等同于专用网。

**VPN构建要求**：经互联网传送的数据**必须加密**；每个场所需专门软硬件配置，使各场所VPN系统**互知对方地址**。

> **定义——隧道技术**：两端通过**封装/解封装**在公网建立数据通信道。隧道由隧道协议构建。**隧道技术是VPN最关键的核心技术**。

**隧道封装算例**：X(10.1.0.1)→R1(全球125.1.2.3)→互联网→R2(全球194.4.5.6)→Y(10.2.0.3)。内部数据报源10.1.0.1目的10.2.0.3→加密后作为外部数据报的数据部分，外部首部源=125.1.2.3目的=194.4.5.6。

**VPN三种类型**：
| 类型 | 说明 |
|------|------|
| **内联网 (Intranet)** | 同一机构内部网络构成的VPN |
| **外联网 (Extranet)** | 一个机构和某些外部机构共同建立的VPN |
| **远程接入 VPN (Remote Access VPN)** | 允许外部流动员工通过接入VPN建立隧道访问公司内网 |

---

### 4.8.2 NAT

> **定义——NAT (Network Address Translation)**：1994年提出。在NAT路由器上安装NAT软件，将内部专用地址转换为全球IP地址。NAT路由器至少有一个有效的外部全球IP地址。

**NAT地址转换表**(实例)：

| 方向 | 字段 | 旧地址 | 新地址 |
|------|------|--------|--------|
| 出 | 源IP | 192.168.0.3 | 172.38.1.5 |
| 入 | 目的IP | 172.38.1.5 | 192.168.0.3 |

**两次地址转换**：离开专用网时替换**源地址**(内部→全球)；进入专用网时替换**目的地址**(全球→内部)。

**数据点**：n个全球IP→最多同时n台主机接入互联网。其余轮流使用。**通信必须由内部主机发起→内部主机不能充当服务器程序**。

> **定义——NAPT (Network Address and Port Translation)**：使用运输层端口号，让多台内部主机**共用一个全球IP**。出方向替换源IP+源端口；入方向从IP数据报数据部分找出端口号→查NAPT表找到目的主机。

> ⚠️ 不使用端口号的NAT叫**传统NAT (traditional NAT)**。**NAT本身并不能节省IP地址**。

**NAPT地址转换表实例**：
| 方向 | 旧IP:端口 | 新IP:端口 |
|------|----------|----------|
| 出 | 192.168.0.3:30000 | 172.38.1.5:40001 |
| 出 | 192.168.0.4:30000 | 172.38.1.5:40002 |

### 📝 本节例题

> 同一VLAN中的两台主机（　　）。→ **可以跨越多台交换机**(VLAN是逻辑组，与物理位置无关)

---

## 4.9 多协议标签交换 MPLS

> **定义——MPLS (Multi-Protocol Label Switching)**：给IP数据报打上固定长度**标签**，在第二层用硬件按标签转发。**没有取代IP，而是IP增强技术**。"多协议"=上层可采用多种协议。

**背景**：传统IP转发网络大时查路由表费时，突发通信缓存溢出→分组丢失/时延增大/QoS下降。

**标签交换**：对打标签的IP数据报在第二层用硬件转发。支持多种链路层协议(PPP、以太网、ATM、帧中继)。

> **定义——LSR (Label Switching Router，标签交换路由器)**：同时具备**标签交换**(快速转发)和**路由选择**(构造转发表)两种功能。

> **定义——LSP (Label Switched Path，标签交换路径)**：各LSR用**LDP(Label Distribution Protocol, 标签分配协议)**交换报文找出的路径。像一条虚连接。

**MPLS 工作过程**：
1. 找出LSP(各LSR用LDP交换报文)
2. **入口节点 (Ingress Node)** → 打标签=分类
3. **中间LSR** → 标签对换(**Label Swapping**)：转发+更新标签。**标签仅在两个LSR间有意义**。
4. **出口节点 (Egress Node)** → 去除标签，交付给非MPLS设备

**分类**：第三层(IP首部的源/目的IP)、第四层(增加TCP/UDP端口号)、第五层(检查有效载荷)。大多数运营商实现第四层分类。

> **定义——FEC (Forwarding Equivalence Class，转发等价类)**：路由器按同样方式对待的分组集合。"同样方式"=同样接口转发+同样下一跳+同样服务类别+同样丢弃优先级。FEC与标签**一一对应**。FEC划分由网络管理员控制，同一FEC指派同样标签。

**FEC 用途**：负载平衡。传统最短路径A→B→C可能过载；设置两种FEC可使流量均衡(TE, Traffic Engineering)。

### MPLS 首部格式

```
┌────────────┬──────┬───┬──────┐
│  标签值20位  │试验3位│S位│TTL 8位│
└────────────┴──────┴───┴──────┘
           ← 4 字节 →
```

| 字段 | 位数 | 含义 |
|------|------|------|
| 标签值 | 20位 | 最多2^20=1048576个流 |
| 试验 | 3位 | 保留用作试验 |
| 栈S | 1位 | 用于标签栈 |
| TTL | 8位 | 防止MPLS分组在MPLS域中兜圈子 |

MPLS首部位于第二层(数据链路层)和第三层(网络层)之间。

---

## 4.10 软件定义网络 SDN

> **定义——SDN (Software Defined Network)**：将网络**控制层面**从路由器中分离出来，由**远程控制器**集中计算路由并下发转发表，路由器仅保留数据层面负责快速转发。

SDN的核心变革：传统路由器自己既做路由选择（控制层面）又做分组转发（数据层面）。SDN把路由选择集中到控制器，路由器变成"只转发不思考"的设备。

| | 传统网络 | SDN |
|------|------|------|
| 控制层面 | 每个路由器各自运行 | 远程控制器集中管理 |
| 数据层面 | 每个路由器自行转发 | 路由器按控制器下发的转发表转发 |
| 灵活性 | 每台设备需单独配置 | 控制器统一编程下发 |
| 优点 | 分布式、稳健 | 全局优化、灵活、易管理 |

### 📝 本节例题

> SDN将网络层的（　　）层面和（　　）层面分离，由远程控制器集中管理。→ **答案：数据；控制**

---

## 附录：第4章完整定义与公式汇总

### 核心定义(43条)

| 编号 | 术语 | 严格定义 |
|------|------|---------|
| 1 | **虚电路 VC** | 面向连接。通信前建立逻辑连接，所有分组沿同一路径传。 |
| 2 | **数据报服务** | 无连接。每个分组独立发送，不同分组可走不同路径。 |
| 3 | **数据层面** | 根据转发表转发分组，硬件实现。 |
| 4 | **控制层面** | 根据路由协议计算路由表，软件实现。 |
| 5 | **SDN** | 控制层面集中到远程控制器，下发转发表。 |
| 6 | **直接交付** | 源/目的在同一网络，不需路由器。 |
| 7 | **间接交付** | 经过路由器转发，非直接交付。 |
| 8 | **跳(Hop)** | 分组在两设备间的传递单位。 |
| 9 | **IP地址** | 32位，标志主机/路由器接口。点分十进制。 |
| 10 | **CIDR** | 无分类编址。`地址/N`。消除A/B/C类。 |
| 11 | **地址掩码** | 连续1后连续0。网络地址=IP AND 掩码。 |
| 12 | **子网划分** | 借主机位作子网号。2^N≥子网数。 |
| 13 | **路由聚合(超网)** | 合并连续子网为更大CIDR块。 |
| 14 | **ARP** | IP→MAC地址解析。广播请求+单播响应。 |
| 15 | **ARP高速缓存** | IP→MAC映射表，动态更新。 |
| 16 | **TTL** | 生存时间，每跳减1，到0丢弃。 |
| 17 | **最长前缀匹配** | 多条匹配时选前缀最长。 |
| 18 | **主机路由** | /32路由，针对单台主机。 |
| 19 | **默认路由** | 0.0.0.0/0，无匹配时使用。 |
| 20 | **ICMP** | 网际控制报文协议。差错+询问。 |
| 21 | **IPv6** | 128位地址，首部固定40B。 |
| 22 | **任播(Anycast)** | 送到一组中距离最近的一个。IPv6新增。 |
| 23 | **ICMPv6** | IPv6版ICMP，合并ARP+IGMP功能。 |
| 24 | **AS** | 自治系统。单一管理下网络集合。 |
| 25 | **IGP** | AS内部路由协议(RIP, OSPF)。 |
| 26 | **EGP** | AS间路由协议(BGP)。 |
| 27 | **RIP** | 距离向量IGP，跳数度量，最大15。 |
| 28 | **OSPF** | 链路状态IGP，SPF算法，洪泛。 |
| 29 | **BGP** | 路径向量EGP，AS间路由。 |
| 30 | **IP多播** | 一对多。D类地址。路由器复制。 |
| 31 | **IGMP** | 管理多播组成员关系。 |
| 32 | **专用地址** | RFC 1918。10/8,172.16/12,192.168/16。 |
| 33 | **VPN** | 公网加密隧道=专用网效果。 |
| 34 | **NAT** | 专用↔全球地址转换。 |
| 35 | **NAPT** | 端口号转换+多对一。 |
| 36 | **MPLS** | 标签交换，第二层硬件转发。 |
| 37 | **FEC** | 按相同方式转发的分组集。 |
| 38 | **LSR** | 标签交换路由器。 |
| 39 | **LSP** | 标签交换路径。 |
| 40 | **标签对换** | 中间LSR转发+更新标签。 |

### 核心公式

| 编号 | 公式 | 说明 |
|------|------|------|
| F1 | 网络地址 = IP AND 掩码 | 逐位AND |
| F2 | 可用主机 = 2^主机位数 - 2 | 减网络+广播 |
| F3 | 子网数 ≥ 2^借位数 | 借N位→2^N子网 |
| F4 | 检验和 = 反码求和取反 | IP首部16位 |
| F5 | RIP距离=跳数, 16=不可达 | 最大15跳 |
| F6 | OSPF Cost = Σ入接口(参考带宽÷接口带宽) | FE默认cost=1 |
| F7 | 片偏移 = 数据起始位置÷8 | 单位8字节 |
| F8 | 子网划分 2^N ≥ 所需子网数 | N=借位数 |
| F9 | SDN: 控制层面集中 → 远程控制器下发转发表 | — |

### 重要辨析

| 辨析 | 要点 |
|------|------|
| **虚电路 vs 数据报** | VC=网络保证可靠(同路径)；数据报=端系统保证(独立转发) |
| **IP vs MAC** | IP=端到端逻辑寻址(不变)；MAC=每跳物理寻址(逐跳变) |
| **数据层面 vs 控制层面** | 数据=转发(硬件/ns级)；控制=路由选择(软件/s级) |
| **RIP vs OSPF vs BGP** | RIP=DV/跳数/IGP；OSPF=LS/SPF/IGP；BGP=PV/EGP |
| **IGP vs EGP** | IGP=AS内(RIP/OSPF)；EGP=AS间(BGP) |
| **NAT vs NAPT** | NAT=一对一IP；NAPT=多对一+端口 |
| **IPv4 vs IPv6** | v4=32位+检验和+选项；v6=128位+无检验和+定长40B+扩展首部 |
| **专用地址 vs 全球地址** | 专用=内网(10/172.16/192.168)；全球=互联网唯一 |
| **ARP请求 vs 响应** | 请求=广播；响应=单播 |
| **传统NAT vs NAPT** | NAT=一对一IP转换；NAPT=多对一+端口号复用 |
| **SDN vs 传统网络** | SDN=控制集中+远程下发；传统=每路由器自行路由选择 |
