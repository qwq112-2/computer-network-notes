# 计算机网络·单位词典 & 计算题实战手册

> 把"单位"彻底搞懂，是打通计算机网络任督二脉的第一步。因为网络本质上就是**算时间**和**算大小**。本文从第一性原理出发，把单位体系讲透，让所有计算题的数字都"对得上号"。

---

## 一、总纲：计算机网络计算题的"三角关系"

网络里几乎所有的计算题，都是在三个东西之间找关系：

```
        数据量（bit / Byte）
           ↙        ↘
    速率(bps)  ←──→  时间(s)
```

**核心公式**：`数据量 = 速率 × 时间`

你只要知道其中两个，就一定能求出第三个。所有复杂的网络计算——时延计算、帧长计算、信道利用率、窗口大小——本质上都是这个公式的各种变形。

```
发送时延 = 数据量(bit) ÷ 发送速率(bps)
传播时延 = 距离(m) ÷ 传播速度(m/s)
    总时延 = 发送时延 + 传播时延 + 处理时延 + 排队时延
 最小帧长 = 速率(bps) × 争用期(s)
信道利用率 = 发送时延 ÷ (发送时延 + RTT)
  吞吐量 = 数据量 ÷ 总时间
```

> 💡 **第一性原理**：计算机网络所有单位、所有公式，归根到底都是因为**信号不会瞬间到达**——电信号在铜线中约每米5.5纳秒、光在光纤中约每米5纳秒。传播有延迟→碰撞要等待→帧长不能太短→窗口不能太小。理解了"信号跑不快"这个物理事实，就理解了网络设计的底层逻辑。

---

## 二、五大基础单位体系（用户已整理，此处精要重温）

### 第一类：数据量 — "存东西用多大？"

| 单位 | 符号 | 换算 | 注意事项 |
|------|------|------|---------|
| **比特** | bit / **b**（小写） | 最小单元，一个0或1 | ⚠️ 小写b！大小写是血泪坑 |
| **字节** | Byte / **B**（大写） | 1 B = 8 bit | 大写B！计算机处理文字的最小单位 |
| 千字节 | KB | ≈ 1024 B（内存）/ ≈ 1000 B（硬盘广告） | 计算机网络一律用**1000进制**！ |
| 兆字节 | MB | ≈ 1024 KB | 一首MP3约3-5 MB |
| 吉字节 | GB | ≈ 1024 MB | 一部高清电影约1-2 GB |

> 🚨 **最重要的换算**：宽带100Mbps（小写b）= 下载速度12.5 MB/s（大写B）。原因：`100 ÷ 8 = 12.5`。

**网络速率中的K/M/G一律是1000进制（10³），不是1024（2¹⁰）**。这是计算机网络与操作系统/存储器领域最大的单位分歧点。

| 前缀 | 符号 | 网络速率(1000进制) | 存储容量(1024进制) |
|------|------|-------------------|-------------------|
| 千 | K/k | 10³ = 1,000 | 2¹⁰ = 1,024 |
| 兆 | M | 10⁶ = 1,000,000 | 2²⁰ = 1,048,576 |
| 吉 | G | 10⁹ = 1,000,000,000 | 2³⁰ = 1,073,741,824 |
| 太 | T | 10¹² | 2⁴⁰ |

---

### 第二类：速率/带宽 — "每秒能吐出多少比特？"

> **定义——bps (bits per second，比特每秒)**：网络速率的绝对唯一标准单位。表示每秒传输的比特数。

| 单位 | 值 | 典型场景 |
|------|-----|---------|
| bps | 1 bit/s | — |
| Kbps | 1,000 bps | 早期拨号上网(56Kbps) |
| Mbps | 1,000,000 bps | 宽带、以太网(100Mbps/1000Mbps) |
| Gbps | 1,000,000,000 bps | 光纤主干、万兆以太网(10Gbps) |
| Tbps | 10¹² bps | 大型IXP峰值吞吐量 |

**带宽**的两种含义（本质相同）：

| 视角 | 含义 | 单位 | 场景 |
|------|------|------|------|
| **频域** | 信号能通过的频率范围 | Hz (赫兹) | 通信领域、信道划分 |
| **时域** | 单位时间内能传输的最高数据率 | bps | 计算机网络 |

**典型带宽值**：电话线3kHz、Cat5双绞线100MHz、Cat6 250MHz、Cat8 2000MHz。

---

### 第三类：时间 — "等多久？"

| 单位 | 符号 | 换算 | 网络中的意义 |
|------|------|------|------------|
| 秒 | s | 1 s | 人类感知的延迟下限（<100ms=实时感） |
| 毫秒 | ms | 1 s = 10³ ms | 游戏延迟（Ping），20ms极好，100ms卡 |
| 微秒 | **μs** | 1 s = 10⁶ μs | **以太网的主战场**：争用期=51.2μs |
| 纳秒 | ns | 1 s = 10⁹ ns | 芯片处理、光在光纤中1米≈5ns |

> **核心记忆**：1μs信号在电缆中约跑200米。这是以太网设计的物理基石。

---

### 第四类：距离与传播速度 — "信号跑多远？"

| 常数 | 值 | 说明 |
|------|-----|------|
| 真空光速 c | 3.0 × 10⁸ m/s（30万km/s） | 理论极限 |
| 铜线电缆中 | ≈ 2.3 × 10⁸ m/s（≈ 77% c） | 电信号 |
| 光纤中 | ≈ 2.0 × 10⁸ m/s（≈ 67% c） | 光信号 |
| **工程近似值** | **200 m/μs**（即5 ns/m） | 做题用这个！ |

```
传播时延 = 距离(m) ÷ 传播速度(m/s)
         = 距离 ÷ 200,000,000          （光纤）
         ≈ 距离(km) ÷ 200               （近似，单位km→ms）
```

**几何级记忆法**：
- 1 ns → 信号跑约 **0.2 米**
- 1 μs → 信号跑约 **200 米**
- 100 μs → 信号跑约 **20 公里**
- 2500 m 往返 → 约 **51.2 μs**（这就是争用期的来源！）

---

### 第五类：各层PDU名称 — "不同层叫什么？"

| 层次 | OSI编号 | PDU名称 | 处理设备 | 关键字段大小 |
|------|---------|---------|---------|------------|
| 应用层 | L7 | **报文 (Message)** | 应用程序 | — |
| 运输层 | L4 | **段 (Segment)** / 数据报 (UDP) | 操作系统协议栈 | TCP首部20B |
| 网络层 | L3 | **包/分组 (Packet)** / IP数据报 | 路由器 | IP首部20B，MTU=1500B |
| 数据链路层 | L2 | **帧 (Frame)** | 网卡/交换机 | 64B~1518B(以太网) |
| 物理层 | L1 | **比特 (bit)** | 线缆/集线器 | — |

> 💡 **面试题**："路由器处理的是什么？"→ IP数据报（Packet）。"交换机处理的是什么？"→ 帧（Frame）。

---

## 三、第六类：频率与波长 — "信号长什么样？"

无线电频谱是一切无线通信的物理基础，网络工程师必须认识。

### 频率

> **定义——频率 (Frequency)**：单位时间内信号周期性变化的次数，单位 **Hz（赫兹）= 次/秒**。

| 单位 | 换算 | 典型应用 |
|------|------|---------|
| Hz | 1 Hz | 交流电(50/60Hz) |
| kHz | 10³ Hz | 传统电话(0~4kHz) |
| MHz | 10⁶ Hz | FM广播、Cat5双绞线(100MHz)、WiFi(2.4GHz=2400MHz) |
| GHz | 10⁹ Hz | WiFi 5GHz、微波(2~40GHz)、卫星通信 |

### 波长

> **定义——波长 (Wavelength)**：电磁波在一个周期内传播的距离。λ = c / f（光速÷频率）。

| 波段 | 频率 | 波长 | 应用 |
|------|------|------|------|
| 微波 | 2.4 GHz | ≈ 12.5 cm | WiFi、蓝牙 |
| 微波 | 5 GHz | ≈ 6 cm | WiFi 5GHz |
| 红外/光 | ~193 THz | 1550 nm | 光纤通信（长波长） |
| 红外/光 | ~230 THz | 1310 nm | 光纤通信（短波长） |

**光纤中的波长**（频率太高，用波长表示更方便）：
- **850 nm**：多模光纤短波（850nm=0.85μm），1000Base-SX，距离短
- **1310 nm**：单模/多模光纤，1000Base-LX
- **1550 nm**：单模光纤长波，1000Base-LX/10GBase-ER，距离最长

> 📐 **公式**：频率与带宽 — 基带信号经过调制搬移到载波频率上传输。信道带宽(BW)越宽，能承载的最高数据率越高。奈氏准则：最高码元速率=2×BW。

---

## 四、第七类：波特率 vs 比特率 — "一个波形能装多少比特？"

这两个概念极易搞混，但非常关键。

> **定义——波特率 (Baud Rate)**：单位时间内传输的**码元（符号）**个数。单位：**Baud（波特）**。描述的是**波形变化的快慢**。

> **定义——比特率 (Bit Rate)**：单位时间内传输的**比特**个数。单位：**bps**。描述的是**信息量的大小**。

```
比特率 = 波特率 × log₂M

M = 码元的离散状态数（M进制码元）
```

| 场景 | M | 波特率 | 比特率 | 每码元携带 |
|------|---|--------|--------|-----------|
| 二进制(不归零) | 2 | 100 Baud | 100 bps | 1 bit |
| 四相位调制(QPSK) | 4 | 100 Baud | 200 bps | 2 bit |
| 16QAM | 16 | 100 Baud | 400 bps | 4 bit |
| 64QAM | 64 | 100 Baud | 600 bps | 6 bit |
| 256QAM | 256 | 100 Baud | 800 bps | 8 bit |

> 🚨 **曼彻斯特编码的特殊情况**：波特率 = 2 × 比特率。因为每个比特需要2次电平跳变来表示。

**计算公式汇总**：

| 已知 | 求 | 公式 |
|------|-----|------|
| 波特率B, M进制 | 比特率 | R = B × log₂M |
| 比特率R, M进制 | 波特率 | B = R ÷ log₂M |
| 二元调制(曼彻斯特) | 波特率 | B = 2R |

**典型题**：
> 某链路数据速率为2400bps，采用4相位调制(M=4)，波特率是多少？
> 解：B = 2400 ÷ log₂4 = 2400 ÷ 2 = **1200 Baud**

---

## 五、第八类：分贝与信噪比 — "信号有多干净？"

> **定义——分贝 (dB)**：两个功率值的**对数比值**。dB = 10 × log₁₀(P₁/P₂)。

> **定义——信噪比 SNR (Signal-to-Noise Ratio)**：信号功率与噪声功率之比 S/N，常用dB表示。

```
信噪比(dB) = 10 × log₁₀(S/N)

已知dB求比值：S/N = 10^(dB/10)
```

| dB值 | S/N比值 | 信号质量 |
|------|---------|---------|
| 0 dB | 1:1 | 信号=噪声，没法用 |
| 10 dB | 10:1 | 勉强可用 |
| 20 dB | 100:1 | 可以 |
| 30 dB | 1000:1 | 好（电话线典型值） |
| 40 dB | 10000:1 | 很好 |
| 127:1 ≈ | **21 dB** | 香农公式例题中常用 |

> 📐 **香农公式**：C = W × log₂(1 + S/N)。极限速率C由带宽W和信噪比S/N共同决定。

**常见题**：
> 电话信道带宽3kHz，信噪比30dB → S/N = 1000 → C = 3k × log₂(1001) ≈ **30 kbps**

> ⚠️ **注意**：dB是**对数单位**。3dB=功率翻倍/减半，10dB=10倍，20dB=100倍，30dB=1000倍。不是线性！

---

## 六、第九类：误码率 — "传错多少？"

> **定义——误码率 BER (Bit Error Rate)**：传输错误的比特数 ÷ 传输的总比特数。是衡量信道质量的关键指标。

| BER | 含义 | 场景 |
|-----|------|------|
| 10⁻⁷ | 一千万比特错1个 | 好的有线信道 |
| 10⁻¹⁰ | 百亿比特错1个 | 光纤 |
| 10⁻³ | 千分之一 | 差的无线信道 |
| 10⁻¹ | 十分之一 | 不可用 |

**影响BER的因素**：信噪比（↑信噪比→↓BER）、调制方式（高阶调制→↑BER）、数据率（↑速率→↑BER）。

---

## 七、第十类：网络性能指标 — "网络跑得怎么样？"

### 7.1 吞吐量

> **定义——吞吐量 (Throughput)**：单位时间内**实际**通过某个网络的数据量。受限于带宽（绝对上限），是"实际跑出来的"而非"标称"。

| 常见单位 | 说明 |
|---------|------|
| bps / Mbps / Gbps | 比特吞吐量 |
| pps (packets per second) | 包转发率，交换机/路由器核心指标 |
| fps (frames per second) | 帧速率 |

### 7.2 延迟抖动

> **定义——抖动 (Jitter)**：数据包到达时间的**波动范围**（时延的变化幅度）。单位：ms或μs。对VoIP/视频/游戏至关重要。

```
抖动 = 最大时延 - 最小时延（或标准差）
```

### 7.3 信道利用率

> **定义——信道利用率 (Utilization)**：某信道有百分之多少的时间在被使用。利用率0=完全空闲；利用率1=完全占满。

```
D = D₀ / (1 - U)        ← 利用率与时延的关系（排队论）
```

> 💡 利用率不是越高越好！**以太网利用率到30%就已经重载。**

### 7.4 可用性

> **定义——可用性 (Availability)**：网络正常运行时间的百分比。用"几个9"表示。

| 描述 | 百分比 | 年停机时间 |
|------|--------|-----------|
| 三个9 | 99.9% | ≈ 8.76 小时/年 |
| 四个9 | 99.99% | ≈ 52.6 分钟/年 |
| **五个9（电信级）** | 99.999% | ≈ 5.26 分钟/年 |
| 六个9 | 99.9999% | ≈ 31.5 秒/年 |

---

## 八、第十一类：协议规定的"硬数字" — 以太网常数速查

这些数字不需要推导，需要**记住**。它们都是协议设计者根据物理规律定死的。

| 常数 | 值 | 为什么是这个数 |
|------|-----|--------------|
| **最短帧长** | **64 字节** | 10Mbps×51.2μs=512bit=64B。发完64B前碰撞一定可检测 |
| **最长帧长** | **1518 字节**（802.1Q:1522B） | 数据字段最大1500B+头部14B+尾部FCS 4B |
| **最小数据字段** | **46 字节** | 64总长-14首部-4尾部=46。不够要填充 |
| **争用期** | **51.2 μs** | 2500m往返÷200m/μs×2≈25μs→实际取了保守值51.2μs |
| **帧间最小间隔** | **9.6 μs**（96 bit time） | 10Mbps下96bit的发

送时间。给接收方喘息时间 |
| **MAC地址** | **48 位（6字节）** | IEEE规定。高24位OUI+低24位扩展标识符 |
| **VLAN ID** | **12 位（最大4096个）** | 802.1Q标记中的12bit |
| **IP MTU** | **1500 字节** | 以太网数据字段上限 |
| **CSMA/CD重传上限** | **16 次** | 超过16次丢弃帧，报告上层 |
| **退避参数k上限** | **Min[重传次数, 10]** | 退避窗口最大2¹⁰-1=1023 |
| **交换机表项有效期** | 默认**300秒(5分钟)** | 过期自动删除，适配主机更换 |

### 100Mbps快速以太网的变化

| 常数 | 10Mbps | 100Mbps |
|------|--------|---------|
| 争用期 | 51.2 μs | **5.12 μs** |
| 帧间间隔 | 9.6 μs (96 bit) | **0.96 μs** (96 bit) |
| 最短帧长 | 64 B（不变） | 64 B（不变） |
| 网段最大长度 | 2500m | **100m**（用双绞线） |

> 💡 速率提高10倍→争用时间缩小10倍→网段长度必须缩小10倍。这就是为什么100Mbps以太网网线最长100米。

### 吉比特以太网的补充

| 新增机制 | 说明 |
|---------|------|
| **载波延伸** | 半双工下发送帧不足512B→填充至512B（争用期=4096 bit time） |
| **分组突发** | 首帧填充后，后续短帧连续发至1500B |

---

## 九、第十二类：窗口大小与序号空间 — "能连续发多少？"

> **定义——发送窗口 (WT)**：在收到确认前最多可连续发出的帧数（或字节数）。

| 协议 | 发送窗口 WT | 接收窗口 WR | 序号位数n | 示例(n=3) |
|------|-----------|-----------|----------|----------|
| 停止-等待 | 1 | 1 | 1 bit足够 | WT=WR=1 |
| GBN（回退N） | 1 < WT ≤ 2ⁿ-1 | 1 | 3→WT≤7 | WT最大7 |
| SR（选择重传） | 1 < WT ≤ 2ⁿ⁻¹ | 1 < WR ≤ 2ⁿ⁻¹ | 3→WT≤4 | WR+WT≤2ⁿ=8 |

**TCP窗口的特殊性**：TCP的窗口单位是**字节(Byte)**，不是帧数。这是运输层和数据链路层的关键区别。

---

## 十、单位换算速查表（做题必备）

### 数据量

| 从 | 到 | 换算 |
|----|-----|------|
| bit | Byte | ÷ 8 |
| Byte | bit | × 8 |
| MB | bit | × 8 × 10⁶（或 × 8 × 2²⁰，看场景） |
| GB | bit | × 8 × 10⁹ |
| KB | Byte | × 10³（网络）/ × 1024（存储） |

### 时间

| 从 | 到 | 换算 |
|----|-----|------|
| s | ms | × 10³ |
| s | μs | × 10⁶ |
| s | ns | × 10⁹ |
| μs | 信号跑的距离(m) | × 200（工程近似） |
| 距离(m) | μs | ÷ 200（工程近似） |

### 速率

| 从 | 到 | 换算 |
|----|-----|------|
| bps | Kbps | ÷ 10³ |
| bps | Mbps | ÷ 10⁶ |
| bps | Gbps | ÷ 10⁹ |
| 波特率 | 比特率 | × log₂M |
| 曼彻斯特波特率 | 比特率 | × 2 |

### dB

| 从 | 到 | 换算 |
|----|-----|------|
| S/N 比值 | dB | 10 × log₁₀(S/N) |
| dB | S/N 比值 | 10^(dB/10) |

---

## 十一、终极实战：用千兆网卡发一个64字节帧要多久？

> 题目：家里1000Mbps（千兆）网卡，全双工模式，发一个最小64字节的以太网帧，需要多少时间？

```
已知：速率 = 1000 Mbps = 10⁹ bps
      帧长 = 64 Byte = 512 bit

发送时延 = 512 bit ÷ 10⁹ bps
         = 512 × 10⁻⁹ 秒
         = 0.512 微秒
         ≈ 512 纳秒
```

**对比**：

| 速率 | 发64B帧的时间 | 发1518B帧的时间 |
|------|-------------|---------------|
| 10 Mbps | 51.2 μs | 1.214 ms |
| 100 Mbps | 5.12 μs | 0.121 ms |
| 1000 Mbps | **0.512 μs (512 ns)** | 0.012 ms |
| 10 Gbps | 51.2 ns | 1.214 μs |

> 🎯 **千兆网发最小帧只需512纳秒**。在这512纳秒里，电信号只跑了约**100米**。这就是为什么千兆以太网半双工需要"载波延伸"——帧发得太快，512纳秒内碰撞报告来不及从网线远端返回（需要至少1μs），所以必须把最短帧从64B强行拉长到512B来争取时间。

---

## 附录：核心数字速记卡

| 数字 | 含义 | 关键词 |
|------|------|--------|
| **8** | 1 Byte = 8 bit | B↔b换算 |
| **5 ns/m** | 信号在电缆/光纤中跑1米≈5纳秒 | 传播速度 |
| **200 m/μs** | 信号1微秒跑200米 | 工程近似 |
| **51.2 μs** | 10Mbps以太网争用期 | 碰撞窗口 |
| **64 B** | 以太网最短帧长 | 最小帧 |
| **1518 B** | 以太网最长帧长 | 最大帧 |
| **1500 B** | IP MTU（以太网数据字段上限） | MTU |
| **46 B** | 以太网最小数据字段 | 不够要填充 |
| **18 B** | 以太网帧首部(14B)+尾部FCS(4B) | 帧开销 |
| **12.5 MB/s** | 100Mbps宽带理论下载速度 | 100÷8 |
| **48 bit** | MAC地址长度 | 6字节 |
| **9.6 μs** | 10Mbps帧间间隔(96 bit time) | inter-frame gap |
| **16次** | CSMA/CD最大重传次数 | 退避上限 |
| **3kHz** | 电话线带宽 | 香农公式 |
| **30dB** | 电话线典型信噪比(S/N=1000) | 香农公式 |
| **~30 kbps** | 电话线极限速率(C=3k×log₂1001) | 香农极限 |
| **1000 vs 1024** | 网络速率用1000，存储用1024 | K/M/G进制 |
| **log₂M** | M进制码元每码元携带比特数 | 波特→比特 |
| **2W** | 奈氏准则：无噪声最高码元速率(baud) | Nyquist |
| **10 log₁₀(S/N)** | dB换算公式 | 信噪比 |
